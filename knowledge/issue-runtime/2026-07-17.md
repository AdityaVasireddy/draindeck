---
project: issue-runtime
date: 2026-07-17
type: mixed
stack: [python, pytest, git, windows-subprocess, claude-cli]
status: Session 9 committed (eed1760); ADR-22 re-verified live at CLI 2.1.212, B stays landed; vacuity-guard discrimination gap parked as open decision; Step 3 still blocked on 3 user-side preconditions (Ollama model, Issues.md location/format, validation command)

---

# issue-runtime — 2026-07-17

## What I worked on
Session 8 executed the accepted ADR-22 plan: marked ADR-22 Accepted in docs/08 and landed the mechanism (`--setting-sources ""` in engine argv + `HISTORIAN_SWEEP_ACTIVE=1` via `engine.child_env`), backed by 3 new unit tests (106/106 total) and a durability-gate run (60/60 on seeds 42 and 1337). An external reviewer flagged that the "VERIFIED" label on argv-survival was based on an incorrect claim (no shell-join on Windows) and that the harness-pass claim lacked named evidence; both were corrected with real witnesses (a live Popen spawn test, and invocation lines/tails in doc 14 §2.5) before the reviewer cleared the session to commit. Session closed with two commits, NEXT.md updated with a new Step-3 preflight gate and a B-sunset tickle, and a handoff written.

## Cases

### CASE: adr22-setting-sources-empty-over-project-local
status: superseded (by issue-runtime/2026-07-16#empty-setting-sources-preferred-for-spawned-children)
problem: Needed to suppress ambient Claude Code settings contamination from leaking into the engine's child `claude -p` invocation without hardcoding machine-specific values into `src/`.
decision: Accepted `--setting-sources ""` (loads no settings scopes) rather than `--setting-sources project,local`, combined with `HISTORIAN_SWEEP_ACTIVE=1` injected via `engine.child_env` config (not hardcoded in src/), with the child_env layer under an explicit sunset condition.
why: `project,local` would still load settings scope from the child's cwd (the target repo itself), which is a cross-run injection vector — witnessed directly in doc 14 §2.4 Probe 2. Option C was rejected and Option D left as optional separate-repo operator hygiene.
tags: [adr-22, claude-cli, config, isolation]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: `--setting-sources project,local`, plus Option C (rejected outright) and Option D (left as optional operator hygiene rather than adopted).
Tradeoff: `project,local` preserves legitimate project/local scope but was directly witnessed (doc 14 §2.4 Probe 2) still loading settings from the child's cwd, i.e. the target repo — a real cross-run injection vector; the empty form gives up that scope but closes the vector.
Tipping factor: Probe 2 directly witnessed `project,local` still loading settings scope from the child's cwd, confirming the injection vector empirically rather than by inference.

### CASE: argv-verified-label-required-popen-boundary-witness
status: cited
problem: First report claimed the empty `--setting-sources ""` token was VERIFIED to survive to spawn, reasoning that argv passed as a Python list has "no shell-join anywhere in the path to subprocess.Popen."
decision: Corrected the label — Windows `Popen` does join a list argv via `subprocess.list2cmdline()` before `CreateProcess`, so the original rationale was factually wrong even though the outcome (token survives) held. Split the claim into two separately-witnessed legs: Python-side handoff (new live spawn test with real `_command()` output run through actual `subprocess.run()` with a dummy child echoing `sys.argv`) VERIFIED this session, and CLI-side interpretation VERIFIED only via the pre-existing §2.4 probe (a hand-built argv, not literally through `_command()`). The still-missing single end-to-end run through `ClaudeHeadlessEngine.run()` against real `claude` was queued as a new, blocking Step-3 preflight item rather than silently dropped.
why: The reviewer required either a Popen-boundary capture test or a live spawn to justify VERIFIED; the existing unit test only asserted on `_command()`'s return value, which proves list construction, not spawn survival — confirmed on inspection during this session.
tags: [windows, subprocess, verification-label, testing]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: keeping the original VERIFIED label on the strength of the existing unit test, which only asserts on `_command()`'s return value.
Tradeoff: keeping the label is free but rests on a factually wrong rationale (Windows Popen does join list argv via `list2cmdline()`) and a test that proves list construction, not spawn survival; splitting into two separately-witnessed legs costs a new live spawn test but makes the claim honestly scoped to what was actually observed.
Tipping factor: the reviewer required either a Popen-boundary capture test or a live spawn to justify VERIFIED, and inspection confirmed the existing test never crossed that boundary.

### CASE: step3-preflight-item0-is-hard-gate-not-checklist
status: distilled
problem: After splitting the argv-survival evidence into two isolated legs (Python handoff + CLI probe), there was no single run proving the two compose correctly end-to-end through the real engine path.
decision: Added this as NEXT.md Step-3 preflight item 0, explicitly framed as a hard gate that must fail loudly on a live smoke run if the real spawn ever mangles the empty token in a way the two isolated legs didn't predict — not just an optional checklist line.
why: Reviewer's closing verdict explicitly called out that a sunset/gate item silently downgraded to a checklist line risks passing quietly instead of failing loudly if the real spawn diverges from the isolated legs.
tags: [step-3, testing, gating, adr-22]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: recording item 0 as an optional checklist line, to be run whenever convenient before Step 3.
Tradeoff: a checklist line is easier to skip under time pressure and risks passing quietly if the real spawn diverges from the two isolated legs; a hard gate costs a firm blocking step but fails loudly instead of silently.
Tipping factor: the reviewer's closing verdict explicitly warned that a sunset/gate item silently downgraded to a checklist line risks passing quietly instead of failing loudly if the real spawn diverges from what the isolated legs predicted.

### CASE: b-sunset-condition-needs-explicit-cli-bump-tickle
status: distilled
problem: Option B (`HISTORIAN_SWEEP_ACTIVE=1` via child_env) has a sunset condition (removable after one clean CLI-upgrade cycle with re-pinned probes) that depends on a future event nobody is scheduled to check.
decision: Added an explicit tickle in NEXT.md tied to the next CLI version bump specifically, rather than leaving the sunset condition only documented in ADR-22 prose.
why: Reviewer noted a sunset condition depending on a future upgrade cycle is the kind of thing that "silently outlives its rationale if nothing points at it."
tags: [adr-22, sunset, next-md, maintenance]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: leaving the sunset condition documented only in ADR-22 prose, trusting a future session to notice it when the CLI is next upgraded.
Tradeoff: prose-only documentation costs nothing now but depends on someone independently remembering to check it against a future event nobody is scheduled to watch for; an explicit NEXT.md tickle costs one more tracked line but ties the condition to a concrete trigger.
Tipping factor: the reviewer noted that a sunset condition depending on a future upgrade cycle is exactly the kind of thing that silently outlives its rationale if nothing points at it.

### CASE: stale-readonly-temp-dir-cleared-not-worked-around
status: distilled
problem: Durability gate runs failed against `%TEMP%\ch` due to leftover git objects from unrelated 2026-07-11/13 harness runs that Windows marks read-only, which `shutil.rmtree` can't delete without clearing the attribute first.
decision: Cleared read-only attributes on the pre-existing, outside-the-repo disposable scratch data, then ran the gate in fresh subdirectories (`ch2`, `ch3`) rather than working around or skipping the gate.
why: Identified as disposable harness scratch outside the repo (not user work), so clearing it was judged safe, and the alternative (bypassing/skipping the harness) was rejected as masking a code failure that wasn't actually present.
tags: [durability-gate, windows, temp-files, harness]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: working around the read-only leftover objects by skipping or bypassing the durability gate rather than clearing them.
Tradeoff: skipping the gate avoids the cleanup step entirely but risks masking a genuine code failure behind an environment artifact; clearing the read-only attributes costs a manual step but keeps the gate meaningful.
Tipping factor: the leftover objects were identified as disposable harness scratch data outside the repo, not user work, making the clear-and-rerun path safe, while bypassing the gate was rejected as masking a failure that wasn't actually present.

### CASE: ambient-sweep-log-excluded-from-commit-per-precedent
status: cited
problem: `knowledge/.sweep/sweep.log` shows as modified in working tree at session end; unclear whether it belongs in the session's commit.
decision: Left it unstaged/excluded from both commits, following the precedent that Session 7 explicitly excluded the same file.
why: Confirmed it is ambient output from the engineering-historian's own SessionEnd hook, not this session's work product.
tags: [git, commit-scope, ambient-files]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: including `knowledge/.sweep/sweep.log` in the session's commit since it was present as a modified file at session end.
Tradeoff: including it is simpler (one less file to exclude) but attributes ambient historian-hook output to this session's work product; excluding it costs a small manual staging decision but keeps the commit scoped to actual deliverables.
Tipping factor: the file was confirmed to be ambient output from the engineering-historian's own SessionEnd hook, not this session's work, and Session 7 had already established the precedent of excluding it.

Also noted (not drafted): including the pre-existing Session 7 handoff doc (`docs/handoffs/HANDOFF_...`) in the Session 8 commit since it was untracked repo documentation already reviewed and kept.

## Commands that worked
None — the transcript narrates command execution and results but does not include verbatim command text or shell invocations.

## Problems solved
- Symptom: durability-gate harness failed against `%TEMP%\ch`. Root cause: stale read-only git objects left by unrelated harness runs from 2026-07-11/13. Fix: clear read-only attributes on that scratch data, then run in fresh subdirectories.
- Symptom: argv-survival claim labeled VERIFIED was based on a false "no shell-join on Windows" rationale. Root cause: `subprocess.Popen` with list argv on Windows still joins via `subprocess.list2cmdline()` before `CreateProcess`; the existing unit test only checked `_command()`'s return value, not the actual spawn boundary. Fix: ran a live spawn test with a dummy child echoing `sys.argv`, then relabeled the claim into two honestly-scoped, separately-witnessed legs.

## Mistakes
- Asserted VERIFIED for argv-survival with an incorrect technical rationale (no shell-join) and without naming which test provided the witness; corrected after external review to name the actual test (`_command()`-return-only) and add real spawn-boundary evidence.
- Doc 14 §2.5's title/header still read as a blanket "VERIFIED" after the initial (incorrect) claim was drafted; fixed to match the corrected, split labels.

## Prompts worth keeping
None.

## Questions I still have
- Whether the single end-to-end run through `ClaudeHeadlessEngine.run()` against the real `claude` binary (Step-3 preflight item 0) will confirm the two isolated legs (Python handoff + CLI probe) actually compose as expected — not yet executed.
- Which of Step 3's other preconditions (beyond item 0) remain unconfirmed — NEXT.md lists them but this session didn't work through them.

## TODOs for next session
- Run Step-3 preflight item 0: live end-to-end argv survival through `ClaudeHeadlessEngine.run()` against the real `claude` binary before Step 3 starts.
- Work through the remaining Step-3 preconditions listed in NEXT.md.
- On the next `claude` CLI version bump, execute the ADR-22 re-pin discipline (control probe + Probe-2-style re-witness) per the new NEXT.md tickle before removing Option B.

## Session 2

### What I worked on
Session 9 executed the ADR-22 upgrade re-pin discipline triggered by a CLI version bump (2.1.211 → 2.1.212), re-verifying ADR-18/21/22 live against the installed `claude 2.1.212` and sweeping Step 3's five NEXT.md preconditions plus a new preflight item 0. Two live probes ran in fresh scratch dirs: Probe B (production argv, `--setting-sources ""` + fence) re-verified clean at the full 450s poll with a new corroborating signal (no entry at all in the historian's `skips.log`, meaning settings never loaded and the hook never fired); Probe A (control, no suppression) failed to reproduce the original contamination, traced to the historian hook's own upstream source having apparently been patched independently. A same-day follow-up review flagged that root-cause claim as an unsupported inference (no prior line-anchored record of the original vulnerable code existed to diff against) and asked for a vacuity-guard gap to be recorded separately; both were addressed by relabeling the claim INFERRED and adding a new standalone NEXT.md item. Item 0 was designed (doc 14 §2.6) but explicitly not run, per scope. No `src/` changes, no commit until the user said "commit" at session close, then a handoff was written.

### Cases

### CASE: probe-a-rootcause-downgraded-to-inferred
status: distilled
problem: Probe A (control run meant to reproduce the original settings-contamination bug) could not reproduce contamination; the initial write attributed this to "the historian hook's own source having been independently patched upstream to gate on transcript-existence before writing," and this was on track to be recorded as VERIFIED / root-caused.
decision: Downgraded the claim to INFERRED in both doc 14 §2.6 and NEXT.md, keeping the VERIFIED observations (contamination occurred before, doesn't now, hook completed per `skips.log`) explicitly separate from the causal attribution, after confirming no original-state record of the vulnerable code (no prior quote/diff, no git history in `~/.claude/historian`) existed to compare against current source.
why: A negative result (no reproduction) has multiple possible causes — real upstream patch, probe-environment divergence from original repro conditions, or a local confound in this repo's probe env — and only one was being claimed as fact; the review required either a direct before/after comparison to keep VERIFIED, or a downgrade.
tags: [evidence-discipline, verification-label, adr-22, historian-hook]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: recording the initial "independently patched upstream" attribution as VERIFIED / root-caused, since it was a plausible and specific-sounding explanation.
Tradeoff: keeping it VERIFIED reads as more conclusive and closes the question, but a negative result (no reproduction) has multiple possible causes (real upstream patch, probe-environment divergence, a local confound) and only one was being claimed as fact; downgrading to INFERRED is less satisfying but honest about what was actually shown.
Tipping factor: no original-state record of the vulnerable code existed anywhere to diff against current source, so the causal attribution had no direct evidence — only the observation that contamination doesn't reproduce now, which the review required be kept separate from the causal claim.

### CASE: vacuity-guard-gap-parked-not-resolved
status: distilled
problem: With Probe A no longer able to reproduce contamination, there is no discriminating signal left to prove `--setting-sources ""` (A-empty) is still doing real suppression work versus being a no-op now masked by an unrelated upstream fix to the historian hook — a clean Probe B is consistent with both interpretations.
decision: Recorded this as its own distinct NEXT.md item (separate from the existing B-sunset tickle), naming two un-chosen options for whoever next touches ADR-22 re-pinning — (a) build a synthetic project-scope hook reproducing the original write-before-check shape to re-establish an independent control, or (b) retire the control and rely on structural/unit evidence instead — without picking one. B (`HISTORIAN_SWEEP_ACTIVE=1`) stays landed and its sunset is not triggered by this finding.
why: The follow-up review explicitly framed this as a lost discriminating signal that a future session must resolve before trusting the control again, not something to silently paper over by treating the clean Probe B result as sufficient on its own.
tags: [adr-22, vacuity-guard, next-md, open-decision]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: treating the clean Probe B result as sufficient on its own to trust the A-empty suppression as still doing real work.
Tradeoff: accepting Probe B alone is simpler and lets Step 3 proceed sooner, but a clean Probe B is equally consistent with "suppression still works" and "there's nothing left to suppress" — an unresolved ambiguity; parking it as a named open decision costs continued uncertainty but avoids trusting an indistinguishable signal.
Tipping factor: with Probe A no longer able to reproduce contamination, there is no discriminating signal left to tell "suppression works" apart from "nothing is contaminable anymore" — a future session must resolve this before trusting the control again.

### Commands that worked
None — the transcript narrates probe execution and polling but does not include verbatim command text.

### Problems solved
- Symptom: durability/evidence review flagged the Probe A root-cause claim as unsupported. Root cause: no prior line-anchored record of the historian hook's original vulnerable code existed to compare against current source, making "independently patched upstream" an inference dressed as a finding. Fix: relabeled INFERRED, kept VERIFIED facts separate, and explicitly ruled out one alternative cause (local confound: `HISTORIAN_SWEEP_ACTIVE` unset, `jq` present and functioning per the well-formed `skips.log` entry, no disable flags) while flagging a second alternative (probe-methodology divergence) as residual, un-ruled-out uncertainty.

### Mistakes
- Initial draft of doc 14 §2.6 stated the Probe A root-cause ("independently patched upstream") flatly, without distinguishing it from the separately-witnessed VERIFIED facts; corrected after review to explicit INFERRED labeling with the causal claim isolated from the observations.

### Prompts worth keeping
None.

### Questions I still have
- Whether cause 2 (probe-methodology divergence between the original repro conditions and this session's Probe A) is actually ruled out — left flagged as residual, unresolved uncertainty rather than dismissed.
- Which of options (a) synthetic control hook or (b) retire-the-control-and-rely-on-structural-evidence should be chosen for the vacuity-guard gap — left open for the next ADR-22 re-pinning session.

### TODOs for next session
- Whoever next touches ADR-22 re-pinning must pick option (a) or (b) on the vacuity-guard control before trusting Probe A as a discriminating signal again.
- Resolve the three Step-3 blockers, none of which are Claude Code's to fix: pull `qwen2.5-coder` in Ollama (currently only `qwen2.5vl:7b`); move `Issues.md` to repo root and reformat from a numbered list to `## <id>: <title>` (currently at `docs/Issues.md`); supply a validation command (no test-runner config exists anywhere in the repo, blocking baseline-green).
- Run Step-3 preflight item 0 (designed in doc 14 §2.6, not yet run) once the above blockers clear.
