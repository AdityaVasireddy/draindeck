---
project: issue-runtime
date: 2026-08-08
type: mixed
stack: [python, powershell, git, claude-code-cli, markdown/adr, yaml]
status: issue-runtime clean at 234af18 (docs/reviews pair committed, session-38 handoff drafted at docs/handoffs/session-38-close.md but not committed); StockPhotoAgent backlog 13-28 done except issue 25 (needs-decomposition, unresolved) and an unconfirmed issue-28 Issues.md-vs-event-log discrepancy; full-codebase audit completed with tiered findings (Tiers A-D, 20 issues total, 71/108 files reviewed, 100% src/tests coverage); B-CRIT-1 finding (orphan-detection unfalsifiability) verified mechanically; reconciler-86 residue confirmed inert and left-as-is per user.
---

# issue-runtime — 2026-08-08

## What I worked on
Started with a read-only cold-start check across issue-runtime and StockPhotoAgent, which surfaced a mismatch (expected `refs/attempts/_recovery/reconciler-86` residue ref was absent). Ran `/doctor` to clean up the Claude Code install — disabled three unused skills/plugin, kept two low-use ones at the user's discretion, and set `auto` as the default permission mode. Then ran a three-vector `bypassPermissions` probe of Write/Edit-tool cwd-escape behavior using the runtime's real `engine._command()`/`engine.run()` argv against a disposable sandbox, and wrote up the findings as ADR-21 Amendment 3 (committed `c39f1c7`, docs-only). Closed with a session-36 handoff (committed `04b11fc`) that explicitly declined to assert the reconciler-86 ref was resolved, filing it as an open cross-repo question instead.

## Cases

### CASE: reconciler-86-cross-repo-ref-question
status: cited
problem: Executor prompt instructed the handoff to record `refs/attempts/_recovery/reconciler-86` as "confirmed GONE... carry-forward item closed," but this session's cold-start check only queried issue-runtime's ref namespace, and session-34's handoff places that ref in StockPhotoAgent instead.
decision: Declined to assert closure; filed it as an open, non-blocking question in the handoff instead.
why: The ref lives in a different repo's namespace than the one checked this session, so an empty result in issue-runtime cannot be read as the ref being gone — user confirmed this deviation was correct and accepted.
tags: [git, recovery, cross-repo, handoff]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: asserting the ref was "confirmed GONE... carry-forward item closed" per the executor prompt's literal instruction, based on the empty result from checking only issue-runtime's namespace.
Tradeoff: asserting closure per the literal instruction would satisfy the prompt as written, but the ref lives in a different repo's namespace than the one checked this session, so an empty result there cannot be read as the ref being gone; declining to assert and filing an open question costs leaving the item unresolved for another session, but avoids a false closure claim.
Tipping factor: the ref lives in a different repo's namespace than the one checked this session, so an empty result in issue-runtime cannot be read as the ref being gone — user confirmed this deviation was correct and accepted.

### CASE: powershell-out-file-noNewline-utf16-garble
status: cited
problem: Pasting git diff/status output through a PowerShell temp-file redirect (plain `>` / `Out-File -NoNewline`) repeatedly produced garbled, spaced-out output when read back.
decision: Switched to `Set-Content -Encoding utf8` (without `-NoNewline`) for all temp-file round-trips of git output on this machine.
why: Plain `>` defaults to UTF-16LE, and `Out-File -NoNewline` on piped input strips newlines *between* lines, not just the trailing one — together they corrupted the file when read back as UTF-8.
tags: [powershell, encoding, git, debugging]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: continuing with the plain `>` / `Out-File -NoNewline` redirect pattern after the first garbled read-back.
Tradeoff: continuing with the existing pattern would require no change, but plain `>` defaults to UTF-16LE and `Out-File -NoNewline` on piped input strips inter-line newlines, together corrupting the file when read back as UTF-8; switching to `Set-Content -Encoding utf8` costs adjusting the command pattern but produces byte-clean round-trips.
Tipping factor: plain `>` defaults to UTF-16LE, and `Out-File -NoNewline` on piped input strips newlines between lines, not just the trailing one — together they corrupted the file.

### CASE: doctor-disable-unused-skills-plugin
status: distilled
problem: `/doctor` usage audit (50 sessions, 13 days) found the crucible and find-skills user skills and the understand-anything plugin at 0 real feature uses in the window (crucible 0 ever, find-skills 1 use pre-window, understand-anything's high counter was inflated by a per-Bash-call hook, not real skill dispatch).
decision: Disabled all three via `~/.claude/settings.json` (`skillOverrides` off, plugin disabled), removing ~1.4k tokens of resident context per session.
why: Zero real usage in the 13-day window despite issue-runtime being the most active project, and understand-anything's apparent usage was a hook artifact, not genuine feature use.
tags: [claude-code, config, doctor, context-budget]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: keeping the three zero-use skills/plugin enabled since they might see future use.
Tradeoff: keeping them enabled preserves optionality for possible future use, but they showed 0 real feature uses across 50 sessions/13 days despite issue-runtime being the most active project, and one's apparent usage was a hook artifact, not genuine dispatch; disabling them costs losing that optionality but reclaims ~1.4k tokens of resident context per session.
Tipping factor: zero real usage in the 13-day window despite issue-runtime being the most active project, and understand-anything's apparent usage was a hook artifact, not genuine feature use.

### CASE: doctor-keep-low-use-skills-user-override
status: distilled
problem: caveman (26 uses historically, 0 in the 13-day window) and impeccable (6 uses historically, 0 in window, most expensive of the four candidates) were flagged by the same audit as removal candidates alongside crucible/find-skills/understand-anything.
decision: Kept both enabled, overriding the assistant's remove recommendation.
why: User's explicit call, recorded as "your call" — despite zero recent use, both had real historical use and were not removed.
tags: [claude-code, config, doctor, user-preference]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: disabling caveman and impeccable along with the other three zero-use candidates, per the assistant's recommendation.
Tradeoff: disabling them would follow the same zero-recent-use logic applied to the other three, reclaiming more context budget, but both had real historical use (26 and 6 uses respectively) unlike the other candidates; keeping them enabled costs retaining unused context now but preserves tools the user judged still valuable.
Tipping factor: user's explicit call, recorded as "your call" — despite zero recent use, both had real historical use and were not removed.

## Commands that worked
- `git rev-parse --abbrev-ref HEAD`, `git log --oneline -N`, `git status --porcelain --untracked-files=all`, `git stash list`, `git diff -- config.yaml`, `git for-each-ref refs/attempts/_recovery/` — full cold-start read-only check across both repos.
- `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` + `Set-Content -Encoding utf8` (no `-NoNewline`) to route git output through a temp file without corruption — used successfully three times after the initial garble.
- `git -C C:\Projects\issue-runtime add docs/08-session-0-closure-and-adr-amendments.md && git commit -m "docs(adr): ADR-21 Amendment 3 — Write/Edit cwd-escape probe-verified, residual open"` — produced `c39f1c7`, 1 file, 16 insertions, verified via `--numstat`.
- `git -C C:\Projects\issue-runtime add docs/handoffs/HANDOFF_2026-08-08_session36-adr21-amendment3-write-escape-probed.md && git commit -m "docs(handoff): session-36 close — ADR-21 Amendment 3 shipped, Write/Edit cwd-escape residual documented open"` — produced `04b11fc`, 1 file, 55 insertions.
- Three headless `-p` bypassPermissions child probes built from the real `engine._command()`/`engine.run()` (imported unmodified from `src/runtime/engine/claude_headless.py`) against a disposable sandbox at `$env:TEMP\wtprobe` — ran to completion, raw stream-json events captured for all three vectors.

## Problems solved
Garbled git-diff/status output when read back from a PowerShell temp-file redirect → root cause was plain `>` defaulting to UTF-16LE combined with `Out-File -NoNewline` also stripping inter-line newlines → fixed with `Set-Content -Encoding utf8` (no `-NoNewline`), confirmed byte-clean on three subsequent reads.

## Mistakes
First attempt at routing the ADR-21 diff through a temp file used a plain `>` redirect (per the user's initial suggested command) and produced spaced-out, corrupted text; corrected by switching to `Set-Content -Encoding utf8` for all subsequent temp-file round-trips this session.

## Prompts worth keeping
The cold-start check prompt (explicit "read-only, do NOT modify/commit/run," ordered command list, and a pre-stated "Expected" block asking for mismatches to be reported plainly with no corrective action) cleanly surfaced the reconciler-86 discrepancy without any risk of the session acting on stale assumptions.

## Questions I still have
Is `refs/attempts/_recovery/reconciler-86` actually gone, or does it still exist in StockPhotoAgent's own ref namespace (never checked this session)? Left open in the handoff per user instruction.

## TODOs for next session
- Check StockPhotoAgent's `refs/attempts/_recovery/` namespace for `reconciler-86` before treating it as closed.
- Triage the two untracked `docs/reviews/` files (`coverage-ledger.md`, `full-codebase-review.md`).
- Gap 2 live-run watch: the validation hook shipped in `6cb2f3d` is unit-tested only — first live `cmd_run` with it active needs a pre-committed outcome matrix.

## Session 2 (20:40)

### What I worked on
Continued the same day with a live-fire exercise of the Gap 2 new-file validation hook against the real StockPhotoAgent target. A first scoped run (intended for issues 13/14) drained with zero executions because eligibility was read from `Issues.md`'s stale STATUS text instead of the event log; the actual backlog (issues 1-26) was already fully terminal. Authored and shipped two new issues as live-fire test cases — issue 27 (`_stem_without_edit_suffix` chained-suffix bug) and issue 28 (`_parse_score_response` stray-brace JSON bug) — where issue 27's fix landed but didn't exercise Gap 2 because its text lacked an explicit acceptance bullet, so issue 28 added one and the hook fired, confirmed via `ValidationPassed` gate_results. Separately re-verified the reconciler-86 residue ref: it lives in StockPhotoAgent (not issue-runtime, resolving session 36's cross-repo question), points to a dangling, inert "reconciler dirty-workspace" snapshot from 2026-08-01, and was left as-is per the user's decision. Closed with a session-37 handoff (commit `a7b4f13`).

### Cases

### CASE: issue-acceptance-bullet-forces-gap2-test-file
status: distilled
problem: Issue 27 (`_stem_without_edit_suffix` chained-suffix bug) shipped correctly but the child treated an ad-hoc `python -c` sanity check as sufficient proof and never authored a persisted test file, so the Gap 2 new-file validation hook had nothing to match and never fired.
decision: Issue 28 was written with an explicit acceptance bullet in issue-26's proven phrasing ("Tests live in `tests/test_<module>.py` and all pass"), which forced the child to author `tests/test_image_processor.py`; the run's `ValidationPassed` gate_results showed a second, dynamically-appended gate targeting that exact file, confirming Gap 2 fires correctly.
why: Issue 27's text lacked the acceptance bullet issue 26 had used, and that omission is what let the child skip writing a real test file — stated explicitly in the session's own root-cause analysis.
tags: [validation, gap2, issue-authoring, testing]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: accepting issue 27's ad-hoc `python -c` sanity check as sufficient proof, without an explicit acceptance-bullet requirement in the issue text.
Tradeoff: accepting an ad-hoc sanity check costs nothing extra to write into the issue, but the Gap 2 validation hook had nothing to match and never fired, so the fix shipped without dynamic new-test-file gating; adding an explicit acceptance bullet costs one more line of issue-authoring discipline but reliably forces a persisted test file the hook can target.
Tipping factor: issue 27's text lacked the acceptance bullet issue 26 had used, and that omission is what let the child skip writing a real test file — confirmed by issue 28 adding the bullet and the hook firing correctly.

### CASE: reconciler-86-resolved-inert-left-as-is
status: distilled
problem: Session 36 left open whether `refs/attempts/_recovery/reconciler-86` (flagged absent by a cold-start check scoped only to issue-runtime) was actually gone or merely lived in StockPhotoAgent's own ref namespace, which had never been checked.
decision: Confirmed the ref exists in StockPhotoAgent, points to a dangling (unreachable from agent-work, no containing branch, no reflog) runtime-authored "reconciler dirty-workspace" snapshot commit dated 2026-08-01; disposition is to leave it as-is with no deletion and no GC recording.
why: It matches the preserve-residue/dirty-workspace-check snapshot pattern documented in prior session handoffs, is unreachable and harmless with no fence/run impact — user's explicit call ("leave-as-is per Adi").
tags: [git, recovery, reconciler-86, cross-repo]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: deleting the dangling reconciler-86 snapshot commit or recording it for garbage collection, now that it was confirmed inert.
Tradeoff: deleting or GC-recording it would tidy up the ref namespace, but it's unreachable and harmless with no fence/run impact, matching the documented preserve-residue/dirty-workspace-check snapshot pattern; leaving it as-is costs a lingering dangling ref but avoids touching state that matches a known-benign pattern for no functional gain.
Tipping factor: it matches the preserve-residue/dirty-workspace-check snapshot pattern documented in prior session handoffs, is unreachable and harmless with no fence/run impact — user's explicit call.

### Commands that worked
- `curl.exe -s http://localhost:11434/api/tags` — verified Ollama reachable with `qwen2.5-coder:14b` listed, as a live-run precondition.
- `git rev-parse HEAD` / `git status --porcelain` / `git log --oneline -3` across issue-runtime and StockPhotoAgent — cold-start and precondition checks, repeatedly confirmed clean state.
- Temporarily setting `config.yaml`'s `budget.max_executions_per_run: 10 → 1`, running the orchestrator, then reverting — used twice (issue 27 run, issue 28 run) to scope a single-issue live run without draining the backlog; confirmed via `git diff --stat -- config.yaml` empty after revert both times.
- `git for-each-ref refs/attempts/_recovery/`, `git show <sha> --stat --no-patch`, `git merge-base --is-ancestor <sha> agent-work`, `git branch --contains <sha> -a`, `git reflog show refs/attempts/_recovery/reconciler-86` — full forensic read on the reconciler-86 ref, ran clean.
- `git -C C:\Projects\StockPhotoAgent add Issues.md && git commit -m "..."` — committed issue 27 (`083077a`) and issue 28 (`22c30cd`) additions to the backlog.

### Problems solved
First live run scoped for issues 13/14 drained with zero executions → root cause: eligibility was read from `Issues.md`'s STATUS text (stale, never written back) instead of `state/events.jsonl` → fix: switched to checking the event log's actual terminal/open issue state before scoping subsequent runs (issue 27, issue 28 both baseline-confirmed via event_id before launch).

Issue 27's fix landed but the Gap 2 hook didn't fire → root cause: issue text had no explicit "tests live in tests/test_X.py" acceptance bullet, so the child's ad-hoc sanity check was accepted as sufficient → fix: issue 28's text added that bullet, hook fired and was confirmed via `gate_results`.

reconciler-86 ref disposition unknown since session 36 → root cause: session 36's check only queried issue-runtime's ref namespace → fix: checked StockPhotoAgent directly, found and forensically confirmed the ref as a dangling, inert reconciler snapshot.

### Mistakes
Scoped the first live run based on `Issues.md`'s STATUS text reading issues 13/14 as OPEN; this is documented in NEXT.md as stale and unreliable, but the check was skipped, costing one wasted live-run cycle (zero executions) before switching to the event log as source of truth.

Cancelled the fallback wakeup for the issue-27 run before it had actually finished (log was still empty); caught immediately and corrected by rescheduling a proper wait for the completion notification.

### Prompts worth keeping
The repeated "Live run AUTHORIZED — single issue only... Halt after that single issue commits-or-denies" pattern, paired with an explicit raw-verbatim evidence checklist (event log, diff --stat, gate_results, reviewer events, post-run git state, cwd-escape grep, config revert confirm) — kept every live run against the real target tightly scoped and fully auditable with no summarization.

### Questions I still have
Whether `git show --stat --no-patch` on `a16f026` printing no stat lines means the reconciler snapshot commit's diff is genuinely empty, or whether `--no-patch` itself suppressed stat output — flagged as worth an explicit re-check if it matters, not re-verified this session.

Reviewer model identity (`qwen2.5-coder:14b`) remains inferred, not verified, from the event log — the schema only persists `reviewer_provider="qwen"`, not the model string (known Session-33 gap).

### TODOs for next session
- Triage the two untracked `docs/reviews/` files (`coverage-ledger.md`, `full-codebase-review.md`) — still pending, unchanged.
- If reconciler-86-style diff-emptiness questions matter later, re-check whether `git show --stat --no-patch` suppresses stat lines vs. the diff genuinely being empty.

## Session 3

### What I worked on
Ran a background `/code-review medium` on a single-line diff (num_turns persistence) which came back clean (no findings survived). Then executed a multi-step full-codebase read-only audit of the 108-file repository (44 Python files / 7,448 LOC, plus 3 config files, 59 markdown, 2 other). Audit produced tiered findings (Tiers A–D, 20 total issues, weakest evidence levels: Tier A VERIFIED, Tier B INFERRED, Tier C UNWITNESSED, Tier D VERIFIED) with complete coverage accounting (71/108 files = 65.7%, 100% of src/tests covered, gap entirely in historical docs/handoffs/ and docs/scratch/ with reasons documented). Committed two output files under `docs/reviews/` (full-codebase-review.md, coverage-ledger.md) as untracked. Then mechanically verified the highest-severity finding (B-CRIT-1: orphan-detection liveness check is structurally unfalsifiable in production because it only checks the direct Popen child, never descendants, while the real leaf-resolver function is gated behind an unset environment sentinel). Verification traced call sites, gating branches, and sentinel assignments across the entire src/ tree to confirm the finding's correctness.

### Cases

### CASE: separate-agent-for-full-audit-task
status: distilled
problem: A full-codebase audit task (specified steps for inventory, multi-pass review, tiered findings, coverage ledger) was submitted to `/code-review`, designed for diff-based bug reviews only.
decision: Recognized the task scope mismatch and routed to a separate general-purpose background agent with custom multi-step procedure, rather than attempting to force-fit through diff-review.
why: The audit requires full-file inventory, multi-pass analysis, specific output format (tiered findings + coverage ledger), and coverage accounting — the diff-review tool's 8-angle hunk-by-hunk scanning approach is unsuitable for this scope.
tags: [routing, task-splitting, code-review, tool-selection]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: force-fitting the full-codebase audit task through `/code-review`'s existing diff-based review flow.
Tradeoff: force-fitting through the existing tool would reuse an already-available flow with no new routing decision, but the audit requires full-file inventory, multi-pass analysis, and a tiered-findings-plus-coverage-ledger output format that the diff-review tool's hunk-by-hunk scanning approach is unsuitable for; routing to a separate general-purpose agent costs building a custom multi-step procedure but matches the actual task shape.
Tipping factor: the audit requires full-file inventory, multi-pass analysis, specific output format, and coverage accounting — the diff-review tool's 8-angle hunk-by-hunk scanning approach is unsuitable for this scope.

### Commands that worked
- `Get-Content src\runtime\engine\claude_headless.py | Select-Object -Skip 400 -First 40 | ForEach-Object -Begin {$n=401} -Process { "{0,4}: {1}" -f $n,$_; $n++ }` — printed function body with line numbers for manual verification of source.
- PowerShell `Select-String` with `Get-ChildItem -Recurse` for multi-file call-site tracing (initial glob `src\**\*.py` did not recurse; corrected with recursive enumeration).
- `git show --stat 3040c25`, `git rev-parse HEAD`, `git log --oneline -8`, `git for-each-ref refs/attempts/_recovery/` — cold-start state verification across repo state before audit.

### Problems solved
Full-audit task submitted to diff-review tool → root cause: tool selection did not match task scope (full audit with inventory/tiering/ledger vs. diff scanning) → fix: routed to separate agent designed for multi-step procedures.

PowerShell call-site tracing using glob `src\*.py,src\**\*.py` returned incomplete results → root cause: glob `**` does not recurse in PowerShell; only matches one level deep → fix: switched to `Get-ChildItem -Recurse` to enumerate entire tree before filtering.

### Mistakes
First attempt at tracing `is_execution_alive()` call sites used `Select-String -Path src\*.py,src\**\*.py` which missed nested paths; corrected by using `Get-ChildItem -Recurse` to properly traverse the directory tree.

### Prompts worth keeping
The multi-step full-codebase audit prompt (cold-start verification → inventory → 5 review passes → tiered findings with evidence accounting → coverage ledger) enforced complete auditable coverage without summarization, and produced a well-evidenced finding set with explicit tiers and skip reasons documented.

### Questions I still have
None outstanding from this session; the audit and B-CRIT-1 verification both completed with full evidence captured.

### TODOs for next session
- Decide on disposition of B-CRIT-1 finding (orphan-detection unfalsifiability in production): propose a fix, document as accepted risk, or escalate?
- Commit `docs/reviews/full-codebase-review.md` and `docs/reviews/coverage-ledger.md` after review if approved.
- Address other Tier A–B findings from the audit if desired.

## Session 4 (session 38, late night — relocated from 2026-08-09.md)

Relocated per [[historian-replay-date-bug-recurred-manually-relocated-not-repatched]] / P-004: `historian-sweep.sh --replay` stamped this transcript with 2026-08-09 (the replay run date) instead of its true origin date. The transcript's own embedded UTC timestamps (`2026-08-09T03:03:02Z`–`2026-08-09T03:23:49Z`) fall late in the evening of 2026-08-08 local time, corroborated by `knowledge/.sweep/sweep.log`'s own local-time entry for this session (`2026-08-08 23:31:05 [SessionEnd] FAIL ... queued in pending.log`). Content follows unchanged except case IDs, which now carry the corrected `2026-08-08` date.

### What I worked on
Session 38 cold-start: re-verified repo state for both issue-runtime and StockPhotoAgent (branch, HEAD, working tree, Issues.md contents), checked Ollama reachability, and confirmed `config.yaml`'s `max_executions_per_run` is still 10 with no diff. Along the way, corrected the eligibility-checking method from reading Issues.md STATUS text to reading the event log directly, after noticing issue 28 showed STATUS: OPEN in Issues.md despite a `merge 28` commit already existing on the `agent-work` branch. Committed two new review docs (`docs/reviews/coverage-ledger.md`, `docs/reviews/full-codebase-review.md`) as `234af18` after confirming a stray untracked `{src` directory would be deleted manually by the user rather than by the assistant. Closed the session with a handoff document via the session-handoff skill, carrying forward the unresolved issue-25 escalation and other standing opens; the handoff was written but explicitly not committed.

### Cases

### CASE: event-log-over-issues-md-status-for-eligibility
status: cited
problem: Issues.md's STATUS text for issue 28 read "OPEN" even though a `merge 28` commit already existed on `agent-work`, making STATUS text an unreliable signal for whether an issue is actually still eligible to run.
decision: Issue eligibility must be proven from the event log (events.jsonl / `state\events.jsonl` terminal-state events), not from Issues.md's STATUS field.
why: Issues.md STATUS text can lag behind the actual resolution state recorded in the event log, as directly evidenced by the issue-28 discrepancy found this session.
tags: [event-log, issue-eligibility, source-of-truth, stockphotoagent]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: trusting Issues.md's STATUS text for issue 28 as the eligibility signal.
Tradeoff: trusting STATUS text is the simpler, cheaper check, but it read "OPEN" despite a `merge 28` commit already existing on `agent-work`, showing it can lag the actual resolution state; proving eligibility from the event log costs an extra lookup against `state\events.jsonl` but reflects the actual resolution state.
Tipping factor: Issues.md STATUS text can lag behind the actual resolution state recorded in the event log, as directly evidenced by the issue-28 discrepancy found this session.

### CASE: stray-directory-manual-delete-not-assistant-driven
status: distilled
problem: An untracked, empty directory literally named `{src` (distinct from the real `src/`) sat at the issue-runtime repo root and needed to be removed before committing the two review docs.
decision: The assistant did not force-delete it after the `rm -rf` tool call was denied; it asked for explicit confirmation, and the user chose to delete it manually instead of having the assistant do it.
why: The directory was never git-tracked, so an assistant-driven delete would not be reversible via git, unlike the review-doc commit itself.
tags: [destructive-action, confirmation, git-cleanup, untracked-files]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: force-deleting the stray directory itself once the initial `rm -rf` call was denied, e.g. via an alternate removal command.
Tradeoff: force-deleting it would resolve the blocker immediately without a round-trip to the user, but the directory was never git-tracked, so an assistant-driven delete would not be reversible via git, unlike the review-doc commit itself; asking for confirmation and letting the user delete it manually costs a pause but keeps an irreversible action under the user's direct control.
Tipping factor: the directory was never git-tracked, so an assistant-driven delete would not be reversible via git, unlike the review-doc commit itself.

### Commands that worked
- `Invoke-RestMethod http://localhost:11434/api/tags | ConvertTo-Json -Depth 4` — returned the Ollama model list (llama3.2:latest, luvz-fast:latest, llama3.2:1b, ...) after `curl` failed.
- `git rev-parse HEAD` / `git status --porcelain` / `git log --oneline -1` in `C:\Projects\issue-runtime` — confirmed clean tree at `a7b4f13` (pre-commit) then `234af18` (post-commit).
- `git rev-parse --abbrev-ref HEAD` / `git rev-parse HEAD` / `git status --porcelain` / `git log --oneline -1` in `C:\Projects\StockPhotoAgent` — confirmed `agent-work` branch, clean, at `df9b8d1 merge 28`.
- `Select-String -Path config.yaml -Pattern "max_executions_per_run"` — confirmed value `10`, no diff.
- `git show --stat --oneline 234af18` — confirmed the commit added exactly the two intended review files, 1148 insertions.
- `Select-String -Path state\events.jsonl -Pattern '"issue_id":"25"'` — surfaced issue 25's full event history (IssueCreated → IssueActivated → ExecutionSpawned → ExecutionFinished REJECTED, needs-decomposition).
- `Get-ChildItem -Path C:\Projects\issue-runtime,C:\Projects\StockPhotoAgent -Recurse -Filter events.jsonl` — UNVERIFIED (output not shown in transcript before it was cut off), but its result (`state\events.jsonl`) is corroborated by the subsequent successful issue-25 query against that path.

### Problems solved
- Symptom: `curl http://localhost:11434/api/tags` threw `System.NullReferenceException` in PowerShell. Root cause: PowerShell's `curl` is aliased to `Invoke-WebRequest`, which errored on this call. Fix: use `Invoke-RestMethod ... | ConvertTo-Json -Depth 4` directly instead of the `curl` alias.
- Symptom: querying `events.jsonl` at the assumed path `C:\Projects\StockPhotoAgent\events.jsonl` was about to be run against a guessed location. Root cause: the real event log lives nested at `state\events.jsonl`, not the repo root. Fix: `Get-ChildItem -Recurse -Filter events.jsonl` to locate the actual path before querying it.

### Mistakes
- First cold-start reply summarized the raw command output into prose bullets instead of pasting verbatim fenced output as instructed; corrected by the user, who re-ran the same command set demanding raw output only.
- Initial StockPhotoAgent commands were run with incorrect bash syntax for a Windows path/drive change, causing them to fail; corrected by retrying with proper syntax.

### Prompts worth keeping
- "Prove issue eligibility from the event log, not Issues.md STATUS text." — precise instruction that surfaced and fixed a real staleness bug in how issue eligibility was being read (issue 28).

### Questions I still have
- Is issue 28 actually resolved? Issues.md STATUS still reads OPEN despite a `merge 28` commit on `agent-work`; the event-log check to confirm this was redirected toward issue 25 before the transcript showed a resolution for issue 28.
- What does the new Issues.md (promised "next session") contain, and does it change the pending full-run scope?

### TODOs for next session
- Obtain and load the new Issues.md before running the next full run.
- Decompose issue 25 (country-derivation unit tests) — currently escalated `needs-decomposition`, unresolved.
- Confirm from the event log whether issue 28 is actually resolved despite Issues.md STATUS still showing OPEN.
- Standing opens carried forward: B-CRIT-1 (`_resolve_leaf_worker` gated behind `ITEM9_SENTINEL`, no live wiring), Write-tool cwd-escape residual (documented, not fenced), reviewer model-string not persisted in event schema.
- Decide whether/when to commit `docs/handoffs/session-38-close.md` (written this session, intentionally left uncommitted).
