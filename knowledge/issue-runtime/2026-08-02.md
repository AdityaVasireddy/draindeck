---
project: issue-runtime
date: 2026-08-02
type: mixed
stack: [python, git, windows-process-management, ollama, docker, powershell]
status: Session 28 closed on top of session 27's state: Group S gates (a)/(b)/(d) synthetically witnessed, gate (c) UNWITNESSED-corrected, S-D dedup verified. This session additionally closed the `project.name` StockAgent/StockPhotoAgent config mismatch (commit `4afdb4a`) and committed the session-28 handoff (commit `d36fe43`). Build HEAD is `d36fe43`. `hold_pid` diff in `claude_headless.py` remains uncommitted — commit gate S-E still unwitnessed, needs a real StockPhotoAgent merge before it ships.
---

# issue-runtime — 2026-08-02

## What I worked on
Session 27 of the two-role (executor/reviewer) relay on issue-runtime. Started by re-verifying the state inherited from the d32a917 handoff (build HEAD, StockPhotoAgent tip, baseline, attempt refs, event log, Ollama endpoint) — first in prose, then re-run with raw command output after the reviewer rejected the prose version. Appended a correction to the 2026-08-01 handoff downgrading gate (c) from PASS to UNWITNESSED. Built two new scratch harnesses that import the real `claude_headless.py`/`projections.py` functions rather than reimplementing them: one that synthesizes a real settling process tree to witness Group S gates (a)–(d) end-to-end (including the tasklist evidence gate (c) never had), and one that proves the real event-projection dedup guard skips an already-spawned execution while still accepting a genuinely new one. Closed the session with a `.gitignore` entry for the harnesses' transient run dirs and a written session handoff. No `src/` files, StockPhotoAgent, or commits were touched — everything is unstaged/untracked per the standing "no commit without explicit authorization" rule.

## Cases

### CASE: gate-c-downgraded-unwitnessed
status: cited
problem: HANDOFF_2026-08-01 claimed gate (c) PASS — "tasklist confirms hold_pid alive — all three held on the one live spawn (11-e2)" — but the reviewer required raw evidence for gates (a)(b)(c), not another retelling.
decision: Appended a dated, append-only correction to the same handoff file downgrading gate (c) from PASS to UNWITNESSED, while leaving (a)/(b) as VERIFIED and (d) as NOT ACHIEVED.
why: No tasklist artifact exists on disk for the 11-e2 spawn — `state/artifacts/11-e2/` only contains `sentinel_ready`, `hold_ready`, and `sentinel_chain_log.jsonl`, no captured tasklist snapshot; the original PASS rested on prior-session prose, not a reproducible artifact.
tags: [evidence-standards, group-s, gate-c, verification]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: leaving gate (c) as PASS per the prior handoff's claim.
Tradeoff: leaving it as PASS costs nothing further, but rests on prior-session prose rather than a reproducible artifact; downgrading to UNWITNESSED costs an honest process regression but reflects only what's actually verifiable from disk.
Tipping factor: no tasklist artifact exists on disk for the 11-e2 spawn — the original PASS rested on prior-session prose, not a reproducible artifact.

### CASE: sd-dedup-guard-is-execution-spawned-not-next-actionable
status: distilled
problem: The S-D harness task named the dedup check as `_next_actionable`/`open_executions()` in `projections.py`, keying on `(issue_id, execution_id)` via `ExecutionSpawned` — an assumption carried over from a prior harness.
decision: Verified the real mechanism via grep + source read before building anything, found `_next_actionable` actually lives in `loop.py` and never itself rejects work, and built `scratch/group_s_synthetic_sd.py` against the real `projections.py` guard, `_execution_spawned`, which raises `TransitionError` on either a duplicate `execution_id` or a still-open prior execution for the issue.
why: The task explicitly required confirming the real mechanism first ("if the real skip key is NOT... say so and stop; do not build a test against a mechanism you haven't confirmed"), and the prior naming turned out to be imprecise once the actual code was read.
tags: [projections, dedup, verification, group-s]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: building the S-D harness against the assumed `_next_actionable`/`open_executions()` mechanism, carried over unverified from a prior harness.
Tradeoff: building against the assumed name would save the verification step, but the task explicitly required confirming the real mechanism first; verifying via grep and source read costs extra time but avoided building a test against a mechanism that turned out not to exist as named.
Tipping factor: the task explicitly required confirming the real mechanism first before writing any test against it, and the prior naming turned out to be imprecise once the actual code was read.

## Commands that worked
- `git -C /c/Projects/issue-runtime rev-parse HEAD` → `d32a917ac5a01b71eb04e93b3946705dc832b732` (raw re-verification after prose was rejected).
- `git -C /c/Projects/StockPhotoAgent rev-parse --abbrev-ref HEAD` / `rev-parse HEAD` → `agent-work` / `d663e32c...`.
- `git -C /c/Projects/StockPhotoAgent log -1 --format="%H %s" 1e06481` → confirmed baseline subject line.
- Attempt/recovery ref listing re-run against **StockPhotoAgent** (not issue-runtime) → returned all 6 refs raw.
- `tail -n 3` / `wc -l` on `state/events.jsonl` → confirmed event 109 tail.
- `docker exec ollama ollama list` → confirmed `qwen2.5-coder:14b` present on the Docker endpoint.
- `git -C /c/Projects/issue-runtime diff -- docs/handoffs/HANDOFF_2026-08-01_...` → confirmed pure append (all `+` lines, zero deletions/modifications).
- `python scratch/group_s_synthetic_harness.py` (via `.venv`, matching `scratch/ingest_only.py` precedent) — run 2 passed clean, exit code 0, after fixing the assertion and adding `try/finally`.
- `python scratch/group_s_synthetic_sd.py` — clean pass on first try.
- `git -C /c/Projects/issue-runtime check-ignore scratch/_gs_synth_20260802T051926Z` and `check-ignore scratch/group_s_synthetic_harness.py` — confirmed run dirs ignored, harness files not ignored.

## Problems solved
- **Symptom:** initial git commands failed. **Root cause:** Git Bash backslash path mangling (known issue). **Fix:** reissued with forward-slash absolute paths.
- **Symptom:** attempt/recovery ref grep returned empty. **Root cause:** the command targeted the `issue-runtime` repo, but those refs live in `StockPhotoAgent`. **Fix:** re-ran against the correct repo and reported both the empty and corrected results.
- **Symptom:** first synthetic-harness run crashed on a byte-for-byte equality assertion for `capture_work_liveness`, orphaning the `hold_pid` process. **Root cause:** the "before" snapshot was taken an instant before the kill landed, so one more benign append from the still-live leaf could land in that gap; the script also had no `try/finally`, so the crash left a process running. **Fix:** changed the invariant to "not corrupted / not truncated" instead of byte-equality, and added `try/finally` cleanup; second run passed clean (documented in-file via an INCIDENT NOTE).
- **Symptom:** prior harness's stated dedup mechanism (`_next_actionable`/`open_executions()` in `projections.py`) didn't match the code. **Root cause:** `_next_actionable` actually lives in `loop.py` and never rejects anything itself; the real guard is `_execution_spawned` in `projections.py`. **Fix:** verified via grep+read, then built `scratch/group_s_synthetic_sd.py` against the real guard.

## Mistakes
- Reported the full state re-verification as prose ("matches exactly") with no raw command output; the reviewer rejected it and required verbatim stdout for all 7 checks — corrected by re-running and pasting raw output for each.
- Grepped attempt/recovery refs against the wrong repo (`issue-runtime` instead of `StockPhotoAgent`), producing a misleadingly empty result before catching and correcting it.
- Carried forward an imprecise claim from an earlier report that `_next_actionable`/`open_executions()` was the dedup mechanism in `projections.py`; corrected this session after reading the actual source.

## Prompts worth keeping
- The reviewer's "raw output only" re-verification prompt — 7 numbered commands, explicit "no summaries, no 'matches' claims" instruction — produced complete verbatim evidence for all load-bearing refs in one pass.
- The S-D task's "verify that's actually what the code does before writing a test against it... if not, say so and stop" instruction — caught a real mechanism-naming error before any wasted test-building effort.

## Questions I still have
- What specifically satisfies gate S-E (the outstanding commit gate) is not defined anywhere in this transcript.
- True per-poll BFS depth for the chain_log data isn't recoverable from the stored artifacts (only `{poll, descendant_pids, leaf}` is logged, no depth field) — whether that matters for the final gate-(d) verdict is unresolved.

## TODOs for next session
- Await the reviewer's gate on the session-27 handoff before taking further action.
- Commit gate (S-E) is still open — get explicit authorization before committing `.gitignore`, either handoff doc, or `claude_headless.py`.
- The `hold_pid` diff in `claude_headless.py` stays uncommitted until Group S passes with a witnessed production-shape leaf (gate d) — sentinel/discriminator commit only after that.
- `config.example.yaml` line 11's `<StockAgent test command — REQUIRED before first run>` placeholder still needs updating (deliberately scoped out of session 28's edit) — optional one-line cleanup.

## Session 2 (session 28)

### What I worked on
A second same-day session, again executor role under the same reviewer relay, opening from the session-27 handoff as a snapshot to re-verify rather than trust. Repeated the raw-evidence re-verification pattern (build HEAD, working tree, StockPhotoAgent tip, baseline, attempt refs, event log, Ollama) first via Git Bash, which the reviewer then declared inadmissible for this project outright, forcing a switch to PowerShell for all further evidence. Ran read-only reconnaissance (file dumps + recursive grep + a targeted test-file read) on the long-standing `project.name: StockAgent` vs. repo-path `StockPhotoAgent` mismatch in `config.yaml`/`config.example.yaml` before the reviewer authorized a rename. Applied the scoped two-file edit (rename + stale-comment removal), got it authorized and committed as `4afdb4a`, then used the session-handoff skill to write and commit the session-28 handoff (`d36fe43`), with the reviewer requiring raw re-prints of git/PowerShell output (not prose descriptions) at every step before closing.

### Cases

### CASE: git-bash-banned-powershell-mandated-for-evidence
status: distilled
problem: The executor's read-only state re-verification, run via Git Bash, produced output the reviewer explicitly rejected as inadmissible: "Git Bash is inadmissible for validation in this project — it mangles backslash paths and has produced false results before."
decision: All subsequent raw-evidence commands in the session were run by invoking `powershell.exe -NoProfile -File <script>` (since no dedicated PowerShell tool is exposed), with each paste explicitly stating "Shell used: PowerShell" to satisfy the reviewer's admissibility check.
why: The reviewer stated Git Bash had "produced false results before" in this project and required PowerShell or cmd as the only admissible shells for gating evidence going forward.
tags: [tooling, evidence-standards, windows, powershell]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: continuing to use Git Bash for evidence gathering.
Tradeoff: continuing with Git Bash requires no tooling switch, but the reviewer explicitly rejected its output as inadmissible, citing backslash-path mangling and prior false results; switching to PowerShell for all evidence costs re-running every check under a new invocation pattern but satisfies the reviewer's admissibility requirement.
Tipping factor: the reviewer stated Git Bash had "produced false results before" in this project and required PowerShell or cmd as the only admissible shells for gating evidence going forward.

### CASE: config-name-rename-gated-on-verified-non-consumer
status: distilled
problem: `project.name: StockAgent` in `config.yaml`/`config.example.yaml` conflicted with the actual `StockPhotoAgent` repo path, flagged by a stale in-file comment as an unresolved decision; the reviewer refused to authorize any edit — "I won't authorize a find-replace against unread lines" — until it was shown whether the string was load-bearing.
decision: Renamed `project.name` to `StockPhotoAgent` and removed the stale divergence comment in both files (committed as `4afdb4a`), choosing option (ii) — rename to match the repo — over option (i) — leave the name and only clean the comment.
why: A recursive grep for "StockAgent" across the repo plus a raw read of `tests/unit/test_foundation.py`'s `GOOD_YAML` fixture (~line 245) showed the string is never asserted against — that test asserts only on `cfg.engine.auth_mode` and `cfg.experiment.cost_per_shipped_issue_max_usd` — confirming no code consumes `project.name`'s literal value before the edit was made.
tags: [config, verification, low-blast-radius, evidence-standards]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: option (i) — leave the `project.name` string as-is and only clean the stale divergence comment, without renaming.
Tradeoff: leaving the name unchanged avoids touching a potentially load-bearing config value, but leaves the misleading name/repo mismatch in place; renaming to match the repo costs an edit that needed prior proof of safety, but resolves the mismatch permanently once shown safe.
Tipping factor: a recursive grep plus a raw read of the `GOOD_YAML` test fixture showed the string is never asserted against, confirming no code consumes `project.name`'s literal value before the edit was authorized.

### Commands that worked
- `powershell.exe -NoProfile -File verify.ps1` (invoked from the Bash tool wrapper) — first PowerShell-run evidence pass after Git Bash was ruled inadmissible; markers matched the prior Git Bash run with no discrepancy.
- `Test-Path C:\Projects\issue-runtime\config.yaml` / `Test-Path C:\Projects\StockPhotoAgent\config.yaml` → `True` / `False` — located the live config.
- `git check-ignore -v config.yaml` (exit 1, no match) + `git ls-files --error-unmatch config.yaml` (exit 0) → confirmed `config.yaml` is tracked, not ignored.
- `Select-String -Path *.yaml,*.yml,src\*.py -Pattern "StockAgent" -SimpleMatch -Recurse` and the repo-wide recursive variant → confirmed all "StockAgent" occurrences, none load-bearing.
- `git add config.example.yaml config.yaml` (explicit filenames, no `-A`) then `git commit -m "chore(config): align project.name to StockPhotoAgent, drop stale divergence comment"` → `4afdb4a81e7c53a863bc6510e672ee01c56f54fb`, 2 files changed, 4 insertions(+), 6 deletions(-).
- `git add docs/handoffs/HANDOFF_2026-08-02_session28-config-name-mismatch-closed.md` then `git commit -m "docs(handoff): session-28 close - config name mismatch closed (4afdb4a)"` → `d36fe4353a69f9922502c7571e7fad7aa385eb3a`, 1 file changed, 73 insertions(+).
- `git show --stat HEAD` re-run read-only after each commit → confirmed file counts and that `M src/runtime/engine/claude_headless.py` plus `.gitignore`, the session-27 handoff, and `?? scratch/` remained uncommitted both times.

### Problems solved
- **Symptom:** reviewer rejected the Git Bash verification pass outright, independent of its content. **Root cause:** project-level rule that Git Bash is inadmissible for evidence (backslash mangling, prior false results). **Fix:** re-ran identical checks via `powershell.exe`, stated the shell explicitly, and used that shell for the rest of the session.
- **Symptom:** a read-only recon script errored twice with `if : The term 'if' is not recognized...` around the `DUMP_ISSUE_RUNTIME (if it exists)` / `DUMP_STOCKPHOTOAGENT (if it exists)` markers. **Root cause:** the parenthetical `(if it exists)` in the `echo` marker text was parsed by PowerShell as a subexpression rather than literal echoed text. **Fix:** none applied in-session — the errors were pasted as raw evidence per the read-only-diagnostic instruction rather than corrected; UNVERIFIED whether later prompts avoided the syntax deliberately.

### Mistakes
- Reported the config-rename commit's raw evidence as prose ("Committed: 4afdb4a... 2 files changed...") instead of pasting the literal terminal text; the reviewer rejected it and required a read-only re-print of `===VERIFY_REF===`/`===POST_COMMIT_TREE===`.
- Repeated the identical mistake for the session-28 handoff commit — reported the ref and file-list as prose instead of pasting `Get-Content`/`git diff`/`git show --stat` output verbatim; again required a read-only re-print before the reviewer would close the session.

### Prompts worth keeping
- The reviewer's read-only reconnaissance prompt (`===LOCATE===`/`===DUMP_*===`/`===GREP_CONSUMERS_*===`, explicitly "Do NOT edit anything yet") — surfaced the exact config lines and every "StockAgent" occurrence before any edit was authorized, preventing a blind rename.
- The follow-up prompt targeting `test_foundation.py` around the `name: StockAgent` fixture line with a deliberately wide 60-line window ("catch the enclosing test function and any assert") — settled definitively whether the string was load-bearing.

### Questions I still have
None new this session — see the top-level Questions section (S-E gate criteria, BFS depth) for what remains open overall.

### TODOs for next session
- Commit gate (S-E) is still open: the `hold_pid` diff in `claude_headless.py` remains uncommitted, awaiting a witnessed production-shape leaf via a real StockPhotoAgent merge — do not commit it without that.
- `config.example.yaml` line 11's `<StockAgent test command — REQUIRED before first run>` placeholder still needs updating — deliberately scoped out of this session's edit, flagged as optional follow-up.
- ADR-19 sample stands at n=6 (5 smoke + issue-11) with two flags still unresolved before scoring at n=20 (attempt-1 numerator definition and one other, per the handoff-close prompt — not fully detailed in this transcript).
