---
project: issue-runtime
date: 2026-08-05
type: mixed
stack: [python, pytest, git, powershell, ollama, claude-headless]
status: Issues 23 and 24 shipped through the full orchestrator pipeline against StockPhotoAgent; issue 25 escalated via the turn-budget guard and was hand-merged after manual verification; Session 33 closed with handoff committed at a9b168a.
---

# issue-runtime — 2026-08-05

## What I worked on
Continued Session 33: verified cold-start state against the session-32 handoff, then ran preconditions (Ollama model tag, Issues.md dependency chain, baseline validation) before live-running the 23→24→25 country-derivation chain (decomposed from escalated issue-19) against StockPhotoAgent's `agent-work` branch, one issue at a time under explicit user gating. Issues 23 and 24 shipped cleanly through the orchestrator pipeline (validated, Qwen-reviewed, committed). Issue 25 escalated straight from execution to `IssueEscalated` before validation or review ever ran; investigation showed the child's tests were actually correct, so they were hand-merged onto `agent-work` as an explicit pipeline bypass. Two observability/ergonomics gaps were logged to `NEXT.md` (not fixed), and the session closed with `NEXT.md` committed, a handoff written via the session-handoff skill, and the handoff committed.

## Cases

### CASE: manual-max-executions-scoping-for-single-issue-runs
status: distilled
problem: The `run` subcommand has no per-issue flag; Issues.md has issue 23 as the only dependency-satisfied issue, but the orchestrator's continuous `while True` loop combined with `budget.max_executions_per_run: 10` would auto-cascade into issue 24 the moment 23 completed, inside the same process — violating an explicit "issue 23 only" authorization.
decision: Temporarily edit `config.yaml`'s `budget.max_executions_per_run` from 10 to 1 (working-tree only, never committed), run the scoped invocation, confirm exactly one execution ran, then revert to 10 — repeated identically for issues 23, 24, and 25.
why: `run` exposes only `--config` and `--skip-baseline`, and `loop.py`'s continuous loop would pick up the next dependency-satisfied issue within the same run; the execution-count cap was the only available lever to guarantee single-issue scope.
tags: [orchestrator, scoping, config, live-run]
until: a real --issue or --max-executions CLI flag lands (logged as an unauthorized gap in NEXT.md)

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: running the orchestrator unscoped and manually stopping it once issue 23 completed.
Tradeoff: stopping it manually after the fact would avoid editing config, but the continuous loop would auto-cascade into issue 24 within the same process before any manual stop could land, violating the explicit single-issue authorization; temporarily capping `max_executions_per_run` at 1 costs an edit-and-revert cycle each time but guarantees the scope mechanically.
Tipping factor: `run` exposes only `--config` and `--skip-baseline`, and `loop.py`'s continuous loop would pick up the next dependency-satisfied issue within the same run — the execution-count cap was the only available lever to guarantee single-issue scope.

### CASE: turn-budget-escalation-skips-validation
status: distilled
problem: Issue 25 escalated straight from `ExecutionFinished` to `IssueEscalated` with no `ValidationPassed`/`Failed` or `ReviewApproved`/`Rejected` events in between, and it was unclear whether this happens because the child self-reports `REJECTED`, or because the orchestrator's own validation gate ran and failed.
decision: Traced `loop.py`'s post-execution branch and confirmed verdict (A): when `num_turns >= max_turns`, the turn-budget guard emits `ExecutionFinished(outcome=REJECTED, taxonomy_category=needs-decomposition)` immediately followed by `IssueEscalated` — validation is structurally skipped, not merely failed.
why: the raw source lines in `loop.py` show the turn-budget conditional emits both events inline before any call to the validation gate, a separate code path from `ValidationFailed`/`ReviewRejected`.
tags: [orchestrator, escalation, control-flow, turn-budget]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: assuming the escalation happened because the orchestrator's own validation gate ran and failed, rather than the turn-budget guard skipping it entirely.
Tradeoff: assuming validation ran and failed is the more "expected" explanation requiring no further tracing, but it doesn't match what the code actually does; tracing `loop.py`'s actual post-execution branch costs extra investigation time but establishes the real mechanism.
Tipping factor: the raw source lines in `loop.py` show the turn-budget conditional emits both events inline before any call to the validation gate — a separate code path from `ValidationFailed`/`ReviewRejected`.

### CASE: hand-merge-verified-tests-over-pipeline-rerun
status: distilled
problem: Issue 25's execution escalated (turn-budget exhaustion, the headless engine's permission posture blocking the child's own pytest self-verification) even though the child's actual test file was later confirmed correct (5/5 passing against current HEAD); a blind re-run would hit the same wall and burn another ~$0.67.
decision: Investigated read-only first — checked out the unmerged commit's single test file, ran it, restored the tree to pristine — instead of blind-retrying; once confirmed green, hand-merged the escalated commit (`2978c486`) onto `agent-work` directly via `git merge --no-ff`, bypassing the orchestrator pipeline rather than fixing the engine's permission posture and re-running.
why: the escalation root cause is a src/engine permission-mode change, classified as a five-gate change requiring 60/60 both seeds — not justified just to re-land tests already witnessed passing.
tags: [pipeline-bypass, hand-merge, escalation, cost-tradeoff]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: fixing the engine's permission posture and re-running issue 25 through the full orchestrator pipeline.
Tradeoff: fixing the permission posture and re-running would exercise the real pipeline end-to-end, but the escalation root cause is a src/engine permission-mode change classified as a five-gate change requiring 60/60 both seeds — not justified just to re-land tests already witnessed passing, and risked burning another ~$0.67 hitting the same wall; hand-merging the already-verified-green commit costs bypassing the pipeline but avoids that unjustified cost and risk.
Tipping factor: the escalation root cause is a src/engine permission-mode change classified as a five-gate change — not justified just to re-land tests already witnessed passing read-only.

## Commands that worked
- `curl -s http://localhost:11434/api/tags` — confirmed `qwen2.5-coder:14b` present with correct parameter size, ruling out the `qwen2.5vl` wrong-model trap.
- `git merge --no-ff 2978c4863330aa17f57c5a2c845f49f38c4fad4b -m "manual-land issue-25 test module..."` — clean merge, no conflicts, second parent `2978c486`, only `tests/test_country_derivation.py` touched.
- `git checkout 2978c4863330aa17f57c5a2c845f49f38c4fad4b -- tests/test_country_derivation.py` then `python -m pytest tests/test_country_derivation.py -v` — 5 collected, 5 passed, returncode 0 (run both pre- and post-merge).
- Scoped live runs via `max_executions_per_run` 10→1→10 edits for issues 23, 24, 25 — each confirmed exactly one execution, config diff reverted to empty each time.

## Problems solved
1. Symptom: risk that a bare `run` invocation could cascade past issue 23 into 24 within the same process. Root cause: `run` has no per-issue flag and the orchestrator's `while True` loop auto-picks up newly dependency-satisfied issues. Fix: temporarily cap `max_executions_per_run` at 1, revert after.
2. Symptom: issue 25 escalated `REJECTED`/`needs-decomposition` with no validation or review ever running. Root cause: child hit `num_turns (33) >= max_turns (30)` retrying pytest calls blocked by the headless engine's `--permission-mode acceptEdits` + `--setting-sources ""` posture; `loop.py`'s turn-budget guard emits `ExecutionFinished(REJECTED)` + `IssueEscalated` inline. Fix: hand-merge the already-verified-green test commit onto `agent-work`, bypassing the pipeline.

## Mistakes
An unexpected system-reminder mid-task claimed "Exited Plan Mode"/"Auto Mode Active," which the assistant had not triggered and no such mode change had been requested; it was flagged to the user and disregarded rather than acted on. Separately, a self-caught wording pass fixed an "awkward, self-referential passage" in the NEXT.md gap note before finalizing — not a functional error, but a documented pre-commit correction.

## Prompts worth keeping
The repeated "Authorized: live cmd_run on issue N only... Do NOT start issue N+1... paste raw artifacts, no summaries" gating pattern — kept each live orchestrator run strictly single-issue-scoped with mechanical (not self-reported) evidence required before proceeding.

## Questions I still have
Whether the `taxonomy_category: needs-decomposition` label on issue 25 is actively misleading (nothing about the issue's scope failed — the child's self-verify step was blocked by tooling) was raised but explicitly flagged as inferred, not proven, from the transcript.

## TODOs for next session
- Persist reviewer `severity`/`feedback` into the `ReviewApproved`/`ReviewRejected` payload (or write raw reviewer response to `state/artifacts/<exec-id>/review.json`) — five-gate src/ change, not yet authorized.
- Add a real `--issue <id>` or `--max-executions <n>` flag to the `run` subcommand — not yet authorized.
- Investigate the headless engine's permission-mode/setting-sources posture that blocked issue-25's child from self-verifying via pytest, before any future re-run of similar issues.
