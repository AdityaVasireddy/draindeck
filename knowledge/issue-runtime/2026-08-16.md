---
project: issue-runtime
date: 2026-08-16
type: mixed
stack: [python, git, claude-code, powershell]
status: Five docs/cosmetic/hardening items resolved and committed one-at-a-time with explicit per-commit authorization; a seventh read-only audit of prior hardening claims was in progress (item 2 of 7 reviewed) when Session 1 ended. Session 2 is an adversarial re-verification of Session 1's claims (durability harness both seeds, reviewer-parser fix commit, knowledge/ tracking) with raw-output demands; verification substantively succeeded but ends on an unresolved output-contamination flag and an unexplained gitignore-vs-tracked-file discrepancy.
---

# issue-runtime — 2026-08-16

## What I worked on
Ran a chain of tightly-scoped `/resolve-item` runs: fixed mojibake in `main.py`, corrected a Phase-4-review-bypass bug in `.claude/commands/resolve-item.md` after a postmortem, set up a `.gitignore` scheme to track `resolve-item.md` while keeping `settings.local.json` local-only, expanded the README and clarified ADR-19's evidence hierarchy, audited portability/Windows-coupling concerns (found no defects, documented the Windows-only constraint as intentional), and added a regression test proving a historical reviewer-parser hard-halt (event 733) stays fixed. Each unit was committed separately under explicit one-time authorization. Session 1 ended mid-way through a comprehensive read-only audit re-verifying all of today's and prior hardening claims from raw evidence.

## Cases

### CASE: resolve-item-phase4-review-mandatory-regardless-of-blast-radius
status: auto
problem: `/resolve-item` skipped the mandatory `agent-skills:code-review-and-quality` Phase 4 review on a trivial cosmetic diff, reasoning that blast-radius proportionality (CLAUDE.md's "process scales to blast radius" rule) excused it.
decision: Reworded `.claude/commands/resolve-item.md` so blast-radius proportionality explicitly applies only to Phases 2–3 (planning/implementation ceremony, TDD/incremental skippable-with-reported-reason for trivial changes); Phase 4's `code-review-and-quality` invocation is now stated as unconditionally mandatory regardless of blast radius — only review *depth* may scale.
why: Phase 4 step 2 was the one skill invocation in the file with no conditional escape hatch, but the general blast-radius clause had no scope limiter, so it was plausibly (but incorrectly) read as reaching into Phase 4 too.
tags: [resolve-item, review-gate, blast-radius, wrapper-contract]

### CASE: claude-dir-gitignore-commands-tracked-settings-local-untracked
status: auto
problem: `.claude/commands/resolve-item.md` (repo workflow, should travel with clones) and `.claude/settings.local.json` (machine-specific permission allowlist with absolute personal paths) were both untracked, and the repo's own `.gitignore` had no `.claude` rule at all — only the user's personal global gitignore was accidentally preventing `settings.local.json` from being staged.
decision: Added `.claude/*` / `!.claude/commands/` / `!.claude/commands/**` to the repo's `.gitignore`, then committed only `.gitignore` + `resolve-item.md`.
why: A fresh clone or different contributor has no personal global gitignore, so relying on it to protect `settings.local.json` was not a repository-level guarantee; the blanket-ignore-with-explicit-allowlist pattern tracks the workflow command while defaulting all other `.claude` content (including future runtime/session state) to ignored.
tags: [git, gitignore, claude-config, repo-hygiene]

### CASE: adr-19-sample1-alone-satisfies-closure-not-joint-with-sample2
status: auto
problem: The ADR-19 closure addendum in `docs/08` read as if Sample 1 (n=20) and Sample 2 (n=19) were jointly necessary to reach the 20-issue validation requirement, and left ambiguous whether dedicated sampling was still owed or whether the later 74-DONE drain could substitute.
decision: Added an explicit "evidence hierarchy" passage (append-only, no existing ADR-19 text deleted): Sample 1 alone (independently re-derived as n=20 exactly from raw `state/events.jsonl`, 85% attempt-1, ~$0.36/shipped) already satisfies every binding threshold; Sample 2 is corroborating, not a second required leg; the 74-DONE drain is cumulative production history that occurred after closure and lacks population-wide attempt-1/cost metrics, so it does not itself satisfy ADR-19; dedicated sampling is not still owed.
why: An independent adversarial audit re-derived Sample 1's figures directly from `state/events.jsonl` and git history (not from handoff prose) and confirmed them, including a deliberate attempt to break the count via issues 10/11's multi-attempt shipping — the audit is what grounded the wording correction.
tags: [adr-19, evidence, documentation, closure]
until: superseding ADR or new sampling run, per ADR-19's own honesty clause

### CASE: portability-audit-no-code-defects-windows-constraint-documented-not-fixed
status: auto
problem: Item asked to investigate hardcoded absolute paths, Windows/PowerShell-only coupling, and hardcoded Ollama/qwen reviewer config as three possible portability defects, but did not assume the premises were true.
decision: Investigated first — found no hardcoded paths in `src/`/config/tests (only a harmless safety-comment path and a correctly-gitignored `config.local.yaml`), no hardcoded Ollama/qwen values (`QwenCfg`/`_make_reviewer` already fully config-driven), and Windows/PowerShell coupling already correctly isolated to `windows_job.py` + `validation/runner.py` with a clear typed exception on non-Windows. Made no source changes; only extended README to document Windows as an intentional current constraint (no Linux/macOS support claimed).
why: Full cross-platform support would require changing the frozen validation/containment contract (ADR-23's `$`-rejection PowerShell-shaped injection defense + native Job Object containment) — explicitly out of scope per the item's own instruction not to weaken an existing safety invariant to claim cross-platform support.
tags: [portability, windows, powershell, adr-23, investigation]

### CASE: reviewer-parse-error-already-fixed-e43bc72-added-regression-proof-only
status: auto
problem: Item described a historical hard-halt (event 733, 2026-08-14): malformed/non-schema reviewer output raised `ReviewParseError` which propagated uncaught out of `loop.py`'s `_review()`, crashing the whole run; issue 88 was manually hand-appended to NEEDS_HUMAN as an operational workaround, not a code fix.
decision: Investigation found the actual runtime fix already shipped a day later in commit `e43bc72` (try/except routing to the existing legal ACTIVE→NEEDS_HUMAN transition with `reason="reviewer-protocol-violation"`). No `loop.py` change was made this session; only a stale pre-fix docstring in `reviewer/base.py` was corrected and a new regression test added proving one malformed-verdict issue escalates to NEEDS_HUMAN while an independent second issue still reaches DONE in the same run, with no forged approval.
why: Re-fixing already-fixed runtime code would be unnecessary risk on a frozen path; the task called for proof the fix holds, not a new fix — confirmed by checking `git diff --stat -- src/runtime/loop.py` was empty before committing.
tags: [reviewer, event-733, regression-test, hard-halt]

Also noted (not drafted): comprehensive 7-item hardening re-audit (read-only, adversarial evidence verification against raw source/tests/git history) was in progress — item 1 (README) and item 2 (ADR cleanup, verdict NOT APPLICABLE/PREMISE INVALID) were completed when Session 1's transcript cuts off; items 3–7 not yet reached.

## Commands that worked
- `.venv/Scripts/python.exe -m pytest tests/unit -q` — 228 passed baseline, 229 passed after the reviewer-parser regression test was added.
- `git check-ignore .claude/settings.local.json` / `git add --dry-run <path>` — used as ground truth for `.gitignore` correctness (noted `git check-ignore -v` gave misleading exit codes on re-inclusion rules in this git version; plain `git check-ignore` and `git add --dry-run` were reliable).

## Problems solved
- Mojibake em-dashes (`â€”` instead of `—`) in three `print()` strings in `src/runtime/main.py` → UTF-8-bytes-decoded-as-Latin-1 corruption → replaced with correct `—` character (commit `5ce9f4f`).
- `/resolve-item` silently skipping mandatory final review on trivial changes → unscoped blast-radius proportionality clause reaching into Phase 4 → Phase 4 made explicitly unconditional in the wrapper contract.
- `settings.local.json` at risk of being committed on other machines/clones → repo `.gitignore` had no `.claude` rule (only the user's personal global gitignore protected it) → added repo-level `.claude/*` + commands allowlist.

## Mistakes
- Original ADR-19 addendum wording (written earlier, before this session's audit) understated that Sample 1 alone already satisfied the requirement, reading instead as if both samples were jointly necessary — caught by a dedicated final-review pass and corrected via an append-only evidence-hierarchy insertion.
- README's ADR citation attributed "no multi-agent framework" only to ADR-03/ADR-04 when it's more directly ADR-01's subject ("Two AI roles, not seven agents") — caught in the same final review, corrected as a one-word citation fix.

## Prompts worth keeping
- The pattern of chaining `/resolve-item` → read-only postmortem → narrowly-scoped correction → retroactive missing-step completion → read-only pre-commit review → scoped single-purpose commit (repeated across all five items today) reliably produced small, well-isolated commits with an audit trail at each step.

## Questions I still have
- Whether the in-progress 7-item hardening re-audit (cut off after item 2 of 7) will surface any regressions in items 3–7 (ADR-19 verdict, and four unnamed-in-transcript remaining items) — unresolved at Session 1 transcript end.

## TODOs for next session
- Finish the comprehensive read-only hardening audit (items 3–7) that was in progress when Session 1 ends.
- CLAUDE.md's documented "Unit: expect 117 pass" is stale (actual count is now 229) — noted twice this session as an observation, not yet corrected.

## Session 2 (adversarial re-verification)

An adversarial reviewer persona re-demanded live, verbatim (not summarized/reconstructed) terminal output for Session 1's key claims: durability harness 60/60 on both seeds, the reviewer-parser fix commit (`e43bc72`), and `knowledge/` tracking status. Several rounds of inline pastes were reported by the user as arriving empty; the assistant worked around this by writing combined command output to `verify_bundle.txt` and catting it, and by backgrounding the seed-1337 harness run past a ~5-minute foreground timeout. Verification substantively succeeded (both seeds 60/60 fresh against scratch repos at HEAD `7a4173556d03cb34dd0ffe82601139d4f9381a75`, never against StockPhotoAgent) but the session ends with an unresolved contamination flag on the seed-42 output file and an unexplained gitignore/tracking discrepancy for `knowledge/issue-runtime/2026-08-16.md`.

### CASE: durability-harness-seed-run-exceeds-5min-timeout-use-background
status: auto
problem: A single-seed run of `tests\crash\harness.py` (60 scenarios) can take longer than the assistant's ~5-minute foreground command timeout — seed 1337 was still running when the timeout hit mid-audit.
decision: Reran the seed-1337 harness invocation as a background task, polled it via the task-notification mechanism, and pasted its output file's contents once the background run completed, rather than retrying synchronously.
why: The harness genuinely needs longer than 5 minutes wall-clock for some seeds; the foreground run was hitting a hard timeout, not failing, so retrying synchronously would repeat the same timeout.
tags: [durability-harness, timeout, background-task, verification]

### CASE: file-bundle-workaround-for-stripped-inline-paste
status: auto
problem: Repeated inline pastes of raw command output arrived empty on the reviewer's end across multiple turns, blocking verification of the durability harness, the `e43bc72` diff, and git state despite the assistant confirming the underlying tool calls had produced real output.
decision: Wrote all requested command outputs into a single untracked file (`verify_bundle.txt`) via redirected shell output, then pasted that file's contents with `cat`, instead of continuing to paste inline.
why: The user explicitly instructed this file-then-cat approach after three consecutive attempts at inline pasting were reported as not coming through.
tags: [verification, terminal-output, workaround, tooling]

## Commands that worked (Session 2)
- `.venv\Scripts\python.exe tests\crash\harness.py <scratch-root> 42` — ALL 60 SCENARIOS PASSED, fresh run this session.
- `.venv\Scripts\python.exe tests\crash\harness.py <scratch-root> 1337` — ALL 60 SCENARIOS PASSED, run in background past the 5-minute foreground timeout.
- Writing combined verification output to `verify_bundle.txt` then `cat verify_bundle.txt` — successfully surfaced content the reviewer had been unable to see via inline paste.

## Mistakes (Session 2)
- Pointed `tail -8` at `harness_output.txt` for the "seed 42 tail" section of `verify_bundle.txt`, but that file itself contained a stray leftover `=== harness seed 1337 (tail) ===` header from an earlier combined-output run where seed 1337 had timed out mid-write, so the bundle's seed-42 section is contaminated with an unrelated header line. Identified but not yet fixed when the transcript ends — the assistant offered to rebuild the bundle from a clean single-purpose file and was awaiting confirmation.

## Questions I still have (Session 2)
- Why `git check-ignore knowledge/issue-runtime/2026-08-16.md` returns EXIT=1 (not ignored) even though `.gitignore` has contained a bare `knowledge` pattern since commit `70cd4c9`, given the file is nonetheless tracked (added back in `7a41735`) — flagged as a live discrepancy (nested `.gitignore`? `git add -f` history? pattern-matching quirk?) and explicitly not yet explained.

## TODOs for next session (Session 2)
- Rebuild `verify_bundle.txt` with a clean, uncontaminated seed-42 tail section.
- Explain the `knowledge/` gitignore-vs-tracked discrepancy (why `git check-ignore` says not-ignored despite the bare `knowledge` pattern in `.gitignore`) and confirm whether `knowledge/` re-entering version control (via `7a41735`, after `70cd4c9` stopped tracking it) was intentional.
