---
project: issue-runtime
date: 2026-07-16
type: mixed
stack: [python, pytest, git]
status: Session 7 committed at 734d08b (ADR-21 Amendment 1 accepted, ADR-22 proposed, knowledge/-contamination mechanism identified); Step 3 (gated live smoke) remains blocked pending Adi's ADR-22 option selection for Session 8

---

# issue-runtime — 2026-07-16

## What I worked on
Continued Session 6 of the autonomous issue-resolution runtime: resolved deferred item R1, a pre-existing harness-wide gap where check-3's `reset_hard` healing of a mid-VALIDATING dirty-tree crash had never been proven in isolation from the worker loop's masking `reset_hard`. Traced `checkout_branch`'s dirty-tree refusal path to confirm it's a safe wedge (uncaught `RepoError`, no mutation before the raise) rather than a corruption risk, then implemented and verified the planted `f5` fixture, landing two commits (`777ccab` test-only fixture, `b539239` doc 14 + NEXT.md). The user's review of the handoff surfaced that the isolated-mutation proof and commit-hygiene claims needed to be re-demonstrated live rather than trusted from the summary; all three re-checks (isolated `f5` red, commit `--stat` scope, verbatim doc 14 §1.5 text) held up on reproduction.

## Cases

### CASE: isolated-fixture-proof-must-be-reproduced-live-not-summarized
status: cited
problem: The session handoff claimed f5's mutation spot-check was verified in isolation, but the user could not tell from the summary alone whether that isolation had actually been demonstrated or only inferred from an earlier full-run harness pass.
decision: Re-ran `run_reset_fixture()` directly (no `main()`, no worker subprocess, no other fixture in the call graph) with `reset_hard` gutted, live in this turn, and pasted the actual failing assertion output before treating R1 as closed.
why: Aggregate full-run redness with check-3 gutted proves nothing about f5 specifically, since other reconciler-dependent scenarios could independently fail — the whole point of the mandatory isolation requirement was to rule out that vacuousness, and a prose claim of "isolated" doesn't establish it without the actual isolated output shown.
tags: [verification, testing, harness, honesty-discipline]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: trusting the handoff's prose claim that the mutation spot-check had been "verified in isolation," inferred from an earlier full-run harness pass.
Tradeoff: trusting the prose claim is faster but leaves ambiguous whether isolation was actually demonstrated or only inferred from aggregate redness, which could independently come from any of several reconciler-dependent scenarios; re-running live and pasting the failing assertion costs a repeat run but removes the ambiguity entirely.
Tipping factor: an aggregate full-run red result cannot distinguish "f5 specifically failed" from "some other scenario failed," so only a literally isolated re-run with the actual output shown establishes the claim.

### CASE: r1-fixture-prioritized-before-step2-preflight
status: distilled
problem: R1 (proving check-3's reset healing) was a pre-existing, out-of-scope gap deferred in doc 14, and Step 2 preflight (billing re-verification, engine-version re-probe) was the nominal next task for the session.
decision: Traced the `checkout_branch` dirty-tree failure path first as a cheap fail-safe check, then built and verified R1 before starting Step 2, on the same "independent of user-gated services, do it first" logic used to sequence Step 1 ahead of Step 2 previously.
why: Step 4's abort protocol claims "worst-case kill is exactly what the harness proves," a claim the unproven check-3 reset gap falsifies for exactly the case a live Ctrl+C during VALIDATING could hit; the default was to proceed with R1 unless the fail-safe trace showed a genuinely benign, no-correctness-risk wedge, and the trace confirmed a safe wedge but the user's default instruction to build R1 anyway stood.
tags: [prioritization, scheduling, correctness, harness]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: proceeding straight to Step 2 preflight as the nominal next task, leaving R1 as a pre-existing, out-of-scope gap for a later session.
Tradeoff: deferring R1 keeps the session's scope narrow and matches the nominal plan, but leaves Step 4's abort-protocol claim resting on an unproven gap for exactly the crash case (a live Ctrl+C during VALIDATING) it claims to cover; building R1 first costs the session's time budget before starting the nominal task.
Tipping factor: the unproven check-3 reset gap directly falsifies Step 4's "worst-case kill is exactly what the harness proves" claim for a real, plausible crash scenario, so the default was to close it before doing anything else independent of user-gated services.

Also noted (not drafted): switching from system Python to the project's `.venv` mid-session before running the full harness/unit suite.

## Commands that worked
- `git diff --stat src/` — confirmed empty both before running the mutation spot-check and after reverting it, verifying no stray change reached `bindings.py`.
- `git show 777ccab --stat` / `git show b539239 --stat` — confirmed `777ccab` touches only `tests/crash/harness.py` and `b539239` touches only `NEXT.md` and `docs/14-session6-phase2-gate.md`; neither touches `src/runtime/recovery/bindings.py`.
- Full crash harness run on seeds 42 and 1337 with the new f5 fixture — 60/60 both seeds.
- Unit suite re-run after the harness change — 103/103, no regression.

## Problems solved
`checkout_branch(create_from=base)`'s behavior on a dirty worktree was unclear (liveness bug vs corruption risk) → traced to `RepoError` (a plain `RuntimeError` subclass) raised at `git_adapter.py:166-170`, strictly before either mutating `git checkout` call, never caught anywhere in `loop.py` or `main.py`, so it propagates uncaught and crashes the process → confirmed as a safe wedge: no event is emitted, the reconciler always runs before the loop at startup so it cannot race a live checkout, and manual cleanup is required but no state corruption occurs.

## Mistakes
Started running the harness/unit suite against the system Python interpreter instead of the project's `.venv` before catching and correcting it mid-session. The handoff's phrasing of the isolated mutation-check result was ambiguous enough about how the proof was obtained that the user could not confirm from the summary alone whether it was truly isolated or inferred from an aggregate full-run pass — required re-demonstrating live before R1 could be treated as closed (see [[isolated-fixture-proof-must-be-reproduced-live-not-summarized]]).

## Prompts worth keeping
The user's three-part verification demand — "paste (a) the isolated-f5-red output... (b) `git show --stat` for both commits... (c) the verbatim doc 14 §1.5 text" — forced live reproduction of claims instead of accepting a prose summary, and surfaced that the original isolation proof, while substantively correct, hadn't actually been re-shown.

## Questions I still have
Whether a live Ctrl+C during VALIDATING is actually covered by Step 4's abort-protocol guarantees remains explicitly open — doc 14 §1.5 records R1 as proving reconciler-path healing "in isolation from the worker loop" only, disclaiming "joint coverage" and naming the live-abort question as unresolved against Step 4's claim.

## TODOs for next session
Start Step 2 preflight: 2a billing re-verification, 2b engine-version/fence re-probe — not started this session, per the user's explicit instruction to defer it until R1 was closed.

---

## Session 2 (Step 2 preflight: 2a billing + 2b fence re-probe)

## What I worked on
Executed Session 6 Step 2 preflight end to end: re-verified ADR-18 billing
posture and ADR-21 engine fence behavior by live observation rather than
reusing the 2026-07-10/2026-07-12 findings. The plan went through two rounds
of external review before any live probe ran — six amendments to the initial
plan (argv-capture rigor on the production path, transcript-first
classification over file-presence, a pre-committed src/-gate exemption rule,
naming a specific mixed outcome as an explicit stop condition, hygiene
tightening, and an honest probe-budget ceiling), then four more after the
first probe (C1) came back green, before the remaining three probes (C2-C4)
were authorized. Four live `claude -p` probes ran (one hand-built fence
mirror, one `--allowedTools`-only recheck, one whole-tool-removal recheck,
one through the actual production `ClaudeHeadlessEngine.run()` path), zero
re-runs needed against a pre-committed ceiling of seven. Found the installed
CLI had moved to 2.1.211 (off the 2.1.207 pin); fence enforcement matched
ADR-21 in every load-bearing respect, but the whole-tool-denial detection
signal changed shape. A new blocker surfaced mid-probe (ambient
engineering-historian hook writing into the engine-child cwd on the
production path) and was recorded, not fixed, as an ADR-level decision for
the user. Landed a comment-only re-pin in `claude_headless.py`, an as-built
section in doc 14 §2, `config.yaml` billing date bump, and NEXT.md updates,
gated on the 103-unit suite plus a diff review proving zero non-comment
lines changed. Committed at `2877733`, then wrote a session handoff
(`docs/handoffs/HANDOFF_2026-07-16_session6-step2-preflight.md`) and this
capture, leaving the knowledge-vault update itself uncommitted per the
repo's existing gitignore convention.

## Cases

### CASE: comment-only-src-edits-get-a-lighter-gate-than-logic-edits
status: distilled
problem: A version re-pin note in `claude_headless.py` needed a gate, but
running the full 60/60 crash harness for a docstring/comment change would
have been disproportionate to the risk, while skipping verification entirely
would have violated the project's "every session ends at a runnable,
committed checkpoint" rule.
decision: Pre-committed, before writing the diff, that comment/docstring-only
edits are exempt from the full harness gate; the required gate is the
103-unit suite plus a `git diff` review confirming zero non-comment lines
changed, with any non-comment line voiding the exemption and forcing the full
harness gate.
why: The risk a gate exists to catch (behavior change) is structurally absent
from a comment-only edit, so the proportionate check is "prove it's really
comment-only," not "re-run everything as if logic changed" — deciding the
exemption's exact boundary before writing the diff also prevented the
boundary from being redrawn after the fact to fit whatever was written.
tags: [testing, gating, process, git-hygiene]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: running the full 60/60 crash harness gate for the comment/docstring-only re-pin note, the same gate used for logic changes.
Tradeoff: running the full harness is maximally safe but disproportionate to the risk of a comment-only change; skipping verification entirely would violate the project's "every session ends at a runnable, committed checkpoint" rule.
Tipping factor: the risk a gate exists to catch (behavior change) is structurally absent from a comment-only edit, so the proportionate check is proving it's really comment-only, not re-running everything as if logic had changed.

### CASE: stale-present-tense-claims-in-src-comments-get-scoped-not-silently-corrected
status: distilled
problem: The ADR-21 `_DENY_TOOLS` block asserted, as present-tense fact, a
whole-tool-denial detection signal (`tool_result is_error` with
`permission_denials` empty) that a live probe on `claude` 2.1.211 showed no
longer occurs (the denied tool is dropped from the init manifest instead,
with no attempt at all).
decision: Rejected a version that only added a header note pointing at the
change three lines above the stale bullet; required the stale bullet itself
be qualified in place ("Detection signal (2.1.207): ...") plus an inline tag
citing the specific probe and pointing to the current behavior, so no single
line in the file states something false at the currently pinned version.
why: A reader who greps for the detection signal lands on the bullet, not the
header — a correction three lines away from the false claim it corrects is a
known-false statement left live behind a footnote, and this project's
"honesty discipline" standard applies to comments as much as to session
summaries.
tags: [documentation, honesty-discipline, adr-21, correctness]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: a version that only added a header note three lines above the stale bullet, pointing at the change without touching the bullet itself.
Tradeoff: a header-only note is a smaller diff but leaves a known-false present-tense claim live exactly where a reader who greps for the detection signal would land; qualifying the bullet in place costs a slightly larger edit but removes the false statement from the line that actually gets read.
Tipping factor: a reader searching for the detection signal lands on the bullet, not the header, so a correction three lines away is a known-false statement left live behind a footnote — and the project's honesty-discipline standard applies to comments as much as session summaries.

### CASE: production-path-fence-check-needed-its-own-deny-not-just-an-allowed-command
status: distilled
problem: The initial probe plan had the production `ClaudeHeadlessEngine.run()`
check (C4) exercise only an allowed command, leaving the claim "deny enforced
in production" resting on an inferred equivalence between the production argv
and a separately hand-built probe's argv (C1), rather than a direct
observation.
decision: Amended C4 to attempt an allowed write AND a denied `git init` in
sequence, with the same non-vacuity rule as C1 (a denied command must appear
as an exact-match tool_use in the transcript, not just be inferred absent),
and to attempt capturing the literal spawned argv (with an explicit
INFERRED-from-code-reading fallback if capture failed).
why: The whole point of a "production-path witness" probe is to close the gap
between a hand-built mirror and what actually ships — verifying only the
allowed half of the fence through that path would have left the denial half
of the claim just as inferred as before the probe existed.
tags: [testing, verification, adr-21, methodology]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: the initial probe plan, where C4 exercised only an allowed command and inferred the denial half from C1's separately hand-built probe.
Tradeoff: inferring the denial half from C1 is cheaper (one fewer probe leg) but leaves the production-path claim resting on an assumed equivalence between a hand-built mirror and what actually ships; amending C4 to attempt both an allowed and a denied command costs one more probe but closes that inference gap directly.
Tipping factor: the entire point of a "production-path witness" probe is to close the gap between a hand-built mirror and production — verifying only the allowed half through that path would leave the denial half exactly as inferred as before the probe existed.

### CASE: ambient-workspace-contamination-recorded-and-escalated-not-patched
status: distilled
problem: Live probes showed the engine's production `run()` path writes an
unrequested `knowledge/` tree into the run's working directory (an
engineering-historian hook/skill bootstrap), which would dirty a real target
repo's worktree on every single engine run and trip `is_dirty()`/reconciler
check 3 during the eventual gated live smoke.
decision: Recorded the finding verbatim in doc 14 §2.3 and NEXT.md as a
Step-3 blocker requiring an explicit ADR-level decision (sanitized settings
vs. hook suppression vs. other), and took no corrective action this session.
why: The project's frozen-architecture rule requires ADR review before any
change, and this finding surfaced mid-preflight, outside the session's
declared scope (preflight verification, not incident response) — fixing it
ad hoc would have been exactly the kind of unreviewed change the project
exists to prevent, even though the fix might seem small.
tags: [scope-discipline, adr-process, correctness, engine]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: fixing the contamination ad hoc in the same session it was discovered, since the fix might have seemed small.
Tradeoff: fixing it immediately would close the gap sooner, but the fix requires choosing among architecture-level options (sanitized settings vs. hook suppression vs. other) — exactly the kind of unreviewed change the project's frozen-architecture rule exists to prevent; recording and escalating costs a session of continued exposure but keeps the ADR-review discipline intact.
Tipping factor: the finding surfaced mid-preflight, outside the session's declared scope (verification, not incident response), and the project's frozen-architecture rule requires ADR review before any change regardless of how small the fix appears.

### CASE: knowledge-vault-writes-left-uncommitted-per-gitignore-convention
status: cited
problem: The mandatory exit sequence required writing this day-file capture
to `knowledge/issue-runtime/2026-07-16.md`, but `knowledge/` is listed in
`.gitignore:7` from a prior deliberate commit ("Adding handoff documents and
gitignore"), so the file would remain untracked after saving.
decision: Saved the capture to disk and left it unstaged/uncommitted rather
than force-adding it with `git add -f`, treating the vault the same way the
rest of the repo already does.
why: The gitignore entry reflects an explicit prior repo decision, not an
oversight, so overriding it with a force-add would silently reverse that
decision instead of respecting it.
tags: [git-hygiene, process, knowledge-vault, gitignore]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: force-adding the vault capture with `git add -f`, overriding the `.gitignore` entry to get the file tracked.
Tradeoff: force-adding gets the capture into history immediately, but silently reverses a prior deliberate repo decision (the gitignore entry from "Adding handoff documents and gitignore"); leaving it unstaged respects that decision but means the day-file capture lives only on disk.
Tipping factor: the gitignore entry reflects an explicit prior repo decision rather than an oversight, so overriding it without discussion would silently reverse someone else's deliberate choice.

Also noted (not drafted): re-labeling A3 (auth-token readability) from
VERIFIED to INFERRED-from-mode after a first read attempt was blocked by this
session's own permission classifier, then back to VERIFIED once the user
explicitly authorized the read via `!` — the label tracked what was actually
observed at each point rather than staying fixed at an early guess.

## Commands that worked
- `claude --version` / `claude --help` — established the installed CLI had
  moved to 2.1.211 and that `--max-turns` remains absent.
- Four `claude -p --output-format stream-json --verbose ...` live probes,
  each into a pre-verified-empty scratch directory, with env-strip
  re-confirmed in the same shell instance immediately before each spawn.
- `python -c "import json,pathlib; d=json.loads(...); print(d.get('claudeAiOauth',{}).get('expiresAt'))"`
  — extracted only the OAuth expiry field from the credentials file, never
  its contents, once the user authorized the read via `!`.
- `git diff --cached src/runtime/engine/claude_headless.py` — reviewed
  against staged bytes (not the working-tree version reviewed earlier) to
  confirm exactly three comment-only hunks before committing.
- `pytest tests/unit -q` — 103 passed, re-run at the actual pre-commit state
  as its own explicit check, not reused from an earlier run in the session.

## Problems solved
Whether the ADR-21 fence still held on an unpinned CLI version was unknown
until probed — resolved by four live runs showing deny enforcement,
selectivity, and chaining-resistance unchanged, with the one real behavior
change (whole-tool-denial detection mechanism) isolated and scoped rather
than conflated with an actual fence weakening.

## Mistakes
None self-identified this session; all user-directed corrections were
process amendments applied before execution (the plan review rounds) rather
than mistakes caught after the fact, and the one "check before overclaiming"
moment (A3's readability label) was corrected within the same exchange it
arose in.

## Prompts worth keeping
The user's repeated insistence on reading gate artifacts as literal diffs
rather than summaries — "the review standard here is the diff, not the
description," and "read what actually landed rather than approve it by
summary" — caught that a proposed docstring correction would have left a
false present-tense claim live next to its own correction, which a
summary-level review would not have surfaced.

## Questions I still have
None open on the mechanics of this session; the two substantive open
questions (which ADR mechanism resolves the `knowledge/`-contamination
finding, and the exact wording of the ADR-21 amendment for the 2.1.211
detection-signal change) are recorded as decisions for the user in NEXT.md
and the handoff, not as the model's own uncertainty.

## TODOs for next session
Step 3 (gated live smoke) is blocked, not next-in-sequence: resolve the
`knowledge/`-contamination ADR decision (sanitized settings vs. hook
suppression vs. other) before any engine run touches a real repo, and settle
the ADR-21 amendment wording for the whole-tool-denial detection-mechanism
change in the same pass. Both are detailed in NEXT.md and doc 14 §2.2-2.3.

---

## Session 3 (Session 7: ADR-21 Amendment 1, ADR-22 proposed, knowledge/-contamination mechanism)

## What I worked on
Ran Session 7 of the runtime: resolved the two decisions queued from Step 2, first as a gated plan (approved with six amendments by external review), then executed against that plan. Investigated the knowledge/-contamination mechanism via a five-probe matrix (plus a mandatory positive control), identified the root cause as the operator's user-scope `~/.claude/settings.json` hooks firing in every `claude` process including spawned engine children, and recorded `--setting-sources ""` as the preferred fix for ADR-22 (Proposed). Drafted ADR-21 Amendment 1 (Accepted) recording that the whole-tool-denial detection signal at claude 2.1.211 is a manifest-drop, not a result-stream signal, after confirming the init `tools` manifest is never parsed into a structured field. Landed two comment-only pointer edits in `claude_headless.py`. In a second pass, ran a discriminating check on doc 14 §2.3's p2 async-race explanation (session-ID comparison), confirmed it as a match, then went through pre-commit verification that stopped once on unexpected working-tree paths and once on a branch-name mismatch (`agent-work`, which turned out to belong to the target repo, not this build repo) before committing the 4 intended files plus the handoff at `734d08b` on `master`. Wrote a session handoff at `docs/handoffs/HANDOFF_2026-07-16_session7-adr21-adr22.md`.

## Cases

### CASE: user-scope-hooks-fire-in-all-claude-processes-including-engine-children
status: distilled
problem: The knowledge/-contamination blocker's root cause was unknown — unclear whether it came from a skill auto-load, inherited parent-session config, or something else in the spawned `claude -p` child.
decision: Identified the mechanism as the operator's user-scope `~/.claude/settings.json` SessionEnd/PreCompact hooks, which load in every `claude` process on the machine including engine children, writing the vault bootstrap before the hook's own gate; concluded that because the write is async and can land after `run()`/`reset_hard`, only prevention-at-source (not detection or later cleanup) can be deterministic.
why: Contamination was 4/4 across the probes, and the write timing rules out relying on detecting it after the fact.
tags: [adr-22, root-cause, engine, hooks]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: relying on detecting the contamination after the fact (checking for a written `knowledge/` tree post-run) or cleaning it up afterward, rather than preventing it at the source.
Tradeoff: detection/cleanup would work regardless of which hook fired and requires no change to child-spawn settings, but the write is async and can land after `run()`/`reset_hard` complete — so a post-hoc check can miss it entirely; prevention-at-source is more invasive (changes how children load settings) but is the only approach immune to the timing race.
Tipping factor: contamination was reproduced 4/4 across probes, and because the write timing is asynchronous relative to the engine's own lifecycle, only prevention at the settings-load boundary — not detection or cleanup after the fact — can be deterministic.

### CASE: empty-setting-sources-preferred-for-spawned-children
status: distilled
problem: Fixing the contamination required choosing how spawned `claude -p` children load settings; both `--setting-sources project,local` and `--setting-sources ""` tested clean of the historian hook.
decision: Recorded empty `--setting-sources ""` as the preferred A-form for ADR-22, not `project,local`.
why: `project,local` still loads project/local scope from the child's cwd, which equals the target repo, creating a cross-run persistence vector via a written `.claude/settings.json` in that repo; empty loads none.
tags: [adr-22, security, settings, engine]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: `--setting-sources project,local`, which also tested clean of the historian hook in the probe matrix.
Tradeoff: `project,local` preserves any legitimate project/local settings the target repo might want the child to see, but still loads settings scope from the child's cwd — which equals the target repo — creating a cross-run persistence vector via a settings.json the child could itself write there; the empty form gives up that scope entirely but closes the vector completely.
Tipping factor: `project,local` creates a cross-run persistence vector because the child's cwd is the target repo itself, so a written `.claude/settings.json` there would persist across runs — empty `--setting-sources ""` loads nothing and has no such vector.

### CASE: positive-control-probe-added-to-guard-against-vacuous-negatives
status: distilled
problem: The original probe plan for the contamination fix tested only suppression mechanisms, all expected to come back clean — a clean result across all of them would be indistinguishable from a matrix that simply couldn't detect the contamination at all.
decision: Added Probe 0 as a mandatory positive control (no suppression) that had to show contamination or the whole matrix would stop; ran it first, confirmed contamination at t=4s, and derived WAIT=450s from that timing before running the negative probes.
why: Without a probe proven able to detect the failure mode, five clean negative results prove nothing.
tags: [testing, methodology, verification, adr-22]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: running only the suppression-mechanism probes (all expected clean) without a probe proven able to detect the contamination in the first place.
Tradeoff: skipping the positive control saves one probe run, but a clean result across five suppression probes would be indistinguishable from a matrix that simply can't detect the failure mode at all; adding Probe 0 costs one extra run but makes every subsequent clean result meaningful.
Tipping factor: five clean negative results prove nothing without a probe proven able to detect the failure mode — Probe 0 confirmed contamination at t=4s, which is what gave the later negatives evidentiary weight.

### CASE: session-id-match-confirms-p2-async-race-not-a-falsification
status: distilled
problem: Doc 14 §2.3's explanation for p2 appearing clean in Step 2 (an async hook race) was recorded as INFERRED, and Session 7 needed to decide whether to leave it inferred, escalate it, or attempt a discriminating check.
decision: Compared the SessionEnd hook's recorded session ID (`afc8223a`) against p2's own engine-child session ID extracted from its archived transcript init line; they matched, so the async-race explanation was upgraded from inferred to verified-consistent rather than being re-scoped or falsified.
why: A match meant the hook write belonged to p2's own probe run and simply hadn't landed when Step 2's check ran, which is the exact race behavior already claimed.
tags: [verification, testing, honesty-discipline, doc14]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: leaving the p2 async-race explanation as INFERRED indefinitely, or escalating it to the user without attempting a discriminating check first.
Tradeoff: leaving it inferred is cheapest but leaves a load-bearing explanation unverified; escalating without checking wastes the user's time on a question this session could answer itself; running the session-ID comparison costs a small lookup but resolves the question directly.
Tipping factor: a discriminating check was available and cheap (compare the hook's recorded session ID against p2's own transcript session ID), so attempting it before escalating or leaving it inferred was the higher-value option.

### CASE: unexpected-working-tree-paths-halt-commit-for-explicit-scoping
status: cited
problem: Pre-commit verification found two ambient `.sweep/*` file changes and one pre-existing untracked handoff file in the working tree, none matching the described Session 7 deliverable set of 4 files.
decision: Stopped before committing per the prompt's explicit STOP instruction, reported all three paths with their diagnosed origin, and committed only the 4 intended files plus the handoff once the user confirmed that scope.
why: The `.sweep/*` changes were ambient historian churn from an unrelated prior session and the handoff file predated this session, so committing them would have misattributed unrelated changes to Session 7.
tags: [git-hygiene, process, commit-scope]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: committing all working-tree changes as-is, including the two ambient `.sweep/*` files and the pre-existing untracked handoff file, since they were already present when the commit was prepared.
Tradeoff: committing everything present is faster and avoids an extra round-trip, but risks misattributing unrelated changes (ambient historian churn, a prior session's handoff) to this session's deliverable; stopping and scoping explicitly costs a pause but keeps commit history accurate.
Tipping factor: the `.sweep/*` changes were ambient churn from an unrelated prior session and the handoff file predated this session, so committing them would have misattributed unrelated changes to Session 7.

### CASE: agent-work-branch-name-was-cross-repo-prompt-error
status: distilled
problem: The commit instructions specified branch `agent-work`, but the issue-runtime repo was on `master` with no such branch existing.
decision: Proceeded on `master` and dropped the branch name from the commit message, after the user clarified that `agent-work` belongs to the target repo the agent runtime operates on (StockPhotoAgent), not to this build repo.
why: The user stated explicitly that `agent-work` was an error carried over into the prompt from the target-repo context.
tags: [git, process, multi-repo, commit]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: creating a branch named `agent-work` in this repo to satisfy the commit instructions literally, since that was the named branch.
Tradeoff: creating the branch would satisfy the instruction's letter but introduces a branch with no purpose in this repo, papering over what was actually a cross-repo prompt error rather than resolving it; proceeding on `master` after clarifying costs a pause to confirm but keeps the repo's branch structure honest.
Tipping factor: the user clarified directly that `agent-work` belongs to the target repo the runtime operates on (StockPhotoAgent), not to this build repo, making the instruction a carried-over error rather than a real requirement.

## Commands that worked
- `git branch --show-current` — confirmed `master` before proceeding past the branch-mismatch STOP.
- `git status` (pre-commit) — surfaced the two ambient `.sweep/*` paths and the untracked handoff file, triggering the scoping STOP.
- Five live `claude -p` contamination probes (Probe 0 positive control plus Probes 1, 2, 3, 4), each polling the scratch cwd for `knowledge/` up to WAIT=450s — 0 re-runs against a ceiling of 5, 0 deviations.
- `git add` scoped to exactly the 4 intended files plus the handoff, followed by commit — new SHA `734d08b`.
- `pytest tests/unit -q` (re-run at pre-commit state) — 103 passed.

## Problems solved
Whether `--setting-sources ""` breaks auth or the fence for spawned children was unknown → Probe 2 confirmed `rc=0`, `apiKeySource=none` unchanged from baseline, and Probe 3 confirmed `git init` still denied (populated `permission_denials`, `is_error:true`, `.git` absent) under that flag, closing the question in favor of the empty form.

## Mistakes
None self-identified this session; both STOPs (unexpected working-tree paths, branch mismatch) were the agent correctly halting on a mismatch between instructions and observed repo state rather than a genuine error, and both were resolved by the user's clarification within the same exchange.

## Prompts worth keeping
The Session 7 execution prompt's numbered branch logic for the p2 discriminating check (MATCH / NO MATCH / CANNOT LOCATE, each with a pre-specified §2.3 update) let the check run and resolve without needing a follow-up clarification on how to record the result.

## Questions I still have
None open on the mechanics of this session; the substantive open question (which ADR-22 option to adopt) is explicitly deferred to the user for Session 8, not left as the model's own uncertainty.

## TODOs for next session
Session 8 awaits Adi's ADR-22 option selection (the `--setting-sources ""` fix vs. alternatives), which unblocks both the mechanism implementation and Step 3 (gated live smoke).

---

## Session 4 (recovered 2026-07-25 via /distill --replay of a queued pending.log entry; transcript dated 2026-07-16)

### What I worked on
A follow-on session opened with a `/model` switch to Fable 5, then the user issued a plan-only task (investigate the knowledge/-contamination hook mechanism and draft an ADR-21 amendment, Plan Mode, gated by review, no code/config changes). The session did not get underway: the assistant's first two responses were both `529 Overloaded` server errors, and the transcript ends there with no substantive work performed.

### Cases
None — the session never progressed past repeated server errors, so no decision, investigation, or plan was produced to evaluate against the capture gates.

### Commands that worked
None.

### Problems solved
None.

### Mistakes
None — the 529 errors are an external service issue, not an error of action.

### Prompts worth keeping
None.

### Questions I still have
None new.

### TODOs for next session
- Retry the queued plan-only task: investigate the knowledge/-contamination hook mechanism (options a/b/c) and draft the ADR-21 amendment text, per the user's original instructions, stopping for review before any code or config changes.
