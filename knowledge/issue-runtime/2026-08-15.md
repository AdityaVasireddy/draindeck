---
project: issue-runtime
date: 2026-08-15
type: mixed
stack: [python, pytest, powershell, git, windows]
status: durability gate CLOSED and GREEN — 60/60 both seeds (42, 1337) including f4's retain-path guard; fix committed at HEAD 0ca147b. Separately, the /resolve-item wrapper's Phase 4 mandatory-review gap was diagnosed and corrected (commit c170edc adds the workflow file), the mojibake fix itself was committed (5ce9f4f) after a completed Phase 4 review, and a README/ADR-19-closure documentation pass was independently audited and committed (156069d). Working tree otherwise clean aside from pre-existing untracked clutter.
---

# issue-runtime — 2026-08-15

## What I worked on
Ran two independent, strictly read-only senior code reviews of the `agent/backlog-resolution` backlog (B1–B7), then diagnosed the durability crash harness's fixture-f4 failure as a stale fixture rather than a production regression. In a follow-on session, Adi chose Option B (fix the real "stuck-resolving orphan" gap in `src/`) and walked the change through a full five-gate scope/design cycle before authorizing implementation, landing and committing it as `0ca147b`. A later session opened read-only plugin diagnostics and paused mid-way through invoking the agent-skills `/build` flow pending clarification on task-source authority and commit gating. In a further session, Adi ran `/resolve-item` to fix cosmetic mojibake in `main.py`, caught (via a read-only postmortem) that the run had skipped the wrapper's supposedly-mandatory final code-review Skill invocation, corrected the wrapper contract, retroactively completed the missing review, set up a `.gitignore` allowlist so the corrected wrapper file could be tracked without leaking personal `.claude/settings.local.json`, and committed both the workflow file and the mojibake fix as separate authorized commits. The same session then ran a three-part documentation/ADR consistency task (README expansion, ADR race-condition cleanup, ADR-19 verdict ruling), subjected the ADR-19 conclusion to an adversarial independent audit against raw `state/events.jsonl` and git history, corrected one blocking ambiguity the audit surfaced, and committed the documentation work as `156069d`.

## Session 3 — durability gate closed (Option B implemented, committed)
Continuing from the diagnosis handoff, Adi drove a strict scope-before-code sequence: (1) reap rule design citing `started_at` as the only existing timing field and deriving the stale bound from `cfg.timeout_seconds + cfg.containment_confirmation_seconds + margin`, surfacing along the way that Windows executions never promote `resolving`→`resolved` (by design — Job-object containment doesn't need the POSIX PID-chain walk) and so must not be timed against the short POSIX resolve-loop constant; (2) an amendment threading `stale_after_seconds` as an explicit kwarg on `reap_orphans`/`is_execution_alive` rather than backdating fixture JSON, to keep the harness on the real `_write_pidfile` write path; (3) an adversarial "fresh resolving is retained" assertion (large threshold, must NOT reap) added to f4 rather than a new fixture, to prove the timeout comparison direction on the same live record. Only after all of this was authorization given to implement. The change landed cleanly: `_worker_liveness` gained a `resolving`-state branch, `reap_orphans` branches its pid source (`worker` vs `shim`) by record state, both harness seeds went 60/60, and the two files were staged/committed/verified in discrete confirmed steps (diff --stat, staged-file list, final `reap_orphans` block, post-commit clean status) before the commit was authorized.

## Session 4 — plugin diagnostics and /build scope check (read-only, no changes)
A separate, explicitly read-only session opened to diagnose why `autonomous-engineering` plugin commands (`/spec`, `/build`, etc.) weren't visible when Claude Code was launched with `--plugin-dir`. The assistant confirmed the plugin was loaded (SessionStart context present, skills enumerated, `claude plugin validate` passed) and that its commands are exposed under the `agent-skills:` namespace rather than as bare top-level commands, proving this by successfully invoking `/agent-skills:spec`. The user then invoked `/agent-skills:spec` for real; the assistant declined to run a fresh spec-driven-development cycle because a `SPEC.md` already exists in this repo (a B1–B6 backlog spec dated 2026-08-14) and the architecture is frozen per `CLAUDE.md`. The user was interrupted mid-tool-call and instead invoked `/agent-skills:build`. The assistant flagged that `/build`'s default per-task auto-commit step conflicts with `CLAUDE.md`'s no-auto-commit rule, and separately noticed three different, disagreeing sources of "what to do next" in the repo (`tasks/plan.md` T1–T8, `CLAUDE.md`'s stale "Session 3" note, and `NEXT.md`'s newer "Session 22" pointer, plus three same-day handoff files including one suggesting the durability work was blocked). The session ends with the assistant asking the user to resolve which task source is authoritative and how to handle commit gating, before doing any implementation work.

## Session 5 — resolve-item wrapper hardening, mojibake fix, README/ADR-19 documentation (all committed)
Adi ran `/resolve-item` to fix cosmetic mojibake (`â€”` → `—`) in three `main.py` print strings. The run completed and self-declared a formal `code-review-and-quality` Skill pass unnecessary given the triviality of the diff, going straight to manual diff inspection instead. Adi then ran a read-only postmortem, which traced the bypass to `.claude/commands/resolve-item.md`'s Phase 1 blast-radius scaling clause being unscoped, so the model generalized it into Phase 4 even though Phase 4's own wording was unconditionally mandatory. Adi authorized one narrow correction to the wrapper: Phase 4's `code-review-and-quality` invocation is now stated as mandatory regardless of blast radius (only review *depth* scales), while Phase 3's `incremental-implementation`/`test-driven-development` are now explicitly conditional-with-mandatory-skip-reporting for genuinely trivial non-behavioral changes. The missing Phase 4 review was then retroactively run against the already-implemented mojibake diff (Approve, no findings). Adi then investigated tracking the corrected wrapper file safely: `settings.local.json` was found to be un-ignored by the *repo's* `.gitignore` (it only appeared ignored locally due to a personal global gitignore rule at `C:\Users\adity\.config\git\ignore`, which wouldn't apply on another clone/machine), so a `.claude/* ` + allowlist-for-`commands/` block was added to the repo's `.gitignore`. Two separate authorized commits followed: `c170edc` (`.gitignore` + `resolve-item.md`) and `5ce9f4f` (the mojibake fix alone). A further `/resolve-item` run then handled a three-item documentation/ADR task: extended the existing (not missing, as the item's premise assumed) README with newcomer sections, searched all docs/src for misleading same-engine concurrent-writer race reasoning and found none requiring an edit, and added an append-only ADR-19 closure clarification to `docs/08`. The mandatory Phase 4 review caught and fixed two inaccuracies (a nonexistent `docs/17-*` reference — actual ceiling is doc 15 — and an incorrect claim about reviewer/validator construction order in the startup workflow description) before finishing. Adi then ran an adversarial independent audit of the ADR-19 conclusion directly against `state/events.jsonl` and git history (not the new doc-08 text), which confirmed every figure but flagged that the addendum's wording didn't make explicit that Sample 1 (n=20) alone satisfies ADR-19 without needing Sample 2. Adi authorized a narrow correction adding that evidence-hierarchy language, plus one incidental README citation fix (adding ADR-01 to an existing attribution). Final authorized commit `156069d` shipped both files.

## Cases

### CASE: durability-fixture-f4-stale-not-regression
status: auto
problem: durability harness fails identically at fixture f4 on both seed 42 and seed 1337 (55/60 passed), AssertionError "reap_orphans did not report 042-e1: []"
decision: diagnosed as a stale test fixture, not a production regression — `run_engine_orphan_fixture` registers the orphan via `engine._write_pidfile()` alone (always writes `version:2, state:"resolving"`, no `worker`/`ancestry`) and never calls `_resolve_and_persist_worker`, so `reap_orphans()`'s empty return is correct under current policy
why: hand-traced `_worker_liveness` against the fixture's actual record — `state != "resolved"` returns `"unknown"` before reaching the alive/dead branches, and `"unknown"` records are intentionally retained rather than reaped, exactly matching the observed empty `repairs` list
tags: [durability, harness, crash-recovery, liveness, diagnosis]
until: Adi picks Option A (patch the fixture to drive the real `_write_pidfile → _resolve_and_persist_worker` path) or Option B (treat the stuck-"resolving" orphan window as a real src/ defect) and the harness shows 60/60 on both seeds

### CASE: reap-stuck-resolving-via-shim-timeout
status: auto
problem: v2 pidfile records left in `state:"resolving"` past a crash (before `_resolve_and_persist_worker` ever runs) were retained indefinitely by `_worker_liveness`'s "unknown" verdict, so a genuinely dead orphan in that window was never reaped
decision: added a `resolving`-state branch to `_worker_liveness` classifying via `started_at` age against a timeout — fresh→`"unknown"` (retained), timed-out+shim-alive→`"alive"`, timed-out+shim-dead→`"dead"` (reaped), timed-out+shim-unresolvable/malformed→`"unknown"`
why: `started_at` is the only timing field written unconditionally by `_write_pidfile` for every v2 record regardless of state, so it was the only available signal without adding a new field; the reap rule had to distinguish "still legitimately mid-resolve" from "crashed before resolving finished"
tags: [durability, recovery, orphan-reaping, liveness, worker-resolution]

### CASE: windows-resolving-timeout-tied-to-cfg-not-resolve-loop
status: auto
problem: choosing a stale-timeout bound for the new `resolving`-state reap branch risked using the POSIX resolve-loop constant (`_LEAF_MAX_SECONDS = 10.0`) as the threshold
decision: tied the timeout to `cfg.timeout_seconds + cfg.containment_confirmation_seconds + margin` instead, not the short resolve-loop constant
why: `_resolve_and_persist_worker` (which promotes `resolving`→`resolved`) only runs on the POSIX path — the Windows path (`_run_windows_contained`) writes the pidfile once and never promotes it, staying `"resolving"` for the entire legitimate run by design (Job-object containment tracks membership directly). A short resolve-loop-tuned timeout would have made every in-flight Windows execution falsely "reapable" long before it finished.
tags: [durability, windows, containment, timeout, cross-platform]

### CASE: stale-after-seconds-threaded-param-not-fixture-backdate
status: auto
problem: the harness needed a way to make f4's fresh `resolving` record appear stale without waiting out the real cfg-derived timeout
decision: threaded `stale_after_seconds: Optional[float] = None` as an explicit kwarg on `reap_orphans` and `is_execution_alive` (default `None` → cfg-derived) rather than having the harness hand-construct a backdated `started_at` in the fixture JSON
why: backdating the fixture's JSON directly would duplicate `_write_identity_record`'s format outside the real production write path — exactly the kind of fixture drift that produced the original f4 gap (the fixture calling `_write_pidfile` alone, one production step short of reality); threading the param keeps the harness on the real write path and only substitutes the threshold
tags: [durability, harness, testing, fixture-design]

### CASE: f4-adversarial-retain-guard-extends-existing-fixture
status: auto
problem: the 0.0-threshold reap injection alone could not prove the timeout comparison direction (an inverted comparison would also "pass" a same-side test)
decision: added an adversarial assertion — call `reap_orphans(stale_after_seconds=9999)` first, assert the fresh record is NOT reaped, its pidfile still exists, and the dummy process is still alive — as an extension of f4's existing record/process rather than a new fixture
why: f4 already constructs the exact artifact needed (a real v2 `resolving` record backed by a genuinely running child); reusing it proves the comparison direction on identical data instead of a second synthetic record that could accidentally exercise a different code path, and avoids spawning a second 300s child for no added rigor
tags: [durability, harness, testing, adversarial-assertion]

### CASE: autonomous-engineering-commands-namespaced-not-bare
status: auto
problem: launching Claude Code with `claude --plugin-dir "C:\Projects\OPEN_SOURCE_PROJECTS\autonomous-engineering"` and typing `/` did not show the plugin's expected bare commands (`/spec`, `/plan`, `/build`, `/test`, `/review`, `/webperf`, `/code-simplify`, `/ship`)
decision: confirmed the plugin loaded correctly (SessionStart context present, skills enumerated, `claude plugin validate` passing) but its commands are exposed under the `agent-skills:` namespace (`/agent-skills:spec`, `/agent-skills:build`, etc.), not as bare top-level commands — verified directly by successfully invoking `/agent-skills:spec`
why: the injected skill listing enumerated all commands as `agent-skills:*`, and the plugin's `commands` field in `.claude-plugin/plugin.json` pointed at two directories (`./.claude/commands` markdown files and `./commands` TOML files for a different, sibling tool integration), with only the namespaced markdown set actually recognized by this Claude Code version
tags: [claude-code, plugins, command-namespacing, diagnostics]

### CASE: resolve-item-phase4-review-made-unconditionally-mandatory
status: auto
problem: a `/resolve-item` run for a trivial cosmetic mojibake fix skipped the wrapper's `agent-skills:code-review-and-quality` Skill invocation, self-justifying the skip as unnecessary ceremony for a 3-character diff; a postmortem found the wrapper's own text plausibly permitted this
decision: corrected `.claude/commands/resolve-item.md` so Phase 1's blast-radius scaling clause explicitly scopes to Phases 2–3 only, and Phase 4's `code-review-and-quality` invocation is now worded as mandatory for every run regardless of blast radius, with only review *depth* (not invocation) permitted to scale
why: Phase 1 step 4's scaling instruction ("scale process depth accordingly ... do not apply heavy-review ceremony to a low-blast-radius item") carried no scope limiter and CLAUDE.md's own "process depth scales to blast radius" rule is stated as a global hard rule, so the model generalized it into Phase 4 even though Phase 4 was the one phase written with no conditional escape hatch — an ambiguity the postmortem traced to the missing scope limiter, not a reasoning failure at the point of decision
tags: [resolve-item, workflow-contract, code-review, wrapper-correction, self-correction]

### CASE: incremental-tdd-conditional-skip-with-mandatory-reporting
status: auto
problem: correcting the Phase 4 mandatory-review gap risked overcorrecting into making every phase (including `incremental-implementation`/`test-driven-development`) unconditionally mandatory, which would reintroduce ceremony for genuinely trivial non-behavioral changes
decision: kept `incremental-implementation`/`test-driven-development` explicitly conditional — skippable for genuinely trivial non-behavioral/cosmetic changes, but the skip and its reason must now be reported in the final "Agent-skills invoked" section, modeled on the existing conditional pattern already used for `planning-and-task-breakdown`
why: the postmortem's own mandatory-vs-conditional matrix judged these two skills as legitimately proportional to blast radius (unlike the final review, which is the wrapper's one deliberate absolute gate), and Adi's correction instructions explicitly required this distinction be preserved rather than collapsed
tags: [resolve-item, workflow-contract, tdd, proportionality, ceremony]

### CASE: claude-dir-gitignore-allowlist-for-commands-only
status: auto
problem: the corrected `resolve-item.md` needed to be trackable in git so future clones/sessions retain it, but `.claude/settings.local.json` (personal, absolute-path-laden permission allowlist) had to stay untracked; the repo's own `.gitignore` had zero `.claude` rules, and the file only appeared safely ignored locally due to a personal global gitignore (`C:\Users\adity\.config\git\ignore`) that would not apply on another machine or clone
decision: added `.claude/*` plus `!.claude/commands/` and `!.claude/commands/**` to the repo's tracked `.gitignore`, verified via `git add --dry-run` (unambiguous over `git check-ignore -v`, which was found to exit 0 even when a re-inclusion `!` rule was the one matching) that `resolve-item.md` becomes addable while `settings.local.json` stays blocked
why: relying on the personal global gitignore was diagnosed as accidentally-correct-for-one-machine-only; the blanket-then-allowlist pattern was chosen specifically so any future file dropped into `.claude/` defaults to ignored, and only the `commands/` subtree is deliberately re-included
tags: [git, gitignore, workflow-tooling, portability]
until: verified only on this machine/session — full confirmation would be a fresh clone showing the same behavior with no personal global gitignore present

### CASE: adr19-sample1-alone-satisfies-closure-not-jointly-with-sample2
status: auto
problem: an independent adversarial audit of the just-drafted ADR-19 closure addendum (in `docs/08`) found the wording ambiguous about whether Sample 1 (n=20) and Sample 2 (n=19) were jointly necessary to reach the 20-issue validation requirement, versus Sample 1 alone already satisfying it
decision: added an explicit "evidence hierarchy" passage to the append-only addendum stating Sample 1 (n=20) alone satisfies ADR-19's binding sample-size/attempt-1/cost thresholds independently, Sample 2 is corroborating (not a second required leg), dedicated sampling is therefore not still owed, and the later 74-DONE drain is cumulative production history that does not itself satisfy ADR-19 (it lacks recorded population-wide attempt-1/cost metrics)
why: the audit re-derived Sample 1's figures directly from raw `state/events.jsonl` and git history (independent of the new doc-08 text and of the drain), confirming n=20/17-attempt-1/~$0.36-cost stood alone against ADR-19's binding requirements in `docs/08` §4 — so the addendum's prior phrasing understated what was already independently established and needed to say so explicitly rather than implying joint necessity
tags: [adr, adr-19, documentation, evidence, audit, kill-criteria]

## Commands that worked
- `.venv\Scripts\python.exe tests\crash\harness.py "$TEMP\ch42run3" 42` — seed 42, ALL 60 SCENARIOS PASSED, f4 green including the new retain-path guard.
- `.venv\Scripts\python.exe tests\crash\harness.py "$TEMP\ch1337run3" 1337` — seed 1337, ALL 60 SCENARIOS PASSED.
- `python -m pytest tests\unit -q` (post-implementation) — 228 passed, no regressions.
- `git add src/runtime/engine/claude_headless.py tests/crash/harness.py` then `git commit -m "fix(recovery): reap stuck-\"resolving\" orphans past timeout"` — new HEAD `0ca147b`, confirmed via `git --no-pager log -1 --stat --oneline` showing exactly the two intended files, then `git --no-pager status --short` confirmed a clean tree with only pre-existing untracked items.
- Durability harness run (B7-diagnosis session), both seeds — reported 55 passed then crash at fixture f4 on each. UNVERIFIED exact command text for that earlier run, but the 55-pass/crash outcome was pasted as raw output.
- Unit suite during B1–B6 review — reported 205 passed (~36s), run by the reviewer independently.
- Full unit suite during B7 review — reported 228 passed (23 new B7 tests, no regressions).
- `claude plugin validate` against the `autonomous-engineering` plugin directory — passed, with one unrelated warning about root `CLAUDE.md` not being used as context.
- `/agent-skills:spec` — invoked successfully, confirming the plugin's namespaced command form works.
- `.venv/Scripts/python.exe -m pytest tests/unit -q` (mojibake fix, pre- and post-edit) — 228 passed, 0 failed both times.
- `git add --dry-run .claude/settings.local.json` → blocked ("ignored ... hint: Use -f"); `git add --dry-run .claude/commands/resolve-item.md` → `add '.claude/commands/resolve-item.md'` — confirmed the `.gitignore` allowlist worked as intended before staging anything for real.
- `git check-ignore -v .claude/some-future-runtime-state.json` → matched `.gitignore:15:.claude/*`, confirming default-ignore for unrelated future `.claude` content.
- `git add .gitignore .claude/commands/resolve-item.md && git commit -m "feat(dev): add autonomous Claude resolve-item workflow"` — HEAD `c170edc`, verified staged-file list and diff before committing.
- `git add src/runtime/main.py && git commit -m "fix(main): correct mojibake em dashes in CLI output strings"` — HEAD `5ce9f4f`, verified diff contained only the 3 string-literal substitutions.
- `git add README.md docs/08-session-0-closure-and-adr-amendments.md && git commit -m "docs: improve README and clarify ADR-19 closure evidence"` — HEAD `156069d`, verified staged diff and confirmed no `src/`/`state/`/config file included.

## Problems solved
- Durability harness crashing at fixture f4 on both seeds → root cause: `run_engine_orphan_fixture` only exercised `_write_pidfile`, never `_resolve_and_persist_worker`, so a genuinely-stuck `resolving` record was correctly (per then-current policy) never reaped, but that policy itself left a real crash-window orphan leak → fix: `_worker_liveness` gained a timeout-based `resolving` classification (via shim identity), `reap_orphans`/`is_execution_alive` gained a `stale_after_seconds` bound, committed `0ca147b`, 60/60 both seeds.
- `autonomous-engineering` plugin's bare commands (`/spec` etc.) not appearing in the `/` menu → root cause: the plugin exposes commands under the `agent-skills:` namespace, and the `./commands` TOML directory it also declares is for a different tool integration, not Claude Code → resolution: use `/agent-skills:spec`, `/agent-skills:build`, etc.
- `/resolve-item` skipping its supposedly-mandatory final code review → root cause: an unscoped blast-radius scaling clause in Phase 1 was generalized by the model into Phase 4, which had no conditional wording of its own → fix: scoped the scaling clause to Phases 2–3 and made Phase 4 explicitly unconditional; the missing review was then retroactively run (Approve, no findings) before the mojibake fix was committed.
- `.claude/settings.local.json` appeared safely ignored but only via a personal, machine-local global gitignore rule, not the repository's own `.gitignore` → root cause: repo `.gitignore` had zero `.claude` rules → fix: added `.claude/*` + `!.claude/commands/**` allowlist to the tracked `.gitignore`, verified via `git add --dry-run`.
- Draft ADR-19 closure addendum read ambiguously as requiring both samples jointly → root cause: initial wording didn't state the evidence hierarchy explicitly → fix: added an explicit passage stating Sample 1 (n=20) alone satisfies the requirement, confirmed by an independent audit against raw event-log/git evidence.

## Mistakes
- The `/resolve-item` mojibake run skipped the wrapper's Phase 4 mandatory code-review Skill invocation, self-justifying the skip as unwarranted ceremony for a trivial diff — caught by a subsequent read-only postmortem (not by the run itself), corrected by tightening the wrapper contract and retroactively running the missed review.
- During the README expansion, two inaccuracies were introduced and then self-caught during the mandatory Phase 4 review before finishing: a reference to a nonexistent `docs/17-*` file (actual ceiling is doc 15), and a "Basic run workflow" description implying reviewer/validator objects are constructed earlier in startup than they actually are. Both were corrected against source before the run completed.
- The initial ADR-19 closure addendum in `docs/08` was ambiguous about whether Sample 1 alone satisfied the requirement or whether Sample 2 was also necessary — caught by an independent adversarial audit run in a separate step, corrected with an explicit evidence-hierarchy passage.

## Prompts worth keeping
None.

## Questions I still have
- Whether the B5 non-terminal `REVIEWING` execution (LOW finding, prior session) will be remediated or accepted as a known consistency wart.
- Whether the `config.example.yaml` double-nested PowerShell example will be corrected.
- Which task source is authoritative for `/build`: `tasks/plan.md` (T1–T8), or the newest handoff (three same-day handoffs exist, one named as if the durability gate were blocked)?
- Should `docs/handoffs/HANDOFF_2026-08-15_durability-gate-f4-blocked.md` (or whichever is actually latest) be read first before any `/build` work, per `CLAUDE.md`'s "read the newest handoff" rule?
- Given the no-auto-commit hard rule, should `/build` implement and test each task but stop before committing, presenting the diff for explicit per-commit go-ahead?

## TODOs for next session
- Pick up item 1 from the new handoff: reviewer-parser hard-halt (qwen returning prose instead of APPROVE/REJECT halts the whole run) — run scope/design under five gates before any `src/` change.
- Remaining ranked hardening items from the handoff: hardcoded absolute paths; Windows/PowerShell-only coupling; hardcoded Ollama/qwen reviewer dependency; assess ADR-19's 20-issue kill-criteria sample against the completed drain (only n=5 pre-drain) — now largely resolved this session, see `adr19-sample1-alone-satisfies-closure-not-jointly-with-sample2`.
- Decide disposition of the two LOW B1–B6 findings (B5 dangling `REVIEWING` execution; config.example double-PowerShell).
- Note: engine file lives at `src/runtime/engine/claude_headless.py` (not `src/runtime/`); CLAUDE.md's unit-test baseline "expect 117" is stale (actual 228, no regression) and should be updated.
- Resolve which of `tasks/plan.md`, `NEXT.md`, and `CLAUDE.md`'s "Current task" line is authoritative before running `/agent-skills:build` — `CLAUDE.md`'s pointer (Session 3 / RepositoryAdapter) appears stale relative to `NEXT.md` (Session 22) and should likely be updated or removed.
- Decide the commit-authorization mode for `/agent-skills:build` runs in this repo (implement+test then pause for per-task approval, vs. some other gating) given the project's no-auto-commit hard rule.
- Consider whether the corrected `.claude/commands/resolve-item.md` Phase 4 wording should be verified on a second, genuinely non-trivial `/resolve-item` run to confirm the mandatory-invocation fix holds under real pressure, not just the trivial case that exposed the original gap.
