---
project: issue-runtime
date: 2026-08-15
type: mixed
stack: [python, pytest, powershell, git, windows]
status: B1–B7 backlog independently reviewed read-only (HEAD 1dac0bc, 228 tests pass); durability gate (tests/crash/harness.py) diagnosed BLOCKED — fails identically at fixture f4 on both seeds 42/1337 (55/60), root cause traced to a stale fixture rather than a production regression; handoff written to docs/handoffs/HANDOFF_2026-08-15_durability-gate-f4-blocked.md; fix approach (test-only vs src/) left for user to pick.
---

# issue-runtime — 2026-08-15

## What I worked on
Ran two independent, strictly read-only senior code reviews of the `agent/backlog-resolution` backlog. First pass covered B1–B6 (portable config, deterministic PowerShell validation, README, worker liveness/containment, bounded malformed-reviewer handling, truthful reviewer prerequisites): confirmed HEAD at `e43bc72`, ran the unit suite myself (205 passed), and raised two LOW findings — a B5 parse-error escalation that leaves the execution non-terminal in `REVIEWING`, and a `config.example.yaml` validation command that double-nests PowerShell. Second pass verified the B7 remediation (`1dac0bc`, "enforce authoritative event log ownership"): audited `events/log.py` writer-lease/exclusivity, `ReadOnlyEventLog` non-mutation, ownership ordering (`WorkspaceLease → EventLog writer`), and recovery wiring, ran the B7-relevant suites (55 passed) then the full suite (228 passed, up from 205 via 23 new tests), and verdicted both original B7 P1 findings RESOLVED. No code was modified, staged, or committed.

## Session 2 — durability gate diagnosis
A follow-on session (raw-evidence-only, no code/harness edits, no commits) investigated why the durability crash harness had never actually been run in either review. Confirmed `_resolve_and_persist_worker` is on the live spawn path (unconditional, not gated behind `ITEM9_SENTINEL`) and that `is_execution_alive`/`_worker_liveness` judge liveness on a resolved leaf-worker identity. Ran the harness on both seed 42 and seed 1337: both crash identically at the 3rd fixture (`f4`, `run_engine_orphan_fixture`) after 55/60 passes, with `AssertionError: f4: reap_orphans did not report 042-e1: []` at harness.py:413 — a harness-side assertion, not a `src/` exception. Traced the fixture's own pidfile write and hand-walked `_worker_liveness` against it to determine whether the empty `reap_orphans()` return was correct or a real regression, then wrote a handoff (`docs/handoffs/HANDOFF_2026-08-15_durability-gate-f4-blocked.md`) laying out the diagnosis and a two-option fork for Adi to decide.

## Cases

### CASE: durability-fixture-f4-stale-not-regression
status: auto
problem: durability harness fails identically at fixture f4 on both seed 42 and seed 1337 (55/60 passed), AssertionError "reap_orphans did not report 042-e1: []"
decision: diagnosed as a stale test fixture, not a production regression — `run_engine_orphan_fixture` registers the orphan via `engine._write_pidfile()` alone (always writes `version:2, state:"resolving"`, no `worker`/`ancestry`) and never calls `_resolve_and_persist_worker`, so `reap_orphans()`'s empty return is correct under current policy
why: hand-traced `_worker_liveness` against the fixture's actual record — `state != "resolved"` returns `"unknown"` before reaching the alive/dead branches, and `"unknown"` records are intentionally retained rather than reaped, exactly matching the observed empty `repairs` list
tags: [durability, harness, crash-recovery, liveness, diagnosis]
until: Adi picks Option A (patch the fixture to drive the real `_write_pidfile → _resolve_and_persist_worker` path) or Option B (treat the stuck-"resolving" orphan window as a real src/ defect) and the harness shows 60/60 on both seeds

## Commands that worked
- Durability harness run, both seeds — reported 55 passed then crash at fixture f4 on each (exact command string not shown verbatim in this transcript; argv is positional per seed, not a `--seed` flag). UNVERIFIED command text, but the 55-pass/crash outcome was pasted as raw output.
- Unit suite during B1–B6 review — reported 205 passed (~36s), run by the reviewer independently.
- B7-relevant suites — reported 55 passed.
- Full unit suite during B7 review — reported 228 passed (23 new B7 tests, no regressions).

## Problems solved
None — the durability crash was diagnosed, not fixed (root cause: stale fixture never calling `_resolve_and_persist_worker`), and the B1–B7 findings were raised, not fixed (both reviews were read-only).

## Mistakes
None recorded in the transcript.

## Prompts worth keeping
None.

## Questions I still have
- Whether Adi will choose Option A (test-only fixture fix) or Option B (src/ fix for the stuck-"resolving" orphan window) for the durability gate blocker.
- Whether the B5 non-terminal `REVIEWING` execution (LOW finding) will be remediated or accepted as a known consistency wart.
- Whether the `config.example.yaml` double-nested PowerShell example will be corrected.
- Cosmetic mojibake in three `main.py` stderr strings noted during B7 review — left unaddressed.

## TODOs for next session
- Resolve the durability-gate decision fork (Option A vs Option B) per the handoff, then re-run `tests/crash/harness.py` and confirm 60/60 on both seed 42 and seed 1337.
- Decide disposition of the two LOW B1–B6 findings (B5 dangling `REVIEWING` execution; config.example double-PowerShell).
- Fix the mojibake in the three B7 stderr print strings if cosmetic cleanup is wanted.
- Do not trust the B1–B7 PASS marks for reaping/liveness until the durability gate is green — both review docs skipped durability.
