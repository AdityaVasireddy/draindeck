---
project: issue-runtime
date: 2026-07-26
type: mixed
stack: [python, pytest, git, bash, subprocess, markdown]
status: NEXT.md restructure-and-audit arc closed across two commits — 2f232e7 (938→232-line pointer-file rewrite, session-history archived to docs/handoffs/next-md-archive-2026-07-26.md, 3 carried-unwitnessed surfaces homed to a new doc14 EOF section) then 0f3dee8 (honest-provenance relabel of two missed pre-committed targets, three doc14 pointers resolved to exact ranges with one materially corrected, action-3/CLI-2.1.214-tickle contradiction reconciled to one trigger). Both commits scoped-checked via git status before staging; only NEXT.md-class files touched in either. Step-3 precondition #4 (baseline non-vacuity) work from earlier in the day stands as previously recorded — CLOSED, no further change. A third session then codified the process-depth-scales-to-blast-radius correction directly into CLAUDE.md (commit e049aa3) and evicted the historian experiment vault (knowledge/) from git tracking (commit 70cd4c9), both scope-checked via git status/ls-files before staging. A fourth session ran the literal doc14 §2.4 ADR-22 Probe 2/3 re-probe at CLI 2.1.220 (both legs PASS, commit ab99f55), synced NEXT.md §2 to the §4 closure (commit aa75231), generated the session-18 handoff (commit 717865c), and closed out a user-reported "duplicate item 3" that on inspection did not exist in the committed file. A fifth session closed two light-tier doc items flagged by the session-18 orientation read — re-confirmed NEXT.md §1's stale doc14 pointer via a live `findstr` (resolved to L1129, matching the orientation read) and added the "no commit without explicit authorization" bullet to CLAUDE.md verbatim — committed as 931ba5d, then wrote and committed the session-19 handoff (3e77a74). A sixth session (Session 20) resolved NEXT.md §2 item 2 (ingest branch-check gap) via Option A — inserted a `cfg.project.branch` checkout as new step 5b in `cmd_run` before recovery/baseline, reusing the existing `checkout_branch` adapter method with no `create_from`, durability harness re-run green on both seeds — committed as c6f2512 alongside an ADR-20 Amendment 1 and NEXT.md/CLAUDE.md updates, followed by the session-20 handoff (98fa063). A seventh session (Session 21) resolved NEXT.md §2 item 1 (live-smoke sequencing vs. the 3 carried-forward-unwitnessed surfaces) as Option B — carry all three surfaces labeled into live-smoke design with an A-deferred probe-reopening hatch — after confirming via raw grep that no existing harness scaffolding drives the real `cmd_run` entrypoint, making a standalone witness probe strictly costlier than live smoke itself for two of the three surfaces; committed as ebad895 alongside the session-21 handoff. src/ changed only in Session 20 (`main.py`); durability harness re-run and green both seeds that session. No src/ change in Session 21; durability harness correctly not run.
---

# issue-runtime — 2026-07-26

## What I worked on
Closed out Step-3 precondition #4's mutation leg (doc 08 §5d non-vacuity requirement) against StockPhotoAgent. Re-confirmed the collected>0 witness (rc=0, `26 passed`) via `subprocess.run(cmd, cwd=..., shell=True)` from `C:\Python314\python.exe` — the same call shape as `Validator._run_once` — after an initial attempt through the Bash tool produced a plausible-looking false negative (RC=4, `collected 0`) from pure shell-quoting mangling of the backslash path string. Entered plan mode to pre-commit the five bindings for the mutation leg before touching source; the user rejected the first draft's red-leg binding (an exact `1 failed, 25 passed` count derived from reading source not yet mutated) and required it be rebound to the structural invariants doc 08 §5d actually cares about — `rc≠0`, `collected==26`, `≥1 failed`, and an assertion failure rather than a collection/import error — with the specific count kept only as a predicted-and-checked expectation, not a gate. Executed the approved plan: mutated `src/qc/rules/resolution.py:17` (`MIN_WIDTH_PX` 2000→500), witnessed the red leg (rc=1, `1 failed, 25 passed`, assertion failure on `test_resolution_measure_score_below_minimum`), reverted, witnessed green-after-revert (rc=0, exactly `26 passed`), and confirmed `git status`/`git diff` both empty on the reverted file. Wrote a single VERIFIED-labeled closure entry to `NEXT.md` (Session 16) recording both legs, the three still-OPEN orthogonal gates, and the full precondition roll-up (1 MET, 2 CLOSED, 3 CLOSED, 4 CLOSED, 5 MET). Ran the historian sweep and produced a session handoff. During post-write verification, a user-requested re-check of doc 14 (lines ~700–860) found that Step-3 preflight "Item 0" (composed real-spawn through `ClaudeHeadlessEngine.run()`) had actually already RUN clean-with-caveat twice (2026-07-17 CLI 2.1.212; 2026-07-24 re-probe CLI 2.1.215) — the "still unwitnessed" language that had propagated into NEXT.md, the day file, and the handoff was stale, and in two handoff spots had drifted further into flatly wrong ("NOT TESTED", "proceed to Item 0 next"). Corrected all instances across the three artifacts with one identical canonical text block (caveat-leading), reconciled the live-smoke gating language to name the real remaining gates (the parked vacuity-guard positive-control question and the scratch-vs-real-repo step), and confirmed via grep that no stale or contradictory wording survived. Separately, investigated where a narrow "Item 0 vs ADR-22 re-witness" gate-naming convention note should live (engineering-historian skill is user-global — declined to edit it; capture-rules.md and doc 14 were also considered and declined) but abandoned the note entirely once a doc-14 re-read showed its premise was itself wrong, rather than keep chasing placement. No commit in either repo; the durability harness was correctly skipped (no issue-runtime `src/` logic changed this session).

## Cases

### CASE: bash-tool-shell-quoting-false-negative-vs-real-invocation-surface
status: cited
problem: Running the ADR-23 validation command's exact backslash-path string through the Bash tool (Git Bash) mangled the `\\` sequences, silently rewriting `tests\\test_qc_rules.py` into `teststest_qc_rules.py` and producing a plausible-looking failure (`RC=4`, `collected 0`) that was not a real test-suite result at all — just a different, wrong execution surface than the one `Validator._run_once` actually uses.
decision: Discarded the Bash-tool result entirely rather than reconciling it, and re-ran the exact same command string through `subprocess.run(cmd, cwd=..., shell=True)` invoked via `C:\Python314\python.exe` — matching the Validator's real call shape — which returned the true result (rc=0, `26 passed`). Carried this forward as a binding constraint on the later mutation leg: the Bash tool is inadmissible for either the red or green-after-revert witness, since a broken invocation that happens to return green would be far more dangerous than one that returns a visibly wrong red.
why: The user named the asymmetry directly — a false negative from a bad execution surface is annoying but visible; a false *positive* from the same class of artifact would sail through unnoticed, and the mutation leg's entire purpose (proving a red is real) is defeated if run on a surface that can manufacture spurious reds or greens.
tags: [verification, windows, subprocess, shell-quoting, execution-surface]

#### JUDGMENT (distilled 2026-07-26)
Rejected alternative: treating the Bash-tool RC=4/collected-0 result as a genuine finding to investigate or reconcile with the expected 26-collected witness.
Tradeoff: reconciling the Bash-tool result would have looked more thorough in the moment, but the result was evidence about a different execution surface (Git Bash's own backslash-handling) than the one the runtime actually uses (`subprocess.run(shell=True)` via a specific interpreter); discarding it and re-running on the real surface cost one extra invocation but produced the only witness that means anything for `Validator._run_once`.
Tipping factor: the user's framing that a false negative is the safe-failure direction and a false positive from the same artifact class would be the dangerous one — which made "which surface actually matches production" the deciding question, not "which result do I trust more."

### CASE: mutation-red-leg-bound-to-structural-invariants-not-from-source-count
status: distilled
problem: The first drafted plan for the mutation leg's red-leg binding gated PASS/FAIL on an exact predicted count (`1 failed, 25 passed`) derived by reading the target source and test file before any mutation was made — binding a witness's admissibility criterion to an inference made from unmutated source risks the same failure mode the witness exists to guard against: a valid-but-untraced transitive effect (e.g. a second test coincidentally affected) would register as "inadmissible" even though the red was completely real.
decision: Rebound the red leg to what doc 08 §5d actually requires structurally — `returncode != 0` AND `collected == 26` (proving no collection/import error silently dropped tests) AND `≥1 test failed` AND the failure is an assertion failure, not a collection-time error — and kept the specific `1 failed, 25 passed` / exact-test-and-line prediction as "predicted-and-checked, report whether it matched," explicitly admissible even if the count differed, so long as the structural conditions held.
why: The user drew the distinction directly: gating on a from-source inference before the mutation exists conflates "I predicted this" with "this is what proves the red is real," and the whole point of the mutation leg is to avoid exactly that class of unwitnessed assumption.
tags: [testing, mutation-testing, verification, methodology, adr-23]

#### JUDGMENT (distilled 2026-07-26)
Rejected alternative: gating the red leg's pass/fail on an exact predicted failure count (`1 failed, 25 passed`) derived by reading the unmutated source and test files in advance.
Tradeoff: an exact-count gate is simpler to state and check, but it is itself an unwitnessed inference at binding time — if the real mutation produced a valid-but-untraced transitive effect (a different count, a second affected test), the exact-count gate would falsely register a genuine red as inadmissible; binding to structural invariants (collected==26, ≥1 failed, assertion not collection error) costs a slightly less tidy binding but cannot be defeated by an untraced side effect, while the specific count still gets reported and reconciled as a prediction, not silently dropped.
Tipping factor: the user identified that gating on a from-source-derived count before mutation repeats the exact evidentiary weakness (trusting an inference over a witnessed structural fact) that the mutation leg exists to eliminate.

### CASE: adr23-differential-before-half-structurally-unwitnessable-not-merely-deferred
status: distilled
problem: Recording the ADR-23 end-to-end differential (doc 08 §5d) as an OPEN item after #4's closure risked reading as "deferred, will do later" — but for the specific Phase-2 mechanism change that already landed (Session 15), the differential's "before" half cannot be freshly observed live by any future session, since the pre-Phase-2 code no longer exists in the tree and a `git stash` reconstruction was already ruled categorically inadmissible (simulated, not witnessed) in the session that deferred it.
decision: Wrote the NEXT.md closure entry's OPEN-items list with the sharper framing — the differential is deferred behind its own three-part AND, AND separately, the "before" half is unwitnessable for the already-landed change specifically — rather than leaving it as a flat "still deferred" line that could be misread as fully recoverable by simply finding time for it later.
why: The user confirmed this addition as correct and asked that it be carried forward as a known, permanent limit rather than something a future session goes hunting to fix — the same category of finding as ADR-22's permanently-INFERRED root-cause claim (some evidence gaps are structural, not scheduling).
tags: [adr-23, verification, differential-testing, documentation, provenance]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: leaving the differential's NEXT.md entry as a flat "still deferred" line.
Tradeoff: a flat "still deferred" line is simpler to write, but could be misread as fully recoverable by simply finding time for it later; the sharper framing (deferred behind a three-part AND, and the before-half specifically unwitnessable for the already-landed change) costs more words but distinguishes a scheduling gap from a permanent structural limit.
Tipping factor: the user confirmed this addition as correct and asked it be carried forward as a known, permanent limit rather than something a future session goes hunting to fix — the same category as ADR-22's permanently-INFERRED root-cause claim.

### CASE: item0-stale-unwitnessed-claim-corrected-with-identical-canonical-text-not-reauthored
status: distilled
problem: After the Session 16 closure entry, handoff, and day file were written, a user-requested re-check of doc 14 (~lines 700–860) found that Step-3 preflight "Item 0" (composed real-spawn through `ClaudeHeadlessEngine.run()`) had actually already RUN clean-with-caveat twice (2026-07-17 CLI 2.1.212; 2026-07-24 re-probe CLI 2.1.215) — but the three artifacts variously said "not witnessed today" (narrow, originally accurate), "still unwitnessed" (drifted to an absolute false claim), or, in two handoff spots, flatly "NOT TESTED" / "proceed to Item 0 next," directly contradicting doc 14's own source-of-truth record.
decision: Instead of re-authoring a per-artifact explanation for each of the three files, wrote one identical canonical text block (caveat-leading: the unresolved vacuity-guard and scratch-vs-real-repo gaps stated first, the two clean composed runs stated second) and pasted it verbatim into NEXT.md, the day file, and the handoff, then reconciled NEXT.md's live-smoke gating line to name the real remaining gates instead of "run Item 0." Confirmed via a grep sweep across all three files that no stale ("unwitnessed", "NOT TESTED", "proceed to Item 0") or self-contradictory wording survived.
why: The user stated explicitly that "per-file re-authoring is what caused the divergence originally" — the three artifacts had drifted apart precisely because each was hand-worded separately from a shared fact, so the fix mandated identical text rather than three independently-phrased corrections, and ordered the caveat first so that any future truncation of the block degrades toward caution rather than false reassurance.
tags: [documentation, consistency, verification, provenance, multi-artifact]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: re-authoring a separate, independently-worded explanation for each of the three drifted artifacts (NEXT.md, the day file, the handoff).
Tradeoff: per-artifact re-authoring lets each explanation fit its own context, but that is exactly the process that caused the three artifacts to drift apart in the first place; one identical canonical text block pasted into all three costs less tailoring but guarantees the three stay consistent.
Tipping factor: the user stated explicitly that per-file re-authoring is what caused the divergence originally, so the fix mandated identical text rather than three independently-phrased corrections.

### CASE: gate-naming-convention-note-abandoned-after-premise-found-wrong-in-source-doc
status: distilled
problem: A proposed durable note distinguishing Step-3 preflight "Item 0" from the ADR-22 argv/contamination re-witness as "distinct, orthogonal gates" needed a home — the engineering-historian skill was found to be user-global (editing it would leak an issue-runtime-specific example into every project), `knowledge/capture-rules.md` was found to have a narrow existing consumer contract (sweep-decline rules read by `/distill`) that a `## Conventions` section would stretch, and CLAUDE.md / doc 14's dated-correction-note pattern were named as the next candidate placements.
decision: Before placing the note anywhere, re-read doc 14's actual Item 0 definition and found the note's core premise was wrong — doc 14 defines Item 0 as itself being the composed real-spawn of the ADR-22 argv/contamination mechanism, not a distinct orthogonal gate from it — so the note was abandoned entirely rather than relocated a third time, on the grounds that the relocation effort had already exceeded what the original one-line slip could ever cost.
why: The user stated it directly: "the effort chasing its placement has already exceeded what the original slip could cost, and it's not load-bearing" — each proposed location (global skill, capture-rules.md, CLAUDE.md/doc 14) was declined in turn for scope-appropriateness before the premise itself was found false, at which point continuing to search for a home for a wrong claim was recognized as strictly worse than dropping it.
tags: [documentation, scope-discipline, methodology, verification]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: relocating the gate-naming convention note a third time to another candidate home (CLAUDE.md or doc 14's dated-correction-note pattern) after the global skill and capture-rules.md were both declined.
Tradeoff: relocating again might eventually find the note a home, but doing so before checking whether the note's core premise was even true would spend more effort chasing placement for a claim that might be wrong; re-reading doc 14's actual Item 0 definition first cost one more read but avoided placing a false premise anywhere.
Tipping factor: doc 14 was found to define Item 0 as itself being the composed real-spawn of the ADR-22 mechanism, not a distinct orthogonal gate — the note's core premise was wrong, making continued placement-searching strictly worse than dropping it.

## Commands that worked
- `subprocess.run(cmd, cwd=r"C:\Projects\StockPhotoAgent", shell=True)` invoked via `C:\Python314\python.exe`, command string parsed verbatim from `config.yaml` — the exact `Validator._run_once` call shape, used for all four witnessed runs this session (collected>0 re-confirm, red leg, green-after-revert, plus the earlier session's original collected>0 run).
- `git status --short -- src/qc/rules/resolution.py` / `git diff -- src/qc/rules/resolution.py` — both empty after revert, independent confirmation alongside the re-witnessed `26 passed`.

## Problems solved
- Bash-tool invocation of the backslash-path validation command silently mangled the path string, producing a plausible false-negative result (`RC=4`, `collected 0`) → root cause: Git Bash's handling of `\\` escape sequences differs from the Windows `shell=True` surface the runtime actually uses → discarded the result and re-witnessed via `subprocess.run(shell=True)` from the correct interpreter.
- Needed a mutation target for #4's non-vacuity requirement that would fail deterministically and locally, without risking a collection-time error that could be confused with the Bash-tool artifact → chose `ResolutionRule.MIN_WIDTH_PX` (a numeric QC threshold, not a syntax/import change), traced its effect on both `test_qc_rules.py` tests before mutating, and confirmed the prediction matched exactly once run.
- NEXT.md, the day file, and the handoff all described Step-3 preflight Item 0 as "unwitnessed"/"NOT TESTED" → root cause: the phrase drifted from an originally-accurate narrow claim ("not witnessed *today*") into an absolute one during independent per-file authoring, contradicting doc 14's record of two prior clean composed runs → re-verified against doc 14 directly and corrected all three artifacts with one identical canonical text block.

## Mistakes
- First drafted the mutation-leg plan with an exact-count red-leg gate derived from reading unmutated source — caught by the user before execution and rebound to structural invariants; no execution happened against the flawed binding.
- The day-file and handoff status language drifted from the originally-accurate "not witnessed today" into an absolute "still/remains unwitnessed," and two handoff spots went further into flatly wrong ("NOT TESTED", "proceed to Item 0 next") — caught by a user-requested doc 14 re-check, corrected with identical canonical text across all three artifacts.
- Proposed adding a gate-naming convention note whose premise (Item 0 and the ADR-22 re-witness are distinct, orthogonal gates) was itself wrong per doc 14 — caught before any write happened, note abandoned rather than corrected and placed.

## Prompts worth keeping
"That count is derived from reading source you haven't mutated yet — binding a pass/fail gate to a from-source inference is the failure mode we avoid." — reframed the red-leg binding from an exact predicted count to the structural conditions doc 08 §5d actually requires, before any mutation was applied.
"Per-file re-authoring is what caused the divergence originally." — the instruction that mandated pasting one identical canonical text block into all three artifacts instead of independently rewording each correction.

## Questions I still have
None new this session.

## TODOs for next session
- Item 0 (composed real-spawn through ClaudeHeadlessEngine.run()): RUN, clean-with-caveat — not unqualified pass. Still open: (a) the vacuity-guard positive control has never confirmed detectability (three independent non-reproductions per doc 14); (b) every Item 0 run so far used a scratch workspace — live smoke would be the first run against a real target repo (doc 14's own note).
- Standing tickle: doc 14 §2.4 Probe 2/3 two-leg re-probes at CLI 2.1.214 — untouched, still owed whenever the CLI version is re-checked.
- ADR-23 end-to-end differential — remains deferred behind its three-part AND; the "before" half is a permanent structural limit for the already-landed Phase-2 change, not a scheduling gap, per this session's sharpened framing.
- The real gate in front of live smoke is not "run Item 0" (already run) but resolving the parked vacuity-guard question and the scratch-vs-real-repo step — next session should treat these as the actual precondition, not re-litigate whether Item 0 needs running.

## Session 2 (NEXT.md audit and restructure, commits 2f232e7 then 0f3dee8)

An eight-phase adversarial audit (Phase 0 through 1.9) of NEXT.md preceded a collapsed Phase 2 migration, landed as commit `2f232e7` (938→232 lines, non-KEEP content archived to `docs/handoffs/next-md-archive-2026-07-26.md`, 3 carried-unwitnessed surfaces homed to a new doc14 EOF section). A follow-up targeted pass then found and fixed three residual issues in the committed rewrite — two exceptions framed as "by design" that were actually missed pre-committed targets, two approximate ("~") pointer ranges (one materially wrong), and a contradiction between an action item and a standing tickle describing the same owed work — landing as commit `0f3dee8`.

## Cases

### CASE: audit-rigor-must-scale-to-blast-radius-not-apply-uniformly
status: distilled
problem: A routine NEXT.md cleanup (938 lines, stale narrative accumulated across 17 sessions, headed for a trim + archive) was run through a seven-phase adversarial audit — positive claim roll-calls against every discrete sentence, 4x4 redundancy/destination legality matrices, pre-committed outcome matrices, chronology tests via git commit-hash cross-reference — the same discipline this project's docs use for transitions, billing invariants, commit ordering, and engine-fence behavior, where a wrong label is a live correctness failure.
decision: Completed the full seven-phase audit as directed and executed the resulting plan (archive file, doc14 append, NEXT.md rewrite, commit), but flagged for the record that of the whole effort, only ~15 lines of migration (3 carried-unwitnessed surfaces with no durable home outside NEXT.md) actually required that rigor — the roll-call round's own findings were repeatedly "this condensed summary is not byte-identical to its source phrasing," which the underlying artifact (a git-backed, self-declared non-authoritative working queue) was never meant to guarantee.
why: The check's own output was the tell — a check whose finding is "the summary doesn't match its source word-for-word" is testing whether a working queue is lossless against its sources, which is the wrong property to demand of a working queue in the first place; the worst realistic failure mode here was a recoverable lost line in a git-tracked file, not a live transition or billing defect.
tags: [methodology, verification, scope-calibration, documentation, audit-design]
until: re-evaluate if a future NEXT.md-class cleanup again escalates past one careful pass without an explicit reason tied to blast radius.

#### JUDGMENT (distilled 2026-07-26)
Rejected alternative: applying the same five/seven-gate discipline (positive roll-calls, legality matrices, chronology tests) uniformly to every documentation or queue-file cleanup, on the grounds that the discipline is available and has worked before.
Tradeoff: uniform maximum rigor is simpler to invoke (no judgment call about which discipline level applies) and leaves no possible gap unchecked, but it is not free — this session's roll-call round manufactured close to 100 lines of frozen-doc migration work for facts git already preserved untouched, and came close to freezing live-queue shorthand (hedges, cross-references, a status tag like "RUN/CLEAN-WITH-CAVEAT") into a permanent design document, which would have been a net loss of expressiveness for no correctness gain.
Tipping factor: the artifact's own blast radius — a git-backed, explicitly non-authoritative pointer file where the worst-case wrong outcome is one recoverable lost line — versus the blast radius the discipline was built for (a wrong evidence label in a live transition, billing, or fence-behavior claim, where "recoverable from git" is not the applicable safety net).

### CASE: destructive-reclassification-raises-its-own-verification-bar
status: distilled
problem: During Phase 1.8's delta-check, 8 of 10 previously-unaudited DUPLICATE-DEGRADED rows showed "zero identifiable delta" against doc14 and were reclassified DUPLICATE-VERIFIED — a status whose destination is DELETE (destroying the only NEXT.md copy), versus DEGRADED's MIGRATE-TO (which preserves a copy in the archive). The reclassification was accepted on a "no delta found" search, which the user pointed out is a search that can miss, not a proof of completeness — a materially weaker standard than the one the new destination (irreversible deletion) demands.
decision: Ran a full positive claim roll-call (Phase 1.9) on all 8 reclassified rows — every discrete claim, not just suspected gaps, each anchor-grepped against doc14 with the target line number pasted. 5 of the 8 failed outright (claims with zero hits anywhere in doc14, including a literal tag, "RUN/CLEAN-WITH-CAVEAT," that doc14 simply phrases differently) and were reverted to DUPLICATE-DEGRADED/MIGRATE-TO; the other 3 passed only after their citations were widened to true section boundaries. The rule was carried forward explicitly: a reclassification that increases destructiveness (DEGRADED→VERIFIED, i.e. MIGRATE→DELETE) must clear a stricter bar than the check that produced it, not inherit that check's pass.
why: The user stated it directly — the flip raises the bar these rows must clear, and they cleared it on a "no delta found" result, which is a search that can miss, not a proof of completeness; a prior spot-check has no standing to pass a row into permanent deletion.
tags: [methodology, verification, destructive-operations, audit-design, documentation]

#### JUDGMENT (distilled 2026-07-26)
Rejected alternative: accepting the Phase 1.8 "no delta found" result as sufficient grounds to move rows from DEGRADED (copy preserved via MIGRATE-TO) to VERIFIED (copy destroyed via DELETE).
Tradeoff: accepting the shallower check would have closed the audit one phase earlier, but a negative search result ("I looked and found no missing claim") is not equivalent to a positive one ("I confirmed every claim has a hit") — the shallow check had already misclassified all 8 rows once (as DEGRADED, without verification) before the deeper check reclassified 5 of them again; trusting it a second time, now backing an irreversible action, would have compounded the same unverified assumption into permanent data loss.
Tipping factor: the asymmetry between the two destinations the classification controls — MIGRATE-TO is recoverable (archive keeps a copy), DELETE is not — meant the verification standard had to scale with what the classification was about to authorize, not stay fixed at whatever check happened to produce the current label.

### CASE: pointer-file-should-point-at-not-carry-duplicated-detail
status: distilled
problem: Phase 2's scope correction superseded a 281-line MIGRATE-TO list computed across Phases 1.6-1.9: rather than migrating rows 15/16's dropped specifics (exact test/function names, `main.py` call-site line numbers) into `docs/08` §5d as a bundled append, the corrected instruction sent them to the archive only, leaving `docs/08` untouched, while doc14 received a single narrow EOF section limited to the 3 genuinely homeless ("ONLY") surfaces.
decision: Implemented the scope correction as given — docs/08 receives nothing, the 281-line migrate list mostly resolves to ARCHIVE instead, and doc14's append is scoped to exactly the 3 must-survive items with no evidence label added beyond what the sources already carried.
why: The user's stated reasoning is that rows 15/16's dropped specifics are "detail the pointer file should point AT, not carry" — a rewritten NEXT.md is meant to be a pointer file, so recoverable implementation detail belongs in the archive it can point to, not duplicated forward into a second frozen doc, whereas the 3 ONLY items get the doc14 append precisely because no other doc holds them at all.
why: n/a
tags: [documentation, pointer-design, scope-discipline, methodology]

#### JUDGMENT (distilled 2026-07-31)
thin: no rejected alternative named in the record — the case describes implementing a scope correction the user already handed down, not a decision weighed against a real alternative in this session.
Tradeoff: not recoverable from the record beyond the stated reasoning: duplicating recoverable detail forward into a second frozen doc costs ongoing double-maintenance, while archiving it and letting the pointer file point at the archive keeps detail in exactly one place.
Tipping factor: the pointer file is meant to point AT detail, not carry it — recoverable implementation detail belongs in the archive it can point to, reserving direct doc14 appends for the genuinely homeless items no other doc holds at all.

## Commands that worked
- `git ls-files --error-unmatch <path>` / `git log -1 --oneline -- <path>` — used per must-survive item during the Phase 1.7 homing challenge to force a real citation instead of an asserted "already tracked" claim.
- `git status --porcelain` run before and after `git add NEXT.md`, pasted raw both times — the gating check used for both commits (`2f232e7`, `0f3dee8`) to confirm only the intended file was staged, with the two pre-existing `knowledge/.sweep/*` logs correctly left out of both.
- `grep -n "by design" NEXT.md` → zero hits, used to verify the Fix 1 honesty relabel actually removed the framing it was meant to remove.

## Problems solved
- The §2.6 vacuity-background pointer's range was a rough guess (`~L570-670`) at a sub-paragraph rather than the section's real boundaries → corrected to the section's true extent (`L542-890`) after checking doc14's actual heading/separator structure, and anchor-confirmed at L576.
- Two exceptions written into NEXT.md's header/footer ("by design") reframed a missed pre-committed target (zero evidence-labels; ≤120-line cap) as though it had been the plan from the start → relabeled both as explicit amendment-after-miss notes, stating the original target, that it was not met, and why the rule was judged wrong for a live gate-status table.
- Action-3 and the standing CLI-2.1.214 tickle described the same owed re-probe work under two different, contradictory trigger conditions → reconciled to one trigger, one source of truth in §4.

## Mistakes
- The Phase 1.8 delta-check reclassified 8 rows from DEGRADED to VERIFIED (MIGRATE→DELETE) on a "no delta found" search rather than a positive claim-by-claim check; caught one phase later (1.9) when the exhaustive roll-call found 5 of the 8 had claims with zero hits in doc14, including a literal status tag doc14 doesn't use — reverted before Phase 2 executed, no data lost.
- The Phase 2 write initially omitted an entire 98-line block (Session 16 + Session 17 Dry-run A content) from the archive file and dropped one section heading; both caught and fixed via byte-for-byte diff against the original before the commit landed.
- The first NEXT.md rewrite (`2f232e7`) framed two missed pre-committed targets (line cap, zero-evidence-label rule) as intentional "by design" exceptions rather than disclosing they were amended after the target was missed; caught in a separate follow-up review and corrected in `0f3dee8`.

## Prompts worth keeping
"Two histograms can both balance while UNIQUE content is being deleted — this is the exact failure mode the per-line-range rule exists to catch, and it is not yet caught." — the Phase 1.8 instruction that forced a 4×4 cross-tabulation instead of trusting two independently-summing marginal totals, which is what surfaced the row 20/29 delta-coverage gap.
"A search that can miss, not a proof of completeness." — the framing that forced the Phase 1.9 exhaustive positive roll-call instead of accepting the prior "no delta found" result as sufficient grounds for an irreversible reclassification.

## Questions I still have
None new this session.

## TODOs for next session
- §3's precondition table (NEXT.md, 232→254 lines post-audit) is the sole source of the line-cap overage; rotation trigger is firing — next session either shrinks it as preconditions close or graduates it to its own tracking doc.
- Standing tickle: doc14 §2.4 Probe 2/3 re-probes at CLI 2.1.214 — single reconciled trigger now lives in NEXT.md §4 only; no other copy to keep in sync.

Also noted (not drafted): Phase 2's `git diff` byte-verification of the 693-line archive file against the original NEXT.md, and the doc14/docs08 append-scope walk-down across Phases 1.6–1.9 (zero appends → 1 → 2 → 4 sub-notes → collapsed to 1 doc14 section only) — both are process detail behind the two cases above, not independent decisions.

## Session 3 (CLAUDE.md tiering codification + knowledge/ vault eviction, commits e049aa3 then 70cd4c9)

A read-only audit of CLAUDE.md (outline, section coverage check) preceded a single dictated insertion codifying the process-depth-scales-to-blast-radius correction from Session 2 into the Hard rules section, committed alone as `e049aa3` after a scope-check caught `knowledge/.sweep/sweep.log` sitting mixed in the working tree. Adi then hand-edited `.gitignore` to add a `knowledge` line and directed the historian experiment vault to be untracked; the request escalated in scope twice (sweep.log only → all three `.sweep/` files → the entire `knowledge/` tree) as tracked-file scope checks at each step revealed more than the prior step assumed, landing as `70cd4c9`.

## Cases

### CASE: knowledge-vault-eviction-scope-widened-per-file-then-whole-tree-after-tracked-file-checks
status: distilled
problem: Adi's untrack request for the historian experiment vault started narrow — untrack `knowledge/.sweep/sweep.log` alone, since that was the file believed to need a `.gitignore` entry — but a `git ls-files knowledge/.sweep/` scope check found three tracked files there (`pending.log`, `sessions.log`, `sweep.log`), not one, which was reported back rather than acted on. Adi then redefined the eviction target as the entire `knowledge/` tree (not just `.sweep/`), reasoning that the historian vault is a separate experiment, not project state.
decision: Ran a second scope check (`git ls-files knowledge/`) against the wider target before untracking anything, confirmed the full tracked list was historian/experiment output (capture-rules.md, dated case files, principles.md, sweep logs) with nothing that looked like runtime source, config, or docs referenced by CLAUDE.md's frozen-contract set, then executed `git rm -r --cached knowledge/` for the whole tree in one step rather than untracking incrementally file-by-file, and confirmed the `.gitignore` entry (`knowledge`) matched the tree actually evicted.
why: Untracking only `sweep.log` per the original request would have left `pending.log` and `sessions.log` tracked while the `.gitignore` entry Adi was adding covered the whole directory — a mismatch between what gets ignored going forward and what's actually still tracked; escalating to a full-tree scope check before acting avoided leaving a partial, inconsistent untrack in place.
tags: [git, scope-discipline, verification, gitignore, historian]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: untracking only `knowledge/.sweep/sweep.log`, per the original narrow request.
Tradeoff: untracking only `sweep.log` matches the literal original request and is the smallest change, but would have left `pending.log` and `sessions.log` still tracked while the new `.gitignore` entry covered the whole `knowledge/` directory — a mismatch between what's ignored going forward and what's actually still tracked; a full-tree scope check before acting costs more verification but avoids a partial, inconsistent untrack.
Tipping factor: `git ls-files` at each candidate scope surfaced more tracked files than the request assumed at every step, so the target had to be redefined based on what was actually there rather than what was originally described.

## Commands that worked
- `git ls-files knowledge/.sweep/` — surfaced 3 tracked files (not the assumed 1), used to halt before a narrower `git rm --cached` would have left two of them still tracked.
- `git ls-files knowledge/` — full tracked-file scope check before the whole-tree eviction, confirmed nothing project-critical (runtime source, config, frozen-contract docs) was present.
- `git rm -r --cached knowledge/` — untracked all 8 files without deleting from disk; `git status --short` confirmed all 8 as staged `D` and files still present via `ls knowledge/`.
- `git add .gitignore` staged alongside the pre-staged `knowledge/` deletions, then `git commit -m "chore: stop tracking historian experiment vault (knowledge/); add to gitignore"` — commit `70cd4c9`, confirmed via `git show --stat HEAD` as exactly `.gitignore` + 8 `knowledge/` deletions, no src/docs/config touched.
- `git add CLAUDE.md` (explicit path, leaving `knowledge/.sweep/sweep.log` unstaged) → `git commit -m "docs: tier process depth to blast radius (correction to uniform gate application)"` — commit `e049aa3`, confirmed via `git show --stat HEAD` as exactly `CLAUDE.md`, 15 insertions, 0 deletions.

## Problems solved
- The untrack request's stated scope (`sweep.log`) didn't match what was actually tracked under `.sweep/` (3 files) or what the `.gitignore` entry was about to cover (the whole `knowledge/` directory) → root cause: the request was framed before the actual tracked-file set was known → resolved by running `git ls-files` at each candidate scope before acting, which surfaced the mismatch early enough to redefine the target correctly instead of doing a partial untrack.
- `knowledge/.sweep/sweep.log`'s working-tree content-modification was mixed into the staging area alongside the intended CLAUDE.md commit → caught via `git status --short` before staging, CLAUDE.md added by explicit path only, sweep.log left unstaged and later folded cleanly into the separate `knowledge/` removal commit rather than appearing as a stray change in either commit.

## Mistakes
None this session — both commits were scope-checked (`git status`/`git ls-files`) before staging and verified (`git show --stat`) after committing, with no unexpected files in either.

## Prompts worth keeping
None this session — the CLAUDE.md insertion text was dictated verbatim by the user rather than drafted; the value was in the verification sequence (diff, line numbers, scope checks), not prompt phrasing.

## Questions I still have
None new this session.

## TODOs for next session
None stated.

## Session 4 (ADR-22 literal Probe 2/3 re-probe at CLI 2.1.220, NEXT.md §2 sync, session-18 handoff; commits ab99f55, aa75231, 717865c)

Opened with a read-only orientation read (git chain, NEXT.md §1/§2/§5, Session-17 handoff, CLAUDE.md hard rules, pointer spot-checks, CLI version) confirming the prior session's commits were correctly in HEAD's ancestry and doc14 pointers resolved. A read-only spec pull then surfaced that NEXT.md §4's "last re-probe 2.1.215" note referred to a different methodology than the literal doc14 §2.4 Probe 2/3 procedure, prompting a pre-committed-matrix plan and execution of the literal probes at CLI 2.1.220 (both legs PASS), a light-tier doc14/NEXT.md update recording the result (commit ab99f55), a follow-up §2 staleness fix synced to that closure (commit aa75231), and a session handoff (commit 717865c). A final user report of a "duplicated item 3" in NEXT.md §2 was investigated and found not to exist in the committed file.

## Cases

### CASE: literal-probe-reprobe-required-synthetic-marker-substitution-rejected
status: distilled
problem: NEXT.md §4 recorded the ADR-22 B-layer sunset tickle as last re-probed at CLI 2.1.215, but pulling the actual spec showed the 2.1.215 run (doc14 §2.7) used the Session-11 synthetic-marker methodology (Leg B / Synth B / Synth C), not the literal doc14 §2.4 Probe 2 (empty `--setting-sources` clean-run) and Probe 3 (fence-sanity-under-git-init) procedures — meaning the literal probe pair had gone unwitnessed since CLI 2.1.211, across nine minor CLI versions in which fence and tool-removal behavior had silently shifted before.
decision: Built and ran the literal §2.4 Probe 2/3 procedure itself at CLI 2.1.220 — fresh scratchpad dirs, trivial no-tool prompt, subscription auth, full disallowedTools fence, `--setting-sources ""` — with both legs pre-committed to outcome matrices before execution and witnessed via raw output only (transcript JSON, permission_denials grep, `.git` presence checks), not engine self-report. Both legs landed PASS.
why: The user stated directly that a synthetic-marker substitution "closes nothing" — re-running anything other than the literal named procedure would not answer the question NEXT.md §4 actually poses, and both legs were treated as high-blast-radius (fence/suppression safety claims) requiring the pre-committed-matrix discipline rather than a lighter check.
tags: [adr-22, verification, cli-versioning, probe-methodology, safety]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: crediting the 2.1.215 synthetic-marker methodology run as having already satisfied the standing tickle's re-probe requirement.
Tradeoff: crediting the existing 2.1.215 run costs nothing further and the tickle would read as satisfied, but a synthetic-marker substitution answers a related-but-distinct question, not the literal doc14 §2.4 Probe 2/3 procedure the tickle actually names; running the literal probes fresh costs a full pre-committed-matrix pass but actually closes the gap the tickle names.
Tipping factor: the user stated directly that a synthetic-marker substitution "closes nothing" — re-running anything other than the literal named procedure would not answer the question the tickle actually poses.

### CASE: green-reprobe-does-not-auto-license-b-layer-sunset
status: distilled
problem: Both re-probe legs PASSed cleanly at CLI 2.1.220 (Probe 2: rc=0, `apiKeySource=none`, no `knowledge/` across 16 ticks to 450s; Probe 3: git init attempted-not-self-censored, both deny signals present, `.git` absent) — a result that satisfies the literal condition the B-layer sunset tickle names ("A-empty has survived one clean CLI-upgrade cycle with the probes re-run green").
decision: Did not sunset B or touch `config.yaml`/`docs/08` §5c despite the clean PASS; recorded the result as closing only the literal Probe 2/3 re-witness gap, explicitly re-stated hold-B as still in force, and re-armed the tickle for the next CLI bump past 2.1.220 rather than treating the PASS as self-executing.
why: The user's instruction stated explicitly, both in the probe-plan framing and in the doc14/NEXT.md update text, that a passing matrix "does NOT license B-layer sunset" — that decision "stays explicitly the user's to make, each time, not an automatic consequence of a passing matrix."
tags: [adr-22, policy, verification, safety, sunset-criteria]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: treating the clean PASS on both re-probe legs as satisfying the B-layer sunset tickle's literal condition and proceeding to sunset B.
Tradeoff: treating a clean pass as self-executing would close the tickle in the same session, but the sunset criteria state explicitly that a passing matrix does not license the sunset by itself; holding B and re-arming the tickle costs leaving extra config in place but keeps the sunset decision with the user, every time, not automated.
Tipping factor: the user's instruction stated explicitly, in both the probe-plan framing and the doc14/NEXT.md update text, that a passing matrix does not license B-layer sunset — that decision stays explicitly the user's to make each time.

### CASE: next-md-staleness-fixed-before-handoff-not-after
status: distilled
problem: A session-close check found NEXT.md §2 items 3 and 6 still worded as owed/pending work (close the CLI-2.1.214 coverage gap; re-run the ADR-22 probes on next CLI bump) even though both had just been closed and committed via §4/doc14 §2.8 in the same session (commit ab99f55) — and the session handoff had not yet been generated.
decision: Fixed and committed the §2 staleness first (commit aa75231, items 3 and 6 marked CLOSED with pointers to §4/doc14 §2.8, items 1/2/4/5/7 and §1 left untouched), and only then generated the session-18 handoff (commit 717865c), rather than generating the handoff before the fix.
why: The user reasoned that a handoff snapshots NEXT.md's state as-is — generating it before the fix would capture items 3/6 as owed work, and a future session reading the handoff would re-derive and re-run a probe that was already committed done at ab99f55; fixing first means the handoff snapshots a consistent tree.
tags: [documentation, handoff, sequencing, next-md, methodology]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: generating the session-18 handoff first, in the order the session naturally reached it, then fixing NEXT.md §2's staleness afterward.
Tradeoff: generating the handoff first matches the order work was actually finished in, but a handoff snapshots NEXT.md's state as-is — generating it before the fix would capture items 3/6 as still-owed work, misdirecting a future session into re-running an already-committed probe; fixing §2 first costs reordering the session's natural closing sequence but ensures the handoff snapshots a consistent tree.
Tipping factor: the user reasoned that a handoff snapshots NEXT.md's state, so generating it before the fix would misdirect a future session into re-deriving and re-running work already committed done.

### CASE: reported-duplicate-verified-against-file-before-acting-found-not-to-exist
status: cited
problem: The user reported a duplicated NEXT.md §2 item-3 block — a stale "owed" version (pointing to §4 for the trigger) allegedly sitting alongside the new CLOSED version — and asked for it to be viewed, then the stale block deleted, then diffed, then committed as a session-close checkpoint.
decision: Before deleting anything, viewed the actual current §2 item-3 region with line numbers and grepped the whole file for both candidate phrasings ("CLI-2.1.214 Probe 2/3 coverage gap", "precondition and trigger condition"); each phrase appeared exactly once, both inside the single CLOSED block. Cross-checked `git status --short` and `git diff NEXT.md` against HEAD (`aa75231`), both empty, confirming the committed file already had only one item 3. Made no edit, no diff, no commit, and reported that the described duplication did not exist in the file on disk — suggesting the user's view was likely a stale or unsaved editor buffer.
why: The instructed destructive action (delete the stale block) was executed only after directly viewing and grepping the file and confirming via git status/diff that the working tree matched HEAD — not by trusting the user's description of the state at face value, since acting on a mistaken premise would have produced an unwanted edit to an already-correct file.
tags: [verification, git, methodology, documentation, next-md]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: trusting the user's description of a duplicated §2 item-3 block at face value and proceeding directly to view-then-delete-then-diff-then-commit as instructed.
Tradeoff: acting directly on the reported duplicate would follow the instructed sequence faster, but the instructed destructive step assumed a premise (the duplicate exists) that had not been independently confirmed against the actual file; viewing and grepping the file first costs one extra verification round but avoids an unwanted edit if the premise turns out to be false.
Tipping factor: directly viewing and grepping the file, plus checking `git status`/`git diff` against HEAD (both empty), confirmed the working tree already matched HEAD with only one item 3 — acting on the description at face value would have produced an edit to an already-correct file.

## Commands that worked
- `git merge-base --is-ancestor <commit> HEAD` — confirmed e049aa3, 70cd4c9, and 0f3dee8 were all in HEAD's ancestry during the orientation read.
- `git status --ignored --porcelain -- knowledge/` → `!! knowledge/` — confirmed the prior session's eviction still holds (ignored, not tracked/dirty).
- `claude --version` → `2.1.220 (Claude Code)` — confirmed the CLI version triggering the standing re-probe tickle.
- Probe 2 invocation (empty `--setting-sources`, scratch cwd, no-tool prompt, subscription auth) — rc=0, `apiKeySource: "none"`, `knowledge/` absent across all 16 ticks to 450s.
- Probe 3 invocation (fence sanity, git init prompt) — rc=0, git init attempted and denied (`permission_denials` entry + `tool_result is_error:true` with matching `tool_use_id`), `.git` absent by direct `ls`.
- `git status --short` before and after `git add NEXT.md docs/14-session6-phase2-gate.md` (explicit paths), then `git commit` — used to gate commit ab99f55; `git show --stat HEAD` after each of the three commits (ab99f55, aa75231, 717865c) confirmed exactly the intended files, no src/schema/config/transition file in any.
- `grep` for both candidate item-3 phrasings across NEXT.md, plus `git status --short` / `git diff NEXT.md` against HEAD — used to disprove the user-reported duplicate before taking any destructive action.

## Problems solved
- NEXT.md §4 credited the 2.1.215 run as satisfying the ADR-22 standing tickle, but that run used a different (synthetic-marker) methodology than the literal §2.4 Probe 2/3 procedure the tickle actually names → root cause: the 2.1.215 session substituted a related-but-distinct methodology and the substitution wasn't flagged in NEXT.md's wording → resolved by pulling the raw doc14 spec, confirming the identity gap, and re-running the literal procedure at 2.1.220 with both legs witnessed raw and PASS.
- NEXT.md §2 items 3 and 6 read as owed work immediately after the underlying gap they described was closed and committed via §4 → root cause: only §4 was edited in the closing commit, §2 was not synced in the same step → resolved via a dedicated §2-only sync commit (aa75231), sequenced before the handoff so the handoff snapshot would be consistent.
- User reported a duplicated §2 item-3 block that, on direct inspection and grep, did not exist in the committed file → root cause not fully determined from the transcript, but ruled out as a working-tree/commit-state issue via clean `git status`/`git diff`; most likely a stale or unsaved editor buffer on the user's side.

## Mistakes
None this session on the assistant's side — the reported duplicate was investigated and found to be a false premise before any edit was made; no incorrect commit occurred.

## Prompts worth keeping
"Both are high-blast-radius (fence/suppression safety claims), so both get pre-committed outcome matrices, both-halves framing, and no engine self-report — witnessed raw output only. The matrix has to be written before execution so there's no post-hoc rationalization." — set the evidentiary bar for the ADR-22 literal re-probe before any command was run.
"A handoff snapshots NEXT.md's state. If you generate it now, it captures §2 items 3 and 6 as owed work, and the next session re-derives a probe that's already committed done at ab99f55. Fix §2 first, then the handoff snapshots a consistent tree." — the reasoning that fixed the ordering of the §2 sync relative to the handoff generation.

## Questions I still have
Why the user's editor appeared to show a duplicated item-3 block when the committed file and working tree both had only one — not resolved in this transcript (client-side/IDE-buffer question, outside repo state).

## TODOs for next session
- NEXT.md §2 items 1 and 2 (live-smoke sequencing decision; ingest branch-check gap decision) remain the real next work — both decision-only, no precondition blocking them.
- ADR-22 tickle re-armed for the next CLI bump past 2.1.220; hold-B remains standing instruction until the user separately authorizes sunset.
- §1 still correctly reads live smoke as NOT authorized; unchanged by this session's probe work.

## Session 5 (light-tier pointer fix + no-commit hard rule, commits 931ba5d then 3e77a74)

Opened with a read-only orientation read confirming HEAD at 717865c, all prior commits in-ancestry in order, clean tree, and re-quoting NEXT.md §1/§2/§5, the session-18 handoff, and CLAUDE.md's Hard rules verbatim. Two dictated light-tier edits followed: re-confirming NEXT.md §1's stale doc14 pointer via a fresh `findstr` (not the orientation read's cached value) and adding the "no commit without explicit authorization" bullet to CLAUDE.md verbatim, both staged/diffed/returned raw but held uncommitted per instruction. The user then authorized and reviewed the commit (931ba5d), followed by a session-19 handoff and its own separate commit (3e77a74).

## Cases
None. This session executed two edits and their exact wording, sequencing, and commit boundaries under direct, specific dictation from the user (verbatim bullet text, explicit anchor line to use, explicit stage-only-these-files scope); no alternative was weighed or discussed in the record beyond what was already established and captured in prior sessions' cases (raw-artifact discipline, verify-via-live-command-not-cached-value, light-tier scope checks before staging). Nothing here clears G2 as a new decision.

## Commands that worked
- `findstr /n /c:"Carried-forward note (Session 16-17" docs\14-session6-phase2-gate.md` → `1129:### Carried-forward note (Session 16-17, 2026-07-26) — surfaces named UNWITNESSED ahead of live smoke` — used to re-derive the pointer target live rather than reuse the orientation read's cached line number.
- `git diff` (both files) and `git status --porcelain=v1` — returned raw pre-commit to confirm exactly the intended two-line change set (CLAUDE.md +1 bullet; NEXT.md pointer line range→single anchor) before any commit was authorized.
- `git add CLAUDE.md NEXT.md` (explicit paths) → `git commit -m "docs: fix stale NEXT.md §1 pointer (L1129) and add no-commit-without-authorization hard rule"` — commit 931ba5d; `git show --stat HEAD` confirmed exactly two files, +2/−1 lines total.
- `git add docs/handoffs/HANDOFF_2026-07-26_session19-pointer-fix-and-no-commit-rule.md` → `git commit -m "docs: session 19 handoff — NEXT.md §1 pointer fix + no-commit hard rule"` — commit 3e77a74; `git show --stat HEAD` confirmed exactly one file, 80 insertions.

## Problems solved
- NEXT.md §1's doc14 pointer cited a stale range (`L1072-1090`) for the "Carried-forward note (Session 16-17, 2026-07-26)" heading → root cause: the heading's real position had shifted since that range was written → resolved via live `findstr`, confirmed at `L1129`, pointer updated to the single-line anchor.
- CLAUDE.md's "no commit without explicit authorization" rule was operative in practice across multiple prior sessions but not a written hard rule → closed by adding it as a verbatim bullet immediately after the "runnable, committed checkpoint" bullet, closing the Session-18 handoff's deferred item.

## Mistakes
None this session — both edits matched the dictated text/anchor exactly, diffs and status were returned raw before either commit, and both commits were scoped-checked (`git status --porcelain=v1`) before staging and verified (`git show --stat`) after.

## Prompts worth keeping
None this session — both edits, their exact wording, and the commit/handoff sequencing were fully specified by the user; no drafting choice produced the result.

## Questions I still have
None new this session.

## TODOs for next session
- NEXT.md §2 items 1 and 2 (live-smoke sequencing decision; ingest branch-check gap decision) remain the real next work, per the session-19 handoff's Next Action — both decision-only but feed high-blast-radius work, so must run through the full apparatus with a pre-committed outcome matrix before any src/ change or live-smoke design.
- Live smoke remains NOT authorized.

## Session 20 (ingest branch-check gap resolved — Option A, ADR-20 Amendment 1, commit c6f2512)

NEXT.md §2 item 2 (ingest branch-check gap) was designed under plan mode with a pre-committed evidence/design gate (raw grep/read evidence required for every claim before implementation was authorized), implemented as a new step 5b in `cmd_run` (`src/runtime/main.py`), verified against the durability harness on both seeds, and committed as `c6f2512` alongside an ADR-20 Amendment 1 (docs/08) and NEXT.md/CLAUDE.md tracking updates.

## Cases

### CASE: branch-checkout-inserted-before-recovery-not-immediately-before-ingest
status: distilled
problem: The gap's naive framing ("add a checkout before `_ingest_issues` reads `Issues.md`") would have placed the fix at the ingest call site — but `recover()`'s `bind_reconciler(adapter, cfg.project.branch)` binds recovery's seams to the configured branch, and the baseline health check (`Validator.validate`) runs commands against the physical working tree, both BEFORE ingest runs. Either running against a mismatched branch would make the fix cosmetic — recovery would reconcile the wrong branch's state and baseline-green would assert nothing about the configured branch.
decision: Placed the checkout as new step "5b" in `cmd_run`, between adapter construction and orphan reap/recovery/baseline — before every seam that depends on the physical tree matching `cfg.project.branch`, not just before the ingest call site.
why: The user's design gate required raw evidence for every claim before implementation; reading `recover()`'s bind_reconciler call and `Validator.validate`'s command execution surfaced that both consume the physical tree earlier than ingest does, so an ingest-site-only fix would have left the higher-blast-radius consumers (recovery, baseline health) unprotected.
tags: [adr-20, git, verification, design, main-py]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: placing the fix at the ingest call site only, per the gap's naive original framing.
Tradeoff: an ingest-site-only fix is more minimal and matches how the gap was originally described, but recovery's `bind_reconciler` and the baseline health check both consume the physical tree earlier than ingest runs, so an ingest-site-only fix would leave the higher-blast-radius consumers unprotected; placing the checkout earlier (step 5b, before recovery/baseline) costs touching an earlier point in startup but protects every seam that depends on the tree matching `cfg.project.branch`.
Tipping factor: reading `recover()`'s `bind_reconciler` call and `Validator.validate`'s command execution surfaced that both consume the physical tree before ingest does, so protecting only the ingest site would have left recovery and baseline health unprotected.

### CASE: reused-existing-checkout-branch-no-create-from-no-new-adapter-method
status: distilled
problem: The task's initial premise was that `checkout_branch` did not exist and would need to be added to the RepositoryAdapter interface — this was checked against `src/runtime/repo/git_adapter.py` and `adapter.py` before implementation and found false: `checkout_branch(branch, *, create_from=None)` already exists and is already used for per-issue branches in `loop.py:204`.
decision: Called the existing method with no `create_from` argument, rather than adding a new adapter method or passing a pinned commit.
why: `create_from` force-creates/resets a branch at a pinned commit, which is correct for disposable per-issue branches but wrong for the target repo's long-lived configured branch — that branch must only be switched to, never force-reset, so omitting `create_from` was the deciding factor, not just method reuse for its own sake.
tags: [adr-20, git, api-reuse, verification, design]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: adding a new RepositoryAdapter method (or passing a pinned commit via `create_from`) on the assumption that `checkout_branch` didn't already exist.
Tradeoff: adding a new method would match the task's initial premise, but checking `git_adapter.py`/`adapter.py` first found `checkout_branch(branch, *, create_from=None)` already exists and is already used for per-issue branches; reusing it with no `create_from` costs verifying the premise first but avoids both an unnecessary new method and the wrong force-reset semantics `create_from` would apply to a long-lived branch.
Tipping factor: `create_from` force-creates/resets a branch at a pinned commit, correct for disposable per-issue branches but wrong for the target repo's long-lived configured branch, which must only be switched to, never force-reset.

### CASE: single-except-repoerror-arm-covers-dirty-tree-and-missing-branch
status: distilled
problem: Step 5b's checkout call can fail two different ways — a dirty working tree blocking `git checkout`, or the configured branch not existing locally — and the design needed to decide whether these warranted separate handling.
decision: Read `_git`'s implementation (`git_adapter.py:52-75`) and confirmed its default `check=True` re-raises any nonzero git exit as the same `RepoError` class the existing dirty-tree check already raises, so a single `except RepoError` arm (fail-loud: stderr print + `return 1`) covers both failure modes, matching the existing `except ConfigError`/`except EngineError` pattern in `cmd_run`.
why: Verifying the shared exception class first meant one arm was provably sufficient rather than an assumption; adding two separate except arms would have been unjustified complexity for two failure modes that already collapse to the same type and same fail-loud response.
tags: [adr-20, error-handling, verification, main-py]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: writing two separate `except` arms, one for a dirty-tree checkout failure and one for a missing-branch failure.
Tradeoff: two separate arms would let each failure mode carry a distinct message, but reading `_git`'s implementation confirmed both already surface as the same `RepoError` class under its default `check=True` re-raise; a single arm costs less specific messaging but avoids unjustified complexity for two failure modes that already collapse to the same type and response.
Tipping factor: verifying the shared exception class first showed one arm was provably sufficient, matching the existing `except ConfigError`/`except EngineError` pattern already in `cmd_run`.

### CASE: option-a-explicit-checkout-over-option-b-scoped-risk
status: distilled
problem: NEXT.md §5 had left two options open for the branch-check gap — Option A (explicit `checkout_branch` call) vs Option B (accept as scoped risk, rely on Step 3 preflight Item 0) — with neither chosen.
decision: adopted Option A, explicitly noting Option B was already verified in a prior session as not covering the gap in Item 0's current scratch-workspace form.
why: Option B was ruled out because the check it would have relied on doesn't actually reach this case, leaving Option A as the only option that closes the gap directly rather than deferring to a check with a documented blind spot.
tags: [adr-20, option-a, option-b, ingest, branch-check]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: Option B — accept the gap as scoped risk, relying on Step 3 preflight Item 0.
Tradeoff: Option B requires no new code and defers to an existing check, but that check was already verified in a prior session as not covering this gap in Item 0's current scratch-workspace form; Option A costs implementing an explicit checkout but actually closes the gap directly.
Tipping factor: Option B was ruled out because the check it would have relied on doesn't actually reach this case, leaving Option A as the only option that closes the gap directly.

### CASE: detached-head-and-already-on-branch-not-special-cased
status: distilled
problem: the failure-mode enumeration for step 5b included detached HEAD and already-on-target-branch as cases to define behavior for, alongside dirty-tree and missing-branch.
decision: did not special-case either — a plain `checkout_branch` call corrects detached HEAD with no distinct logging, and an already-on-branch state is an idempotent no-op by construction (git checkout exits 0), with the same startup log line firing truthfully either way.
why: both cases are already handled correctly by the plain checkout call itself, so adding special-case branches would only add code without changing behavior.
tags: [adr-20, detached-head, idempotent, failure-modes]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: writing explicit special-case branches for detached HEAD and already-on-target-branch states.
Tradeoff: special-casing would make each state's handling explicit and separately logged, but a plain `checkout_branch` call already corrects detached HEAD with no distinct logging needed, and an already-on-branch state is already an idempotent no-op by construction; not special-casing costs less explicit coverage but adds no code for behavior that's already correct.
Tipping factor: both cases are already handled correctly by the plain checkout call itself, so adding special-case branches would only add code without changing behavior.

## Commands that worked
- `python -c "import ast; ast.parse(...)"` — syntax check on the edited `main.py` before running the harness.
- `tests/crash/harness.py`, seed 42 and seed 1337 — both `ALL 60 SCENARIOS PASSED` after the `main.py` change, re-run in full this session.
- `git diff --cached --stat` + `git status --porcelain=v1` — confirmed exactly four staged files (`main.py`, `docs/08...`, `NEXT.md`, `CLAUDE.md`) before commit.

## Problems solved
- CLAUDE.md's durability-gate comment said "expect 46 pass" with no seed mentioned, stale relative to docs/14's recorded current state (60 scenarios, both seeds) → corrected as a scoped one-line fix with a docs/14 pointer added so the figure can't silently drift again unnoticed.

## Mistakes
None recorded this session — design was gated by a pre-committed plan-mode review before implementation, and all four touched files were diffed raw and scope-checked before staging.

## Prompts worth keeping
None recorded — the plan (insertion point, adapter reuse, exception handling) was produced under plan mode and followed exactly as authorized; no mid-session drafting divergence is in the record.

## Questions I still have
None new this session.

## TODOs for next session
- NEXT.md §2 item 1 (live-smoke sequencing: witness one carried-forward-unwitnessed surface first vs. carry all three labeled into live-smoke design) is the single remaining open decision — pre-existing, not raised or resolved this session.
- Step 5b itself is UNWITNESSED as running code — verified only as correctly added (raw diff) and non-regressing (harness 60/60 both seeds), not as actually executing against a real branch-mismatch, missing-branch, or dirty-tree case in a live `cmd_run` invocation. Closes only when live smoke runs `cmd_run` against StockPhotoAgent.
- Live smoke remains NOT authorized.

## Session 21 (NEXT.md §2 item 1 resolved — Option B ratified, committed as ebad895)

Opened with the deferred Session-20 historian sweep (the 3 branch-check-gap cases above, correctly filed under "Session 20"), then a raw git/CLI orientation read confirming HEAD at 98fa063 with no ADR-22 tickle owed (still 2.1.220). NEXT.md §2 item 1 (live-smoke sequencing vs. the 3 carried-forward-unwitnessed surfaces) was then resolved via a pre-committed outcome matrix built on a feasibility fact gathered mid-session (raw grep confirmed zero `-m runtime` matches in the harness, meaning no existing scaffolding drives the real `cmd_run` entrypoint), and ratified by the user as Option B. The NEXT.md edit was applied, a session handoff was generated (`docs/handoffs/HANDOFF_2026-07-26_session21-branch-check-item1-option-b.md`), and — after the reviewer's closeability check passed all five criteria — the two tracked files were staged explicitly by name, scope-confirmed via `git status`/`git diff --cached --stat` (docs-only, exactly two files), and committed as `ebad895` on separate per-commit authorization. The historian day-file stays uncommitted (gitignored) regardless.

## Cases

### CASE: standalone-witness-probe-for-surfaces-1-2-found-infeasible-from-existing-scaffolding
status: distilled
problem: NEXT.md §2 item 1 needed a decision between witnessing surfaces 1 (`main.py` startup composition) and 2 (orphan-crash recovery through `cmd_run`) via a standalone scratch probe before live-smoke design (Option A), or carrying all three surfaces labeled into live-smoke design untouched (Option B). The matrix's branch condition turned on whether such a probe could be built from existing scaffolding alone.
decision: Read the raw `cmd_run` body (`src/runtime/main.py:151-264`) and found it hard-requires `claude` on PATH (step 4, `ClaudeHeadlessEngine` init fails fast otherwise), a reachable reviewer/Ollama endpoint (step 8, refuses to start if unreachable), and baseline-green-or-skip — most of the live-smoke precondition stack already. Then `grep`'d `tests/crash/harness.py` for `-m runtime` (zero matches) and read `run_worker`/`verify` (lines 158-205), confirming the harness's crash-injection scaffolding drives a separate `WORKER` script via `subprocess.Popen`, never the real `cmd_run` entrypoint — so no existing harness code can plant a crash AND drive `cmd_run` simultaneously. Concluded a standalone probe would need new harness code plus the same live-service stack live smoke needs, making it strictly more costly than live smoke itself for those two surfaces, and recommended Option B on the matrix's own pre-committed terms.
why: The pre-committed matrix stated in advance that if witnessing surfaces 1 & 2 standalone would require any new production/harness code or new production seam, Option B fires — the raw reads of `cmd_run` and the harness settled that condition on evidence gathered mid-session, not a guess made before the matrix was written.
tags: [adr-20, verification, live-smoke, methodology, feasibility]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: building a standalone scratch probe (Option A) to witness surfaces 1 & 2 before live-smoke design, on the assumption existing scaffolding could support it.
Tradeoff: a standalone probe would pre-witness two of the three carried-forward-unwitnessed surfaces before committing to live-smoke design, but reading `cmd_run` and grepping the harness confirmed no existing code could plant a crash and drive the real `cmd_run` entrypoint simultaneously — a probe would need new harness code plus the same live-service stack live smoke needs; carrying the surfaces into live-smoke design directly costs leaving them unwitnessed a while longer but avoids building throwaway harness infrastructure costing more than the live smoke it precedes.
Tipping factor: the pre-committed matrix stated in advance that if witnessing surfaces 1 & 2 standalone would require any new production/harness code or seam, Option B fires — grepping the harness for `-m runtime` (zero matches) settled that condition on gathered evidence.

### CASE: option-b-ratified-carries-all-three-surfaces-labeled-with-a-deferred-hatch
status: distilled
problem: With the feasibility fact settled (standalone probe infeasible from existing parts), the decision was between Option B outright and an immediate reopening of a dedicated scratch-probe work item (A-deferred-now) to close surfaces 1 & 2 before live-smoke design proceeds.
decision: The user ratified Option B — carry all three carried-forward-unwitnessed surfaces (startup composition, orphan-crash recovery, real-tree behavior) labeled into live-smoke design, none pre-witnessed — with an explicit A-deferred escape hatch: a dedicated scratch probe can be reopened later as its own separately-gated item, but only if live-smoke design itself surfaces a specific real-tree risk warranting pre-witness, not scheduled or assumed now. NEXT.md §2 item 1 was struck through with a RESOLVED/Option-B note (matching the existing item-2/item-3 strikethrough convention), with the evidence basis stated in prose and no VERIFIED/INFERRED tags added, per NEXT.md's own no-evidence-labels-outside-§3 rule. §1 and the three-surface tracking language were left factually unchanged — this decision witnesses nothing.
why: Surface 3 (real-tree behavior) is irreducible regardless of A or B, and building a new probe now to close only 2 of 3 surfaces would cost strictly more than proceeding straight to live-smoke design (which needs the same live-service stack anyway) while still leaving surface 3 unwitnessed — so paying that cost now, before live-smoke design has even started and might itself surface a sharper reason to pre-witness, was judged premature.
tags: [adr-20, policy, live-smoke, sequencing, next-md]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: immediately reopening a dedicated scratch-probe work item (A-deferred-now) to close surfaces 1 & 2 before live-smoke design proceeds.
Tradeoff: closing 2 of 3 surfaces now would reduce what live-smoke design carries unwitnessed, but building a new probe now costs strictly more than proceeding straight to live-smoke design (which needs the same live-service stack anyway) while still leaving surface 3 unwitnessed either way; carrying all three labeled into live-smoke design costs leaving more unwitnessed for longer but avoids paying for a probe before live-smoke design itself might surface a sharper reason to pre-witness.
Tipping factor: surface 3 (real-tree behavior) is irreducible regardless of Option A or B, so building a probe now to close only 2 of 3 surfaces was judged premature relative to the cost of proceeding directly to live-smoke design.

## Commands that worked
- `sed -n '151,264p' src/runtime/main.py` — raw `cmd_run` body, used to establish the live-service precondition stack (claude on PATH, reviewer/Ollama reachability, baseline-green) a standalone probe would also require.
- `grep -n "RUNTIME_CRASH_POINT\|crash_point\|def run_worker\|subprocess\|Popen\|-m runtime" tests/crash/harness.py` — zero `-m runtime` hits, the deciding evidence that no existing harness scaffolding drives the real `cmd_run` entrypoint.
- `git diff NEXT.md` / `git status --porcelain=v1` — confirmed exactly one file (`NEXT.md`), +14/-5, at the point the edit was applied but still held uncommitted per the no-commit-without-authorization rule (later staged and committed together with the handoff, see below).
- `git add NEXT.md "docs/handoffs/HANDOFF_2026-07-26_session21-branch-check-item1-option-b.md"` (explicit paths, not `-A`) → `git status --porcelain=v1` (`M NEXT.md`, `A docs/handoffs/...`) and `git diff --cached --stat` (2 files, docs-only) — used to confirm exactly the intended two-file scope before the reviewer released the commit.
- `git commit -m "docs: session 21 — resolve NEXT.md §2 item 1 (Option B: carry all three carried-forward-unwitnessed surfaces labeled into live-smoke design; A-deferred probe hatch)"` → commit `ebad895`; `git log --oneline -3` and `git status --porcelain=v1 --branch` confirmed tip `ebad895`, parent `98fa063`, clean tree, on `master`, not pushed.

## Problems solved
None — this session resolved a pending decision rather than a defect; see Cases above.

## Mistakes
- Three consecutive raw-output pastes earlier in the session (a Bash-tool block, a grep block, and a `sed` block) contained transcription drift from reconstructing tool output from memory instead of passing it through unaltered — a stray colon, a duplicated line, and two garbled grep lines. Each was self-caught and corrected before the reviewer acted on it; the reviewer then issued a standing instruction to stop reconstructing output and pass raw buffers through directly instead.

## Prompts worth keeping
"Benign and caught three times is the setup for load-bearing and missed on the fourth. The fix isn't more vigilance; it's cutting the reconstruction step out of the relay entirely." — the reviewer's framing that produced the standing raw-buffer-passthrough instruction, after three consecutive self-caught transcription errors in raw-output pastes.

## Questions I still have
None new this session.

## TODOs for next session
- Live-smoke design is the next real work item — high-blast-radius per CLAUDE.md's tiering rule, so it re-enters the full apparatus (pre-committed outcome matrices, evidence-backed design review) fresh rather than inheriting this session's lighter decision-record process.
- Live smoke remains NOT authorized; Step-3 §3 preconditions remain unsatisfied — both unchanged by this session.
- The A-deferred escape hatch (dedicated scratch probe for surfaces 1 & 2) stays available but not scheduled — reopens only if live-smoke design itself surfaces a specific real-tree risk warranting pre-witness.

## Session 17 (Dry-run A — composed-loop witness against a real StockPhotoAgent clone; recovered 2026-07-31 from a stuck knowledge/.sweep/pending.log entry, filed here under its correct original date)

Designed and ran "Dry-run A," a composed-loop witness of the orchestrator's transition-table seams against a real, HEAD-matched scratch `git clone` of StockPhotoAgent (not a hand-built fixture), with only the `claude -p` spawn stubbed. Three pre-committed cycles (VALIDATION_FAILED, REVIEW_REJECTED, clean APPROVE) all ran and passed on mechanical git/event-log evidence — `set_attempt_ref`-before-`reset_hard` ordering witnessed on real committed residue on two different reject paths, and commit-on-approval witnessed for real on the third. A stub bug (`stderr_tail` missing) crashed mid-Cycle-1 and left a dirty clone; recovered by wiping and re-cloning fresh rather than resuming through the crash. Closed by writing an explicit doc-12 correction note into `NEXT.md`, then committing seven paths (`d239471`) after a chain of adversarial-review gates on scope boundaries and evidentiary claims.

## Cases

### CASE: doc-12-correction-note-append-not-silent-edit
status: distilled
problem: NEXT.md's Session-16 entry described a "correction pass" that "fixed stale unwitnessed language across NEXT.md, the day file, and the handoff," but grepping for the doc-12 correction-note marker and checking git log on all three files showed none of them had ever been committed with a prior "before" state — the phrasing overstated a same-session in-draft self-edit as if it were a doc-12-pattern correction of previously-committed text.
decision: appended a new, explicitly-dated correction note to NEXT.md stating plainly that the "correction pass" was a same-session self-edit with no witnessable before-state, and that it should not be read as implying appended correction notes exist — left the original Session-16 entry byte-for-byte intact rather than silent-editing it.
why: the note itself had to avoid re-propagating the exact class of error it existed to fix (prose asserting an artifact that doesn't exist), so it names the doc-12 pattern only to explicitly distinguish itself from it, not to claim membership in it.
tags: [next-md, doc-12, correction-note, honesty, process]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: silently editing NEXT.md's Session-16 entry to remove or fix the overstated "correction pass" language once its unwitnessable premise was found.
Tradeoff: silently editing is a smaller change that removes the wrong claim in one step, but erases the original entry without a trace of what was there or why it changed — and the correction note itself had to avoid re-propagating the exact class of error it existed to fix (asserting an artifact that doesn't exist); appending a dated, explicit note costs more text but leaves the original intact and distinguishes itself from the pattern it names rather than claiming membership in it.
Tipping factor: the note itself had to avoid re-propagating the exact class of error it existed to fix, so it names the doc-12 pattern only to explicitly distinguish itself from it, not to claim membership in it.

### CASE: loop-composition-vs-startup-composition-boundary-recorded
status: distilled
problem: Dry-run A constructs `Orchestrator` directly, bypassing `main.py`'s CLI-level startup health checks (baseline-green, reviewer-reachability) — a real scope reduction from what the original gate (b) composition question flagged, which risked being later over-read as "composition already witnessed" in full.
decision: recorded explicitly, in the harness docstring and in the NEXT.md resume entry, that Dry-run A witnesses only the loop transition-table composition (adapter + Validator + reviewer + event log + commit-on-approval) and NOT main.py's end-to-end startup composition, which remains a live-smoke-first surface.
why: leaving the boundary unstated would let a future session cite this run as having witnessed full composition when only half of it — the loop half — was actually exercised.
tags: [dry-run-a, composition, scope-boundary, live-smoke, gate-b]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: leaving Dry-run A's scope boundary unstated, letting a future session infer from the run's existence that full composition — including `main.py`'s CLI-level startup checks — had been witnessed.
Tradeoff: leaving the boundary unstated costs nothing to write now, but a future session could cite this run as having witnessed full composition when only the loop half was actually exercised; recording the boundary explicitly in both the harness docstring and the NEXT.md resume entry costs extra documentation but prevents that over-read.
Tipping factor: Dry-run A constructs `Orchestrator` directly, bypassing `main.py`'s CLI-level startup health checks — a real scope reduction from what the original gate (b) composition question flagged, which risked being read as full composition if left unstated.

### CASE: cycle3-reject-still-counts-as-pass-no-retry
status: distilled
problem: Cycle 3 used a real (non-deterministic) Qwen reviewer call on an on-target patch to witness commit-on-approval; there was a real chance the reviewer would REJECT instead of APPROVE, leaving that seam unwitnessed.
decision: pre-committed, before running any cycle, that a real REJECT on Cycle 3 would still count as an overall Dry-run A PASS, with no retry and no swapping the patch/issue to force an APPROVE — commit-on-approval would simply be carried forward explicitly labeled unwitnessed.
why: two reject-path ordering witnesses (Cycles 1–2) plus a real-firing reviewer seam already collapse most of the composition variable, and carrying one narrow seam forward as honestly-unwitnessed is cheaper and more truthful than chasing a non-deterministic APPROVE by retrying until the matrix is satisfied.
tags: [dry-run-a, pre-commitment, reviewer, non-determinism, honesty]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: retrying Cycle 3 (or swapping the patch/issue) if the real, non-deterministic Qwen reviewer call happened to REJECT instead of APPROVE, in order to force a witnessed APPROVE.
Tradeoff: retrying until APPROVE would guarantee commit-on-approval gets witnessed this run, but chasing a non-deterministic outcome by retrying is itself a form of un-pre-committed result-shopping; pre-committing that a REJECT still counts as an overall PASS costs leaving one seam honestly unwitnessed but is cheaper and more truthful than retrying until the matrix is satisfied.
Tipping factor: two reject-path ordering witnesses (Cycles 1-2) plus a real-firing reviewer seam already collapse most of the composition variable, so carrying one narrow seam forward as honestly-unwitnessed was judged cheaper and more truthful than chasing a non-deterministic APPROVE.

### CASE: stub-crash-fresh-reclone-not-resume-avoid-orphan-seam
status: distilled
problem: a missing `stderr_tail` field crashed the stub mid-Cycle-1, leaving a partial `EXECUTION_SPAWNED` event and a dirty `issue/1` branch in the scratch clone — resuming through this state would have driven the runtime's real crash-recovery/orphan machinery.
decision: fixed the stub bug, then wiped and re-cloned the scratch repo fresh rather than resuming through the crashed state.
why: resuming would have exercised the orphan-crash recovery seam instead of the loop-composition seam Dry-run A was actually scoped to witness, and doing so accidentally would have produced a false or conflated witness for a different, untested surface.
tags: [dry-run-a, crash-recovery, scratch-clone, scope]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: fixing the stub bug and resuming through the crashed state (partial `ExecutionSpawned` event, dirty `issue/1` branch) rather than wiping and re-cloning fresh.
Tradeoff: resuming through the crash would exercise the crash naturally and cost less setup, but doing so would drive the runtime's real orphan-crash recovery machinery instead of the loop-composition seam Dry-run A was actually scoped to witness, producing a false or conflated witness for a different, untested surface; wiping and re-cloning fresh costs redoing setup but keeps the witness scoped to what was intended.
Tipping factor: resuming would have exercised the orphan-crash recovery seam instead of the loop-composition seam Dry-run A was scoped to witness, accidentally conflating two different, untested surfaces into one result.

### CASE: real-git-clone-not-hand-built-fixture-for-dryrun
status: distilled
problem: Dry-run A needed a stand-in for the real StockPhotoAgent tree to run the loop against without touching the actual repository, and a hand-built fixture (synthetic Issues.md, synthetic branch topology) was an available shortcut.
decision: used a real `git clone` of StockPhotoAgent to a scratch path instead, with an explicit pre-check that `HEAD` matched the source and that `agent-work` existed, and a hard stop condition if the clone diverged from the source in any way that wasn't a pure copy.
why: a hand-built fixture would only prove the loop works against fixture data crafted to work, not against the real branch topology, real test suite, and real commit history the composition witness was meant to validate.
tags: [dry-run-a, scratch-clone, fixture-design, real-target]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: a hand-built fixture (synthetic Issues.md, synthetic branch topology) as the stand-in for the real StockPhotoAgent tree.
Tradeoff: a hand-built fixture is faster to construct and fully controllable, but it would only prove the loop works against fixture data crafted to work, not against the real branch topology, real test suite, and real commit history the composition witness was meant to validate; a real git clone with pre-checks (HEAD match, `agent-work` exists, hard stop on divergence) costs more setup but tests against the real target's actual shape.
Tipping factor: a hand-built fixture would only prove the loop works against data crafted to work, not against the real branch topology, test suite, and commit history the composition witness was meant to validate.

### CASE: departing-from-default-commit-hold-to-clear-accumulating-uncommitted-artifacts
status: distilled
problem: NEXT.md and several handoff files had been sitting uncommitted since Session 16, with this session adding two more edits on top (the correction note, the Dry-run A results) — the standing default is to hold and not commit without explicit authorization, and Sessions 8–16 had all held.
decision: departed from the default-hold reflex and authorized committing the accumulated set (seven paths: NEXT.md, five handoffs, sweep log) this session, after explicitly verifying the staged set matched scope and that the doc-12 correction note was clean against all three of its own criteria.
why: the reasoning given was that holding is the correct reflex only up to a point — durable closing artifacts (handoffs, correction notes) sitting uncommitted across many sessions exist only in the working tree and would be lost to a hard reset, so continuing to hold was itself accumulating risk rather than staying cautious.
tags: [commit-ruling, next-md, handoffs, no-commit-default, risk]

#### JUDGMENT (distilled 2026-07-31)
Rejected alternative: continuing the default hold-and-don't-commit-without-explicit-authorization reflex that Sessions 8-16 had all followed, leaving NEXT.md and the handoffs uncommitted again.
Tradeoff: continuing to hold stays maximally conservative and matches established session-to-session precedent, but durable closing artifacts (handoffs, correction notes) sitting uncommitted across many sessions exist only in the working tree and would be lost to a hard reset — continuing to hold was itself accumulating risk; departing from the default to commit the accumulated set costs breaking precedent but clears real, growing risk of loss.
Tipping factor: holding is the correct reflex only up to a point — durable closing artifacts existing only in the working tree across many sessions accumulate loss-risk, making continued holding itself the riskier choice, not the cautious one.

## Commands that worked
- `git -C <clone> rev-parse HEAD` / `git -C <clone> for-each-ref refs/attempts/<n>/*` / `git -C <clone> rev-parse issue/<n>` — the mechanical git-fact capture pattern used for every cycle's dual snapshot (attempt ref at residue commit, branch tip back at base), explicitly in place of any engine/reviewer self-report.
- `subprocess.run(..., shell=True)` via the target interpreter (not the Bash tool) for `git diff` witness checks — avoids the previously-known Git-Bash backslash-path mangling for Windows paths.

## Problems solved
- Stub crashed mid-Cycle-1 with a partial `EXECUTION_SPAWNED` event and a dirty `issue/1` branch → root cause was a missing required `stderr_tail` field in the canned stub response → fixed the stub, then wiped and re-cloned fresh rather than resuming (see CASE above).

## Mistakes
None recorded as engineer error in this segment — the stub crash was a stub-authoring bug, addressed directly (see Problems solved).

## Prompts worth keeping
None beyond what's already recorded in Session 1.

## Questions I still have
None recorded in this transcript beyond the explicitly parked, permanently-labeled vacuity-guard limitation, which the transcript states is not being re-litigated.

## TODOs for next session
- Decide whether to witness `main.py`'s real startup-order composition (health checks + `_ingest_issues` + loop) on the same clone approach before live smoke, or proceed to live smoke on the real tree carrying the three named unwitnessed surfaces (startup composition, orphan-crash recovery path, real-tree behavior) plus the permanently-labeled vacuity-guard limitation.
