---
project: issue-runtime
date: 2026-08-01
type: mixed
stack: [python, git, powershell, event-sourcing, claude-headless-orchestrator, docker, ollama]
status: Session 2 closed — Path A run authorized and executed cleanly: issue-11 shipped via 11-e3 (merged to agent-work at d663e32c), issue-12 root-caused as a byte-identical duplicate and escalated to NEEDS_HUMAN, handoff committed (d32a917) scoped to issue-runtime only; hold_pid diff on claude_headless.py remains uncommitted as intended.
---

# issue-runtime — 2026-08-01

## What I worked on
Fresh executor session building a Group S dangling-execution fixture on issue-runtime, gated at every phase by a separate reviewer relayed through Adi. Re-verified session-25/26 handoff state matched the tree exactly, then plan-gated and executed the ingest-only phase for a new issue-11 (fixture bait: `[logging].log_level` documented but never consumed in `src/common`), discovering along the way that issue_id assignment is literal/author-supplied rather than scan-based and that the `C:\Python314` interpreter pin lacks issue-runtime's own dependencies. Closed both gaps, got ingest approved and executed cleanly (event 86, byte-identical prefix on events.jsonl 1–85), then planned and got approval for the armed-execution + sentinel-capture phase, which spawned a real `claude` child on issue-11 and paused at the sentinel with `leaf_worker_pid=68048`. Session ends mid-witness-sequence, before the four-part gate and shape-falsification check are reported.

## Cases

### CASE: issue-id-assignment-literal-not-scanned
status: distilled
problem: Group S fixture plan needed to prove issue-11 ingest can't collide with existing issue_ids 1–10, and the prior plan draft assumed a max-existing-id+1 scan would provide that guarantee.
decision: Reading `main.py:127-148` (`_ingest_issues`) and `issues_md.py` showed `spec.id` is parsed directly from the `## <id>: <title>` heading text in Issues.md — no scan, no counter — and the `if spec.id in proj.issues: continue` guard is idempotency (skip re-emit), not collision protection; collision-proofing was moved entirely to a manual pre-ingest grep for an existing `## 11:` heading plus a post-ingest exact-count check for exactly one new `issue_id:"11"` event.
why: The reviewer's blocking-gap review explicitly required the assignment code be opened and pasted rather than assumed, because a counter/literal-based scheme changes the whole fixture safety argument; the actual code turned out ambiguous/author-dependent, which is not visible from any commit message or config file.
tags: [event-sourcing, fixture-design, collision-safety, issue-runtime]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: relying on the existing `if spec.id in proj.issues: continue` guard as sufficient collision protection.
Tradeoff: trusting the existing guard would require no extra engineering, but that guard only skips re-emission of an already-seen id — it does not protect against a collision with a different intended id; building an explicit pre-ingest grep plus a post-ingest exact-count check costs manual verification steps but produces a real collision-safety guarantee.
Tipping factor: the reviewer required the actual assignment code be opened and pasted rather than assumed, since a counter/literal-based scheme changes the whole fixture safety argument, and the real mechanism (literal parse from heading text) is not visible from any commit message or config file.

### CASE: python314-pin-scoped-to-spawned-child-only
status: distilled
problem: The armed-execution plan carried forward a `C:\Python314\python.exe` interpreter pin (validated for leaf-worker process-tree resolution) for all Group S phases, but the ingest-only step failed under that interpreter with `ModuleNotFoundError` for both `runtime` and `yaml`.
decision: Confirmed `C:\Python314` only has StockPhotoAgent's own `requirements.txt` installed (it's StockPhotoAgent's validation-subprocess interpreter), never issue-runtime's deps or package; ingest-only and the orchestrator process itself run under `.venv`, while the `C:\Python314` pin is reserved strictly for phases that spawn `claude` children, since Layer-1 leaf resolution walks descendants of the spawned `claude` process (`proc.pid`), not the orchestrator's own interpreter.
why: The pin's original justification (validated leaf-resolution shape) never actually depended on the orchestrator's interpreter — it depended on the spawned child's process tree — so pinning the orchestrator was solving the wrong process; this wasn't visible without opening `claude_headless.py`'s Layer-1 resolution code and hitting the concrete `ModuleNotFoundError` first.
tags: [interpreter-pin, process-tree, claude-headless, issue-runtime]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: carrying forward the `C:\Python314\python.exe` pin for all Group S phases, including the orchestrator process itself.
Tradeoff: keeping one interpreter pin for everything follows the prior session's validated pin with no extra bookkeeping, but that pin only has StockPhotoAgent's dependencies installed, so the ingest step failed with `ModuleNotFoundError`; scoping the pin narrowly (only to phases spawning `claude` children, `.venv` for everything else) costs extra care in tracking which interpreter applies where but actually runs.
Tipping factor: the pin's original justification depended on the spawned child's process tree, not the orchestrator's own interpreter — pinning the orchestrator was solving the wrong process, only visible after opening `claude_headless.py`'s Layer-1 code and hitting the concrete `ModuleNotFoundError`.

## Commands that worked
- `cd C:/Projects/issue-runtime && .venv/Scripts/python.exe scratch/ingest_only.py` — succeeded, stdout `[ingest-only] 1 new issue(s)`, produced event 86 (`IssueCreated`, `issue_id: "11"`) with lines 1–85 of `events.jsonl` byte-identical to the pre-ingest snapshot.
- `set ITEM9_SENTINEL=1` then `.venv/Scripts/python.exe -m runtime.main run --config config.yaml` from `C:/Projects/issue-runtime` — succeeded, spawned the claude child on issue-11 and paused at the sentinel (`[sentinel] paused: orchestrator_pid=43516 child_pid=45312 leaf_worker_pid=68048 execution_id=11-e1`).
- `git -C C:/Projects/issue-runtime log --oneline -3` and `git -C C:/Projects/StockPhotoAgent status --porcelain=v1` — used to re-verify prior-session state matched the handoff before any mutation.

## Problems solved
- Symptom: `C:\Python314\python.exe -c "import runtime"` and `import yaml` both raised `ModuleNotFoundError` when attempting the pinned ingest run. Root cause: `C:\Python314` only ever had StockPhotoAgent's dependencies installed, never issue-runtime's (`pyyaml`, `pydantic`, the `runtime` package itself). Fix: ran the ingest driver under `.venv` instead, and re-scoped the interpreter pin to only the phases that spawn `claude` children.
- Symptom: plan draft assumed issue_id collision-safety came from a scan-based assignment. Root cause: `_ingest_issues` assigns `issue_id` literally from the Issues.md heading text with no scan/counter. Fix: added an explicit pre-ingest grep for an existing `## 11:` heading and a post-ingest exact-count check as the real collision-proofing mechanism.

## Mistakes
- Combined three read-only git status checks (StockPhotoAgent HEAD/branch/status) into one call; the status field came back empty and its meaning (clean vs swallowed output) was ambiguous. Re-ran the status check in isolation to confirm "clean" rather than assuming.

## Prompts worth keeping
- The reviewer's "BLOCKING (plan cannot go until each is witnessed as text)" gate, demanding the exact assignment-code lines be pasted rather than assumed — this is what surfaced that issue_id assignment is literal/author-supplied, not scan-based, before any mutation happened.

## Questions I still have
- Whether `leaf_worker_reason` is null (stable leaf, not a timeout fallback) for execution 11-e1 — witness sequence step 2/3 not yet reported at session end.
- Whether the descendant chain for 11-e1 is actually `claude.CMD → cmd.exe → node` (non-python-rooted) as plan §1 predicted — shape-falsification check (step 4) not yet run.

## TODOs for next session
- Finish the armed-execution witness sequence for execution 11-e1: `sentinel_ready`/`sentinel_resume` presence check, full JSON dump (leaf_worker_pid, leaf_worker_reason, descendant_pids), the four-part gate, and the shape-falsification walk of `descendant_pids`.
- Report all raw witness output to the reviewer before any crash injection is authorized; the child remains paused at the sentinel with nothing killed or resumed.

## Session 2

### What I worked on
Fresh session ("new session" per the transcript) re-verifying the tree against the committed handoff at `c0333d2`, catching and correcting an Ollama host-vs-Docker check mistake, then working through three rounds of read-only gap-closing (a loose uncommitted `paths.py` diff on StockPhotoAgent's `issue/11` worktree, the `1e06481` cold-start-blocker disposition, and orphaned-commit reachability) plus a code-only recovery-path witness across `reconciler.py` / `bindings.py` / `git_adapter.py` establishing that recovery snapshot-commits a dirty worktree to an attempt ref before `reset_hard` touches it. With that safety established, Adi authorized exactly one native `run` (Path A): it recovered dangling `11-e2`, spawned `11-e3`, which succeeded and merged issue-11 into `agent-work` at `d663e32c` (events 92–99). The freed queue activated issue-12, which no-oped twice and escalated to `NEEDS_HUMAN` (events 100–109); this was diagnosed as issue-12 being a byte-identical duplicate of issue-11 whose fix was already satisfied at its own base commit. The session closed with a handoff (commit `d32a917`) scoped strictly to issue-runtime, leaving the uncommitted `hold_pid` diff untouched.

### Cases

### CASE: cold-start-blocker-1e06481-accepted-as-agent-work-baseline
status: distilled
problem: Cold-Start Blocker 1 — an unauthored commit `1e06481` ("history(auto): StockPhotoAgent 2026-07-31", a knowledge-sweep artifact) sits as `agent-work`'s tip, currently coinciding with `issue/11`, and needed a disposition before Path A could be authorized.
decision: `1e06481` accepted in place as `agent-work`'s legitimate tip; all future "clean agent-work" baseline checks must re-baseline to `1e06481`, not the older `b66e795`.
why: `git show 1e06481` confirmed it only adds `knowledge/StockPhotoAgent/2026-07-31.md` (+34 lines, tracked, not gitignored) with no code change, and no `refs/attempts/*` point into it or below it — it's a benign session-history artifact one commit ahead of `b66e795`, not a hostile or accidental code commit.
tags: [git, agent-work, baseline, cold-start, issue-runtime]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: treating `1e06481` as a suspect commit requiring remediation before Path A could be authorized.
Tradeoff: treating it as suspect would force additional investigation or remediation, delaying Path A; accepting it in place after a direct `git show` check costs re-baselining future "clean agent-work" checks to the new tip but lets the session proceed immediately.
Tipping factor: `git show 1e06481` confirmed it only adds a tracked, non-gitignored history file with no code change and no `refs/attempts/*` pointing into or below it — a benign session-history artifact, not a hostile or accidental code commit.

### CASE: path-a-native-recovery-over-manual-escalation
status: distilled
problem: `11-e2` was left dangling (`ExecutionSpawned`, no terminal event) with a loose uncommitted `paths.py` diff sitting in the StockPhotoAgent worktree, and Adi had to decide whether to let the orchestrator's native recover-then-run path handle it or intervene manually.
decision: Adi authorized exactly one native `run` (Path A) rather than manual escalation, after the code-only witness (`reconciler.py`/`bindings.py`/`git_adapter.py`) established that recovery snapshot-commits the dirty tree to an attempt ref before `reset_hard` touches the worktree.
why: this preserves event-log integrity and exercises the real recovery path instead of hand-crafting state; the run outcome exceeded the exhaust-and-escalate prediction — `11-e3` succeeded outright and merged to `agent-work` at `d663e32c`, evidenced by `refs/attempts/11/11-e2` (`00433c9`) containing the `log_level`/`basicConfig` diff and a clean post-run worktree.
tags: [recovery, git-adapter, event-log, path-a, issue-runtime]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: manually escalating and hand-handling the dangling `11-e2` execution and its loose uncommitted `paths.py` diff.
Tradeoff: manual escalation gives more direct control over the outcome, but hand-crafting state risks event-log integrity and bypasses testing the real recovery path; authorizing exactly one native `run` costs ceding control to the orchestrator's own recovery logic but preserves event-log integrity and exercises the actual code path.
Tipping factor: the code-only witness established that recovery snapshot-commits the dirty tree to an attempt ref before `reset_hard` touches the worktree, which preserves event-log integrity and justified trusting the native path.

### CASE: issue-12-duplicate-of-issue-11-excluded-from-adr19-sample
status: distilled
problem: issue-12 spawned two attempts (`12-e1`, `12-e2`), both produced empty diffs (start==end) and were REJECTed by the reviewer, then escalated to `NEEDS_HUMAN` (event 109) — needed root-causing before any ADR-19 sample decision.
decision: root-caused as "(b) already-satisfied criterion" — issue-12's body is byte-identical to issue-11's (confirmed against event 86), and by the time issue-12 activated at base `d663e32c`, issue-11's own fix (`11-e3`) was already merged in, so the fix issue-12 asked for already existed. Issue-12 is excluded from the ADR-19 kill-criteria sample as a moot duplicate and left in `NEEDS_HUMAN` as the correct resting state, not re-run. The underlying ingest de-dup gap (no de-duplication on identical bodies under distinct ids) is logged as an accepted-known limitation rather than fixed this session.
why: `git ls-tree d663e32c` and reading `src/common/paths.py` at that commit showed the `log_level`/`basicConfig` fix already present, matching issue-12's named fix verbatim — the no-op diffs were correct behavior, not a bug in spawning or prompt assembly.
tags: [adr-19, duplicate-issue, ingest, root-cause, issue-runtime]

#### JUDGMENT (distilled 2026-08-09)
Rejected alternative: fixing the underlying ingest de-dup gap this session, or re-running issue-12 instead of leaving it in `NEEDS_HUMAN`.
Tradeoff: fixing the de-dup gap would prevent future duplicate-issue escalations, but wasn't necessary to resolve issue-12 itself and would add scope beyond the session's goal; logging it as an accepted-known limitation and leaving issue-12 in `NEEDS_HUMAN` costs leaving the gap unfixed but correctly reflects that the no-op diffs were correct behavior, not a bug.
Tipping factor: `git ls-tree d663e32c` and reading `paths.py` at that commit showed the fix issue-12 asked for was already present, matching issue-12's named fix verbatim — the no-op diffs were correct behavior, not a bug in spawning or prompt assembly.

### Commands that worked
- `git -C C:/Projects/issue-runtime log --oneline -3` — confirmed HEAD=`c0333d2` matches the handoff.
- `python -c "import ast; ast.parse(...)"` against `claude_headless.py` — confirmed `AST OK` for the uncommitted `hold_pid` diff.
- `docker exec ollama ollama list` / `curl localhost:11434/api/tags` — confirmed `qwen2.5-coder:14b` present on the Docker endpoint the reviewer actually uses, correcting an earlier native-CLI check.
- The single Adi-authorized orchestrator `run` (background task `biwjmfot1`) — completed exit 0; recovered `11-e2`, spawned/shipped `11-e3`, escalated issue-12; verified via `events.jsonl` 92–109 and `refs/attempts/11/11-e2`.
- `git -C C:/Projects/issue-runtime` commit of the session-26to27 handoff — `d32a917`, verified via `git log --oneline -1` and `git status --short` (hold_pid diff still `M`, StockPhotoAgent untouched).

### Problems solved
- Symptom: ollama tag-list check reported `qwen2.5-coder:14b` missing. Root cause: checked the native host CLI's Ollama instead of the Docker container actually serving `localhost:11434`. Fix: `docker exec ollama ollama list` / `curl localhost:11434/api/tags` confirmed the model present; the finding was retracted.
- Symptom: issue-12's two attempts (`12-e1`, `12-e2`) both produced empty diffs and were rejected. Root cause: issue-12's body is byte-identical to issue-11's, and by the time issue-12 activated (base `d663e32c`) issue-11's own fix (`11-e3`) was already merged in, so the criterion was already satisfied. Fix: none needed — escalated to `NEEDS_HUMAN` as the correct terminal state, excluded from the ADR-19 sample as a moot duplicate.

### Mistakes
- Initial ollama tag-list check hit the native host CLI rather than the Docker container serving the reviewer's endpoint, reporting a false "model missing." Re-checked via `docker exec`/`curl` and retracted the finding — the same class of mistake already documented at `config.yaml:49-53`.

### Prompts worth keeping
- The pre-committed outcome matrix (Row 1/2/3) attached to the Path A authorization — naming success/exhaust/anomaly outcomes and their exact proof artifacts in advance — meant the actual run result (Row 1, exceeding prediction) could be scored objectively against the POST-RUN capture rather than narrated after the fact.

### Questions I still have
None.

### TODOs for next session
- Read `docs/handoffs/HANDOFF_2026-08-02_session26to27-close-issue11-shipped-issue12-duplicate-escalated.md` at next session start for full continuity before any further mutation.
