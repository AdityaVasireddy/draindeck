---
project: issue-runtime
date: 2026-07-13
type: mixed
stack: [python, pytest, git, claude-cli]
status: Session 6 Step 0 and Step 1 complete, verified, and committed (aaf6b60, d097f1f); resume point is Step 2 preflight per the handoff.
---

# Issue Runtime — 2026-07-13

## What I worked on
Designed Session 6 (the Phase-2 gate) in Plan Mode on Fable/Sonnet, incorporating six user-dictated amendments to the original plan (offline-gate enforcement, config.ini gate determinism, transcript streaming, Ctrl+C child-process protocol, commit-approval protocol, and all-missing-usage escalation), then switched to Opus for implementation. Ran Step 0 baseline re-verification (103/103 unit, 55/55 harness seed 42) and Step 1 (added the two deferred harness crash points, `validate:post-artifact` and `after_append:IssueEscalated`, plus a scripted issue 045). Before committing, resolved a user-flagged characterization gap: whether the mutation spot-check's "joint worker+recovery coverage" claim was honest or was papering over a harness/production fidelity gap. Investigation found it was the latter — production does not blanket-reset the worktree the way the harness's worker does, and check 3's reset is unproven harness-wide (a pre-existing gap). doc 14 was rewritten to state this honestly, both crash points and doc 14 were committed, and a session handoff was produced.

## Cases

### CASE: check-3-reset-unproven-gap
status: auto
problem: needed to prove via mutation spot-check that the extended crash harness "still bites," specifically that check 3's dirty-tree reset is load-bearing for the new `validate:post-artifact` point.
decision: documented `validate:post-artifact` in doc 14 as PARTIAL proof (loop-level survival only, not check-3's reset specifically), refused to write "joint worker+recovery coverage," and recorded the reset-proof gap as named deferred item R1 (a planned f5 fixture) rather than closing it this session.
why: gutting check 3's `reset_hard` passed the full harness 59/59, including the pre-existing f1/f2 fixtures from Session 2 — proving the reset itself is masked everywhere by the worker's blanket resets and check 3's own archive-snapshot; this is a pre-existing gap, not something Step 1 introduced, and the session was scoped to the two crash-point files only.
tags: [testing, harness, crash-recovery, mutation-testing]
until: f5 fixture (named deferral R1) is implemented to independently prove check-3's reset

### CASE: production-no-blanket-reset-before-executing
status: auto
problem: whether production actually resets the worktree before every attempt spawn the same way `tests/crash/worker.py` does at every EXECUTING entry, which would make "joint coverage" an accurate claim.
decision: confirmed NO by grepping `loop.py` and `git_adapter.py` — production's `_execute` uses `checkout_branch(create_from=base)`, which refuses outright on a dirty tree; its `reset_hard` calls only occur on reject/escalate paths after a terminal outcome; `_validate` performs no reset of its own.
why: this settles that production genuinely depends on recovery check 3 healing the tree at a mid-VALIDATING crash — the harness worker's blanket reset does not model production, so equating the two would have been a fidelity gap.
tags: [production-fidelity, harness, git-adapter, reconciler]

### CASE: escalation-idempotency-mutation-method
status: auto
problem: needed to prove the `verify()` "exactly once" `IssueEscalated` assertion is non-vacuous, but a genuine duplicate escalation is already blocked earlier by production's transition-table idempotency guard, so a normal double-emit couldn't reach the assertion.
decision: temporarily added a tolerant transition-table entry and manually appended a second `IssueEscalated` event to a clean run's log to exercise `verify()` directly, then fully reverted.
why: `(NEEDS_HUMAN, ISSUE_ESCALATED)` is absent from the transition table, so a real duplicate raises `TransitionError` at replay before `verify()` ever sees it — the assertion needed to be tested from outside that guard to know it actually fires.
tags: [testing, mutation-testing, idempotency, event-log]

### CASE: offline-gate-enforcement-via-socket-monkeypatch
status: auto
problem: the original Session 6 plan proposed proving the unit gate's offline-ness via design review/attestation.
decision: amended to add `tests/unit_gate/conftest.py`, monkeypatching `socket.socket` to raise, so offline-ness is enforced empirically on every gate execution rather than attested once.
why: user rejected the design-review approach outright — "design review is not the check I specified" — requiring the property to be mechanically enforced every run, not asserted in docs.
tags: [testing, offline-enforcement, plan-amendment, conftest]

### CASE: config-ini-gate-must-run-without-real-config
status: auto
problem: `paths.py:38` reads `config.ini` at module scope, so a gate that imports every `src` module either fails on a clean tree lacking `config.ini` or silently depends on an untracked credential file.
decision: the gate must supply its own test-scoped config (a temp config generated from `config.ini.example` with a path/env override, or `CONFIG_FILE` stubbed pre-import) and must be green with the real `config.ini` absent.
why: user cited Issue 4 as prior evidence of this exact hidden-dependency failure mode.
tags: [testing, config, gate-design]

### CASE: execution-crashed-consumes-attempt-1
status: auto
problem: the ADR-19 audit script needed a precise definition of when an issue's "attempt-1" is consumed, since attempt-1 success is computed via StateProjection replay, not string-parsing `execution_id`.
decision: `ExecutionCrashed` consumes attempt-1 (proposed by the user as "yes," adopted into the plan).
why: needed for the kill-criteria arithmetic (ADR-19) to be mechanical rather than requiring judgment calls at metric-computation time.
tags: [adr-19, metrics, audit-script]

Also noted (not drafted): committing Step-1 changes directly to `master` because "prior sessions commit directly to master" (precedent-following, no alternative weighed in record); the Opus/Sonnet model-split rule (Opus for transitions/harness/fence, Sonnet for fully-specified mechanical work) stated as a plan directive without a counterfactual in the transcript.

## Commands that worked
- `python -m pytest tests\unit -q` — 103/103 pass, ~27.9s (verified this session, Step 0).
- Full crash harness (`tests\crash\harness.py`), seed 42 — 55/55 pass at Step 0 baseline, then 59/59 after Step 1's two new crash points; also 59/59 on seed 1337. All verified this session.
- Filtered individual runs of the two new crash points (`validate:post-artifact`, `after_append:IssueEscalated`) — both green independently.
- `git diff src/` — confirmed empty after each of the three temporary production mutations was reverted.

## Problems solved
- Symptom: doc 14's original mutation spot-check ("gut check 3's reset, expect `validate:post-artifact` to go red") produced no red result. Root cause: the harness worker's blanket `reset_hard(base)` at every EXECUTING entry, plus check 3's archive-snapshot, mask any dirty-tree issue regardless of whether check 3's reset itself runs — a pre-existing property since Session 2's f1/f2 fixtures. Fix: substituted a mutation on production's `_escalation()` (returning `DONE` instead of `NEEDS_HUMAN`) to prove the new terminal-map/escalation assertions are non-vacuous, and documented the reset gap honestly in doc 14 as deferred item R1 instead of claiming proof that didn't exist.
- Symptom: needed to prove the `IssueEscalated` "exactly once" assertion isn't vacuous, but production's transition table already blocks a genuine duplicate before `verify()` runs. Fix: temporarily loosened the transition table and hand-appended a duplicate event to trigger the assertion path directly, confirmed it fires (`issue 045 has 2 IssueEscalated events (want 1)`), then fully reverted.

## Mistakes
- Doc 14's inline code comments (post-mutation-revert) still implied `validate:post-artifact` proved check-3's reset specifically — contradicting the session's own findings. Corrected before committing.

## Prompts worth keeping
- The three-check diagnostic prompt ("PRODUCTION FIDELITY CHECK... FREE EVIDENCE CHECK... TARGETED MUTATION...") that forced resolution of the doc-14 characterization gap rather than accepting the original "joint coverage" framing — produced a materially more honest doc 14 and surfaced a real pre-existing harness gap.

## Questions I still have
None stated as unresolved in the transcript beyond what's already captured as deferred item R1.

## TODOs for next session
- Implement the f5 fixture (named deferred item R1) to independently prove check-3's `reset_hard` is load-bearing, since it's currently unproven harness-wide.
- Update NEXT.md (deliberately skipped this session since scope was limited to Step 0-1).
- Session 6 Step 2 preflight: 2a billing re-verification, 2b engine-version/fence re-probe.
- Harden `_reviewer_reachable` to check `/api/tags` for the configured model tag (noted as a NEXT.md line item during plan amendments).
