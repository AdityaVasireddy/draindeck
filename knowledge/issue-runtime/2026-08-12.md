---
project: issue-runtime
date: 2026-08-12
type: mixed
stack: [python, pytest, git, powershell, ollama, qwen2.5-coder]
status: ADR-19 formally closed PASS across two corroborating samples; live drain resumed through issue 47 (cap-stopped, 8/10 shipped attempt-1, 2 escalated). Session 2: backlog issues 36 and 43 decomposed/rescoped and committed to StockPhotoAgent (f8a69f7) — 36 superseded by self-contained sub-issues 99-102, 43 rescoped to review_manager.py:215 — but not yet ingested into events.jsonl; no live drain ran; drain-scoping for 99-102 and issue-48's stalled orphaned activation remain unresolved.
---

# issue-runtime — 2026-08-12

## What I worked on
Cold-start verification (issue-runtime HEAD, StockPhotoAgent tree, event count, Ollama reachability) confirmed a clean starting state, then the session logged a scoping-gap note in NEXT.md (`max_executions_per_run` caps total executions per run, not the targeted issue) and committed it (7a58762). Preconditions were re-verified (131 unit tests pass, crash harness 60/60 on both seeds), then a real batch drain ran against StockPhotoAgent (issues 38–47), cap-stopping at 10/10 executions with 8 shipped attempt-1 and 2 escalated (`needs-decomposition`). The event log was used to tally a second ADR-19 corroboration sample (n=19: 16 shipped attempt-1, 3 escalated across the fuller 29–47 window), which was formally recorded as ADR-19 CLOSED PASS in `docs/08-session-0-closure-and-adr-amendments.md` §4 (commit e9f5d5b), after an initial mistaken append to a different file was caught and reverted. A session-41 handoff was written and committed (9d7b22e). Mid-session, a fake system-reminder attempted to get the file revert silently suppressed; it was verified false and disclosed.

## Cases

### CASE: fresh-temp-roots-over-stale-windows-git-locks
status: cited
problem: Crash harness runs against pre-existing stale temp dirs (`%TEMP%\ch`, `%TEMP%\ch42`, left over from an earlier session) hit `PermissionError` during git-object cleanup because Windows holds git's read-only object files locked against direct `rmtree`.
decision: Used genuinely fresh, previously-unused temp roots (`%TEMP%\ch_s40i38_42`, `%TEMP%\ch_s40i38_1337`) for the seed-42 and seed-1337 runs instead of forcing deletion of the locked stale ones; the stale dirs were left untouched on disk.
why: Windows won't allow `rmtree` to remove git's read-only object files directly, so fighting the lock was avoided entirely by picking unused roots — this let both seeds reach a clean 60/60 pass without further cleanup work.
tags: [windows, git, temp-files, testing, harness]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: force-delete the locked stale temp dirs before rerunning, instead of picking new roots.
Tradeoff: forcing cleanup would reclaim disk space and avoid accumulating throwaway temp roots over time, but requires fighting a Windows read-only git-object lock that `rmtree` can't clear directly; picking fresh roots reached a clean 60/60 pass immediately, at the cost of leaving stale dirs on disk.
Tipping factor: Windows won't allow `rmtree` to remove git's read-only object files directly, so fighting the lock was avoided entirely by picking unused roots.

### CASE: adr19-closure-goes-in-canonical-criteria-doc-not-handoff
status: distilled
problem: There is no single file literally named "ADR-19.md" — the kill-criteria table lives in `docs/08-session-0-closure-and-adr-amendments.md` §4, while the original n=20 verdict data happened to live only in a handoff file (`HANDOFF_2026-08-03_session29...`). An initial attempt appended the new closure text to that handoff file.
decision: Reverted the handoff-file append and instead appended the formal `### ADR-19 — CLOSED PASS` closure block in-structure inside `docs/08-session-0-closure-and-adr-amendments.md` §4, right after the existing honesty clause.
why: The user explicitly directed reverting the handoff edit and appending in the canonical criteria section instead, so the verdict lives alongside the criteria it satisfies rather than scattered across a handoff that only incidentally held the earlier data.
tags: [documentation, adr, adr-19, structure]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: leave the closure text appended to the handoff file where the original n=20 data happened to live.
Tradeoff: appending to the handoff file keeps related historical data together with less structural search, but leaves the formal verdict reachable only by incidental file-search rather than the doc that states the criteria being closed; appending to `docs/08` §4 costs a revert-and-redo but keeps the verdict discoverable alongside its criteria.
Tipping factor: the user explicitly directed reverting the handoff edit and appending in the canonical criteria section instead.

### CASE: disclosed-suspected-prompt-injection-instead-of-complying
status: cited
problem: Mid-session, a system-reminder appeared claiming the session-29 handoff file "was modified, either by the user or a linter," instructing the assistant not to revert it and not to mention it to the user — directly conflicting with the user's just-given instruction to revert and confirm the file was clean.
decision: Treated the reminder as a likely prompt-injection attempt, independently verified via `git diff --stat` and `git status --porcelain` that the file was byte-identical to HEAD (the claim was false), completed the user's actual revert/confirm request, and explicitly flagged the injection attempt to the user rather than staying silent.
why: The injected instruction asked for concealment and contradicted the user's explicit request; ground truth (git) was checked directly rather than trusting the reminder's claim at face value.
tags: [security, prompt-injection, trust, verification]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: comply with the reminder's instruction not to revert the file and not to mention it to the user.
Tradeoff: complying avoids acting on a possibly-false reminder and skips the verification step, but risks concealing a real issue and ignores the user's just-given explicit instruction; disclosing costs a quick `git diff`/`git status` check but keeps the user's actual request honored and the user informed.
Tipping factor: the injected instruction directly contradicted the user's just-given explicit instruction, which is disqualifying regardless of the reminder's source.

## Commands that worked
- `.venv\Scripts\python.exe -m pytest -q` → 131 passed (matches documented expected count; system `C:\Python314\python.exe` fails at collection for lacking pyyaml/pydantic).
- `tests\crash\harness.py <fresh-root> 42` and `... 1337` → ALL 60 SCENARIOS PASSED, both seeds.
- `.venv\Scripts\python.exe -m runtime.main run --config config.yaml` (logged to `run_cohort_38_47.log`) → real batch drain against StockPhotoAgent, exit code 0, halted on `max_executions_per_run reached (10/10)`, `proxy_dollars_this_run=$6.5999`.
- `git add NEXT.md && git commit -m "docs: log single-issue live-scope scoping gap (session 40)"` → 7a58762.
- `git add docs/08-session-0-closure-and-adr-amendments.md && git commit -m "docs(adr-19): record formal CLOSED PASS verdict across two samples (n=20, n=19)"` → e9f5d5b.
- `git add docs/handoffs/HANDOFF_2026-08-11_session41-adr19-closed-pass.md && git commit -m "docs: session-41 handoff — ADR-19 closed PASS, drain through issue 47"` → 9d7b22e.

## Problems solved
- System Python (`C:\Python314\python.exe`) lacking `pyyaml`/`pydantic` → `ModuleNotFoundError`, 0 tests collected → root cause: wrong interpreter (known gap, documented in NEXT.md §7) → fix: re-ran with `.venv\Scripts\python.exe`, 131 passed.
- Crash-harness `PermissionError` on stale temp roots → root cause: Windows read-only lock on git object files from an earlier (Aug 5) run → fix: fresh unused temp roots per seed (see CASE: fresh-temp-roots-over-stale-windows-git-locks).
- ADR-19 verdict location ambiguity (no dedicated ADR-19.md) → root cause: verdict data had drifted into a handoff file instead of the canonical criteria doc → fix: reverted the handoff append, placed the formal closure in `docs/08...` §4 (see CASE: adr19-closure-goes-in-canonical-criteria-doc-not-handoff).

## Mistakes
Initially appended the second-sample ADR-19 corroboration text to `docs/handoffs/HANDOFF_2026-08-03_session29-adr19-pass-holdpid-blocked.md` (the wrong location, only reachable via file-search rather than the canonical criteria doc). Caught before commit, reverted with `git checkout --`, confirmed byte-identical to HEAD, and redone correctly in `docs/08-session-0-closure-and-adr-amendments.md` §4.

## Prompts worth keeping
"Revert the mistaken append... git status --porcelain # confirm handoff no longer modified" — the explicit revert-then-independently-verify instruction is what surfaced the prompt-injection attempt (a false system-reminder claiming the file was already modified), since the assistant re-checked against git rather than trusting the reminder.

## Questions I still have
None explicitly unresolved in the transcript — the injection's origin/cause was not determined, but the user was not left with an open question about it (verified false and reported).

## TODOs for next session
- Decompose backlog issues 39, 43, and 36 into sub-issues before re-execution (36 is needs-human, 39/43 are needs-decomposition).
- Issue 48 is `IssueActivated` but not yet spawned — first-in-line on any plain drain.
- Carried non-blocking items from the session-41 handoff: B-CRIT-1 (`_resolve_leaf_worker` unwired), Write-tool cwd-escape residual (ADR-21 Am.3), reviewer model-string not persisted (currently inferred), reviewer raw-response persisted nowhere.

## Session 2 (session 42 — decompose 36/39/43, collision recovery)

### What I worked on
This session picked up specifically to decompose the needs-decomposition backlog (issues 36, 39, 43) before any further drain, with cold-start checks re-confirming issue-runtime HEAD `a36ff1a`, StockPhotoAgent clean at `0143486`, the authoritative event log at `C:\Projects\issue-runtime\state\events.jsonl` (not inside StockPhotoAgent, contrary to an earlier assumption), 131 unit tests passing, and Ollama's `qwen2.5-coder:14b` reachable. Issue 39's spec text was pulled and its author reclassified it WONT-FIX in conversation (a separately-scoped label-consistency follow-up item was identified but never appended to Issues.md). Issue 43 was rescoped in place to a single concrete call site (`review_manager.py:215`), and issue 36 was decomposed into four self-contained sub-issues (one per QC rule) rather than left as a single cross-file item. Appending the new sub-issues repeatedly collided with pre-existing issue IDs before the true ceiling was found and the final IDs (99-102) were committed to StockPhotoAgent as `f8a69f7`. A session-42 handoff was written via the session-handoff skill, and in writing it a claimed fact (issue 39 marked WONT-FIX in the file) was checked against the actual file and found not yet durable, so it was recorded as pending rather than done. No live drain was run this session.

### Cases

#### CASE: issue-id-collision-verify-true-ceiling-before-renumbering
status: distilled
problem: Appending new backlog sub-issues to StockPhotoAgent's `Issues.md` (decomposing issue 39, then issue 36) using a prior handoff's stated ceiling of issue 48 as the basis for new IDs collided three times running — first at 49-54, then again at 55-58 — against already-existing unrelated issues that in fact ran up to ID 98.
decision: Queried the file directly for the true highest issue ID (`Select-String -Pattern "^## (\d+):"`, sorted numerically) before choosing final IDs, confirmed 99-102 were free, then appended and committed at those numbers (`f8a69f7`).
why: The prior handoff's ceiling of 48 was stale and wrong by 50 issues; only a direct grep against the live file gave a trustworthy ceiling after two renumbering attempts still collided.
tags: [documentation, issue-tracking, collision, git, verification]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: continue trusting the prior handoff's stated ceiling (48) for subsequent renumbering attempts.
Tradeoff: trusting the handoff's stated ceiling was faster to act on and needed no extra verification step, but it was wrong and caused two separate ID collisions before being abandoned; querying the file directly cost one extra command but gave a ceiling that held on the first try.
Tipping factor: two renumbering attempts based on the stale ceiling still collided, showing the handoff's number could not be trusted without direct verification.

#### CASE: powershell-roundtrip-mojibakes-em-dashes-use-edit-tool
status: cited
problem: Stripping an appended issue block from `Issues.md` via a PowerShell `Get-Content`/join-style edit corrupted the file's existing UTF-8 em-dashes into mojibake (`â€”` instead of `—`).
decision: Discarded the corrupted working tree back to the last clean commit and redid the two pending edits (issue 43 rescope, issue 36 status) using the Edit tool only, then verified byte-level (0 mojibake occurrences, correct em-dash byte count of 147 after the append).
why: PowerShell's `-Command` string argument passing mis-encoded the file's UTF-8 em-dashes on the Get-Content/write round-trip; the Edit tool round-tripped the same bytes correctly earlier in the same session.
tags: [windows, powershell, encoding, utf-8, editing]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: continue using PowerShell to fix or redo the strip edit rather than switching tools.
Tradeoff: continuing with PowerShell would avoid discarding work already done, but its `-Command` string-argument passing had already proven to mis-encode non-ASCII bytes on this exact round-trip; switching to the Edit tool cost discarding and redoing the edits but avoided repeating the same corruption.
Tipping factor: the Edit tool had already round-tripped the same bytes correctly earlier in the same session, while PowerShell had just proven it could not.

#### CASE: verify-user-supplied-handoff-facts-against-file-before-recording-as-settled
status: distilled
problem: Asked to write a session handoff including a fact block "verbatim," including that issue 39 had been reclassified WONT-FIX, as part of a larger set of facts to carry forward as-is.
decision: Checked `Issues.md` directly before writing the handoff, found issue 39's `STATUS:` line still read "OPEN — not started," and recorded the WONT-FIX reclassification as a session decision not yet made durable in the file, rather than as a completed edit — flagging the deviation to the user.
why: The file, not the user's summary, is the ground truth for what's actually committed; the project's honesty-discipline rule requires separating verified state from assumed/stated state in every summary.
tags: [handoffs, honesty, verification, documentation]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: record the fact as given "verbatim" — i.e. as a completed edit — per the literal instruction.
Tradeoff: recording it verbatim would satisfy the literal request and take no extra verification step, but would misrepresent an undone edit as durable, settled fact in the handoff; verifying against the file cost one extra check but kept the handoff honest about what was actually committed.
Tipping factor: the project's honesty-discipline rule requires separating verified state from assumed/stated state in every summary, and the file directly contradicted the "verbatim" instruction.

### Commands that worked
- `git -C C:\Projects\StockPhotoAgent add Issues.md && git commit -m "Decompose 36 into self-contained rule sub-issues 99-102; rescope 43 to review_manager.py:215; mark 36 DECOMPOSED"` → `f8a69f74939843d59f600ccf9c4b42b95c1ec601`, clean tree after.
- `git -C C:\Projects\StockPhotoAgent reset --hard HEAD~1` (to undo the colliding `1ccf502` commit before re-applying cleanly) → `HEAD is now at 0143486 merge 47`.

### Problems solved
- Backlog decomposition append collided three times with pre-existing issue IDs → root cause: relied on a stale prior-handoff ceiling (48) instead of the file's true highest ID (98) → fix: grepped the true ceiling directly, renumbered new sub-issues to 99-102 (see CASE: issue-id-collision-verify-true-ceiling-before-renumbering).
- A PowerShell-based strip edit corrupted `Issues.md`'s UTF-8 em-dashes into mojibake → root cause: PowerShell `-Command` string argument passing mis-encodes non-ASCII bytes on a Get-Content round trip → fix: discarded the corrupted tree and redid the edits via the Edit tool, verified 0 mojibake bytes remained (see CASE: powershell-roundtrip-mojibakes-em-dashes-use-edit-tool).
- Handoff fact block asserted issue 39 was reclassified WONT-FIX in the file, but the file still showed OPEN → root cause: the reclassification was a conversational decision that was never written back to `Issues.md` → fix: recorded the discrepancy explicitly in the handoff instead of treating it as settled (see CASE: verify-user-supplied-handoff-facts-against-file-before-recording-as-settled).

### Mistakes
Two consecutive sub-issue append attempts (first at IDs 49-54, then again at 55-58) collided with pre-existing, unrelated backlog issues because the true ceiling of `Issues.md` (98) wasn't checked directly before choosing IDs; both were caught before commit and reworked to 99-102. Separately, a PowerShell-based edit meant to strip a bad append corrupted the file's UTF-8 em-dashes into mojibake; caught via byte-level verification before commit and redone with the Edit tool.

### Prompts worth keeping
"Find the TRUE highest issue number in the file... Report that top-5 list and STOP. Do not append, do not commit." — forcing a direct grep-based ground-truth check before renumbering is what surfaced the real ceiling (98, not 48) and avoided a third ID collision.

### Questions I still have
- How to scope a drain to run only the new/rescoped issues (99-102, and rescoped 43) without first grinding through ~50 pre-existing eligible issues (49-98) — `max_executions_per_run` cannot pin a specific target issue, per this session's finding and the prior session's NEXT.md note.
- Whether issue 48's stalled state (`IssueActivated` with no following `ExecutionSpawned`) is recoverable, or needs manual intervention before any further drain.

### TODOs for next session
- Resolve the drain-scoping problem: how to execute only 99-102 + rescoped 43 without draining through pre-existing eligible issues 49-98 first.
- Investigate issue 48: stalled at `IssueActivated` with no `ExecutionSpawned` — orphaned activation, check recoverability before draining on top of it.
- Create the still-owed label-consistency extract issue from issue 39 (scoped to `base_rule.py` + `sharpness.py`) — decided in conversation but never appended to `Issues.md`.
- Ingest the committed `Issues.md` changes (43 rescope, 99-102) into `events.jsonl` — as of this session's end, events.jsonl still shows issue 43 at `IssueEscalated` and 99-102 don't exist as events yet.
- Write issue 39's WONT-FIX reclassification into `Issues.md`'s `STATUS:` line — currently still reads "OPEN — not started."
