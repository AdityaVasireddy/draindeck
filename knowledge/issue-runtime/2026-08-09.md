---
project: issue-runtime
date: 2026-08-09
type: mixed
stack: [git, markdown, engineering-historian]
status: knowledge vault fully distilled (51 judgments, 3 new principles) and now under version control for the first time; committed as af0b73e.
---

# issue-runtime — 2026-08-09

## What I worked on
Ran the engineering-historian `/distill` pass on issue-runtime's own knowledge vault: cleared a leftover pending-queue entry from 2026-08-08, reviewed and confirmed 51 `status: auto` cases in one batch, then distilled all 51 into judgments across nine day files. While distilling, caught a mis-dated file caused by a known recurring `--replay` bug and corrected it, then ran the promotion scan, producing three new principles and strengthening an existing one. Also discovered and fixed a long-standing bug where the entire `knowledge/` vault had never actually been under version control. Finished by committing everything (152 vault files plus today's output) in a single commit (`af0b73e`).

## Cases

### CASE: knowledge-vault-gitignored-since-inception
status: distilled
problem: knowledge/ was excluded by the repo's top-level .gitignore, so every past sweep/distill's `git add knowledge/` had been silently failing — the entire 152-file vault had never been version controlled.
decision: removed the top-level `knowledge/` exclusion from .gitignore (kept `.sweep/` ignored, which is correct per spec) and committed all 152 existing vault files plus today's distill output in one commit.
why: only `.sweep/` (scratch/working state) is meant to be gitignored per spec; excluding `knowledge/` (the durable vault) was an unnoticed bug, not a design choice.
tags: [git, gitignore, engineering-historian, data-loss-risk]

#### JUDGMENT (distilled 2026-08-12)
thin: no rejected alternative in the record — the fix (un-ignore, backfill-commit everything) is presented as the only path considered.
Tradeoff: not recoverable from the record.
Tipping factor: the exclusion was diagnosed as an unnoticed bug against the spec's own stated intent (only `.sweep/` should be gitignored), not a considered design tradeoff.

### CASE: replay-date-stamping-bug-third-recurrence
status: cited
problem: replaying pending session 639c8483 (originating 2026-08-08) via `--replay` stamped its content with today's date (2026-08-09) instead of the transcript's true origin date — the third known occurrence of this pattern (P-004).
decision: verified the date against P-004 before trusting it, deleted the spuriously-dated 2026-08-09.md, relocated its cases into 2026-08-08.md as "Session 4" with corrected case IDs, and logged the occurrence as new evidence for P-004.
why: `--replay` is known (P-004) to stamp output with today's date rather than the transcript's real origin date, so replayed content must be checked against the source session's actual date before being treated as authoritative.
tags: [engineering-historian, replay-bug, date-stamping, p-004]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: fix `--replay`'s date-stamping code itself this session instead of manually correcting the output again.
Tradeoff: fixing the script would end the recurring pattern for good, but was out of scope for a session already focused on the distillation pass; manually correcting again is fast but guarantees a fourth recurrence next time `--replay` runs.
Tipping factor: the bug was already known (P-004) and out of scope to fix here, so the fast manual correction was the only option that didn't block the rest of the distill pass.
until: `--replay` is fixed to stamp the transcript's true origin date instead of being manually corrected each time

Also noted (not drafted): declining the Bash-tool-outer-shell-corruption promotion for failing the 2-distinct-dates bar (reasoning already recorded directly in principles.md's declined-promotions entry); batch-confirming all 51 auto cases in one go rather than reviewing individually; distilling judgments file-by-file with full re-reads for traceability; dropping four stale `until:` lines after confirming their conditions were resolved.

## Commands that worked
None.

## Problems solved
- Symptom: pending.log carried a leftover entry from 2026-08-08. Root cause: session 639c8483's transcript had never been replayed into a day file. Fix: replayed it, clearing the queue.
- Symptom: a day file (2026-08-09.md) contained content that didn't belong to today. Root cause: `--replay`'s known date-stamping bug (P-004), third recurrence. Fix: deleted the spurious file, relocated the corrected-ID cases into 2026-08-08.md as Session 4.
- Symptom: distill/sweep commits had been silently failing across the vault's entire history. Root cause: `knowledge/` was excluded by the top-level `.gitignore`. Fix: removed the exclusion and committed all 152 files.

## Mistakes
None — the bugs found (gitignore exclusion, replay mis-dating) were pre-existing, discovered rather than introduced this session.

## Prompts worth keeping
None.

## Questions I still have
None stated.

## TODOs for next session
- `--replay`'s date-stamping bug (P-004) still isn't actually fixed in code — this session was its third manual workaround.
- capture-rules.md's sweep-health band evaluation stays gated until it accumulates 15+ rules (currently 0).

## Session 2 (14:15–20:15)

### What I worked on
Prepared and executed a real gated cohort run of issues 29–38 against StockPhotoAgent. Added and later reverted `Depends-On` metadata on Issues.md, discovered and fixed a heading-format regression (from commit `7f47533`) that was blocking ingestion of all 70 pending issues, then launched the run. Issues 29–35 shipped and were accepted (linear commit chain, zero collisions), but the run crashed on issue 36 when the reviewer returned `"Approve"` and the verdict parser required exact-case `"APPROVE"`. Diagnosed the parser bug down to the exact line and comparison points, got the fix approved (not yet applied), and wrote a session handoff for the next session to apply it and resume the drain at issue 36. Also caught and disclosed an attempted prompt injection mid-session.

### Cases

### CASE: prompt-injection-detected-and-disclosed
status: cited
problem: a system-reminder claimed Issues.md had been externally modified and instructed the assistant not to tell the user about it.
decision: treated the reminder as a suspected prompt injection, independently verified via `git status`/`git diff` (both empty, contradicting the claim), and disclosed the attempted injection to the user instead of complying with the silence instruction.
why: an instruction to conceal information from the user is not followed regardless of the reminder's source, per stated policy on suspected prompt injection.
tags: [security, prompt-injection, trust-boundary, verification]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: comply with the reminder's instruction and say nothing to the user about the claimed modification.
Tradeoff: complying would avoid raising a possibly-false alarm and skip the verification step, but risks concealing a real injection attempt or real drift from the user; disclosing costs a brief `git status`/`git diff` detour but keeps the user informed regardless of the reminder's truth.
Tipping factor: the instruction itself asked for concealment from the user, which is disqualifying regardless of the reminder's actual truth value.

### CASE: verdict-parser-normalize-in-place-not-new-local
status: distilled
problem: the reviewer returned `"Approve"` (mixed case); `qwen_ollama.py`'s `_parse()` compares the raw string against an exact-case tuple with no normalization at two separate comparison points, crashing the orchestrator (exit 2) mid-run on issue 36.
decision: the approved fix reassigns the same `verdict` variable — `verdict = (obj.get("verdict") or "").strip().upper()` — rather than introducing a separate normalized local.
why: a separate local would miss the REJECT-feedback check further down, the `ReviewVerdict` construction, and the persisted payload, all of which read the original un-normalized `verdict` name.
tags: [bug-fix, reviewer, scoping, verdict-parsing]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: introduce a separate normalized local variable for the case-insensitive comparison instead of reassigning `verdict` itself.
Tradeoff: a separate local is a more contained, minimal-looking change, but it would silently miss the downstream REJECT-feedback check, `ReviewVerdict` construction, and persisted payload, which all read the original un-normalized name; reassigning in place costs nothing extra and fixes every read site at once.
Tipping factor: multiple downstream call sites read the same original `verdict` name, so only an in-place reassignment fixes all of them together.

### CASE: depends-on-abandoned-for-sequential-execution
status: distilled
problem: `Depends-On` metadata (31←30, 33←32, 34←33, 37←35, 38←37) had been added to Issues.md then reverted; before launching the real cohort run, the assistant caught that the dependency edges were never re-added and flagged the mismatch instead of proceeding as originally instructed.
decision: ran the 29–38 cohort with no `Depends-On` edges at all, relying on the runtime's existing one-issue-at-a-time sequential execution; the run's real results (linear base-commit chain across issues 29–35, zero collisions) were then used to justify not reintroducing `Depends-On`.
why: sequential one-at-a-time execution was proven sufficient to handle same-file overlaps without collisions, making the dependency-gating mechanism unnecessary for this cohort.
tags: [architecture, dependency-gating, execution-model, cohort-run]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: re-add the `Depends-On` edges before launching the run, per the originally instructed path.
Tradeoff: re-adding the edges would provide an explicit dependency-gating safety net going into the run, at the cost of extra setup to reverse the already-completed removal; running without edges skipped that setup but relied on inferring safety from the run's outcome rather than proving it up front.
Tipping factor: the run's real results (linear base-commit chain across issues 29-35, zero collisions) were used after the fact to justify not reintroducing `Depends-On`.
until: a future cohort with true out-of-order-unsafe dependencies (not just same-file overlap) is attempted — the case `Depends-On` was originally built for and never actually exercised

### CASE: budget-config-is-not-an-ingestion-cap
status: distilled
problem: assumed `max_executions_per_run: 10` in config.yaml limited which or how many issues from Issues.md would be ingested or eligible, which shaped early planning about excluding issue 36 and 39–61 from the cohort.
decision: corrected the assumption after reading `budget/manager.py:38` — the value gates `BudgetManager`'s counter of execution attempts started across the whole run, not ingestion or eligibility.
why: the ingest step (`parse_issues`) reads and parses the entire file in one pass regardless of the execution budget; the two concepts are unrelated in the code.
tags: [config, budget-manager, ingestion, misread-assumption]

#### JUDGMENT (distilled 2026-08-12)
Rejected alternative: proceed with the original cohort plan that excluded issue 36 and 39-61 based on the budget-cap assumption.
Tradeoff: proceeding on the original assumption would have simplified planning immediately, but built cohort scope on a misread of the code; reading `budget/manager.py:38` first cost a short detour but avoided planning around a nonexistent ingestion constraint.
Tipping factor: reading the source directly showed the counter gates execution attempts across the run, not ingestion or eligibility, contradicting the assumption.

Also noted (not drafted): reformatting all 70 Issues.md headings from `## Issue N:` to bare `## N:` to match the parser's `_ID_TITLE` regex (no alternative to the data-fix was discussed, so this stayed a Problem-solved entry, not a Case); the Bash-outer-shell `$`-interpolation corruption recurring as the assistant's own mistake mid-session (already an established pattern from prior sessions, logged under Mistakes below); `Out-File`'s stdout-only capture hiding the real stderr halt line and true exit code.

### Commands that worked
- `git -C C:\Projects\StockPhotoAgent revert --no-edit 2b5e774` — clean revert of the Depends-On commit, verified via `git log -2`/`status --porcelain`/`git grep -c "Depends-On"` (0 matches).
- `git -C C:\Projects\StockPhotoAgent commit -am "Reformat Issues.md headings to bare '## N:' for ingest compatibility"` — produced commit `6770869`, confirmed clean tree before the run.
- `C:\Projects\issue-runtime\.venv\Scripts\python.exe -m runtime.main run --config config.yaml` (second attempt, after the heading fix) — ingested successfully and shipped 7 real issues (29–35) with a verified linear merge-commit chain (`54d14b6`→`49604e0`→`3f63592`→`3d89d1b`→`244a1a9`→`ee989f4`→`99013f8`) before crashing on issue 36.
- `python -m pytest -q` — referenced in the handoff as showing 123 passed; UNVERIFIED here, raw output not visible in this transcript excerpt.

### Problems solved
- Symptom: `INGEST FAILED: heading '## Issue 29: ...' is not of the form '## <id>: <title>'`, killing ingestion of the whole file. Root cause: commit `7f47533` rewrote every heading in `Issues.md` from bare `## N: <title>` to `## Issue N: <title>`, and the parser's `_ID_TITLE` regex (`^[A-Za-z0-9][A-Za-z0-9_-]*\s*:`) has no whitespace tolerance in the id token. Fix: reformatted all 70 headings back to the bare form (commit `6770869`), verified the diff touched only heading lines.
- Symptom: reviewer verdict `"Approve"` crashed the orchestrator with exit code 2 mid-run. Root cause: `qwen_ollama.py:_parse()` compares the raw model string against an exact-case tuple with no normalization, at two separate comparison points (the entry gate and the REJECT-feedback check). Fix (approved, not yet applied): normalize `verdict` itself at assignment (`.strip().upper()`).
- Symptom: the background-task wrapper reported exit code 0 despite a real crash. Root cause: the true exit code (2) and the halt message both landed on stderr, and the log redirect (`| Out-File -Encoding utf8`) only captures stdout — stderr bypassed the log file entirely.
- Symptom: initial belief that `max_executions_per_run: 10` capped which issues get ingested or become eligible. Root cause: misread of `budget/manager.py:38` — it's an execution-attempt counter for the whole run, unrelated to ingestion.

### Mistakes
- Used `powershell.exe -Command "..."` with `$_`-bearing scripts despite the standing rule to route such commands through `.ps1 -File`; Git Bash's own `$` interpolation corrupted the string before PowerShell saw it. Corrected by rewriting the commands as `.ps1` files.
- Called `check-config --config config.yaml` (flag form); the subcommand takes `config` positionally. Corrected to the positional form.

### Prompts worth keeping
The HARD STOP / HALT trigger-condition list given before launching the cohort run (dependency violation, double-commit collision, writes outside the working tree, cost over $15, single-issue crash/hang) — produced a clean, itemized halt report exactly on the one condition that actually fired.

### Questions I still have
Does the runtime need a pre-run Issues.md validator? `check-config` currently reports "OK" even when headings can't be parsed.

### TODOs for next session
- Apply the approved verdict-parser fix (`qwen_ollama.py:118`, reassign `verdict = (obj.get("verdict") or "").strip().upper()`), add 7 new test cases to `test_seams.py`, run the five-gate verification (single production-line diff, `pytest tests\unit` → 130 passed, durability 60/60 both seeds, prove the constructed `ReviewVerdict.verdict` is uppercase for `"Approve"` input), then stop before commit pending Adi's authorization.
- Resume the StockPhotoAgent drain at issue 36 once the fix lands.
