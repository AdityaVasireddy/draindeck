---
project: issue-runtime
date: 2026-07-18
type: mixed
stack: [python, pyyaml, pydantic, claude-cli, bash, git, windows]
status: ADR-22 discriminating positive control restored and reviewer-certified; Step 3 blocked only on its own five preconditions; all doc/config changes uncommitted per standing no-commit rule.
---

# issue-runtime — 2026-07-18

## What I worked on
Two project-sessions ran in this transcript. Session 10 executed Item 0 live — the end-to-end argv-survival witness through the real, unmodified `ClaudeHeadlessEngine.run()` against real `claude` 2.1.212 — and recorded it as RUN/CLEAN-WITH-CAVEAT because the positive control (pass 2) failed to go red. Session 11 then fixed a corrupted duplicate `child_env:` key in config.yaml, designed and executed a synthetic zero-gate hook as a replacement positive control (Steps A–D: Step B went red at t=0s, Step C stayed clean across 450s at CLI 2.1.214), and survived an adversarial review ruling that demoted the initial pass claims to INFERRED until raw artifacts were pasted. The session ended with the ADR-22/vacuity design gate reviewer-certified as discharged and a handoff written to `docs/handoffs/HANDOFF_2026-07-18_adr22-vacuity-control-restored.md`. NEXT.md, config.yaml, and docs/14 all carry uncommitted working-tree changes reflecting this.

## Cases

### CASE: item0-recorded-clean-with-caveat
status: distilled
problem: Item 0 pass 1 (real composed spawn path) came back fully clean, but pass 2 — the positive control with `--setting-sources ""` deliberately stripped — also came back clean, the third independent non-reproduction of contamination.
decision: Recorded Item 0 as RUN/CLEAN-WITH-CAVEAT in docs/14 §2.6 and NEXT.md, explicitly not as an unqualified "ADR-22 proven working" close.
why: Item 0's gate condition required a discriminating positive control; every green in the family is confounded by the possibility that nothing on the machine is currently contaminable (the INFERRED upstream historian patch).
tags: [positive-control, vacuity, evidence-discipline, adr-22]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: recording Item 0 as an unqualified "ADR-22 proven working" close, since pass 1 (the real composed spawn path) came back fully clean.
Tradeoff: an unqualified close reads as a stronger result and lets Step 3 proceed without a caveat, but every green result in the family is confounded by the possibility that nothing on the machine is currently contaminable (the INFERRED upstream patch); RUN/CLEAN-WITH-CAVEAT is a weaker-sounding close but honestly reflects that the positive control itself failed to discriminate.
Tipping factor: pass 2, the positive control with `--setting-sources ""` deliberately stripped, also came back clean — the third independent non-reproduction — so the gate's discriminating requirement was not met.

### CASE: config-childenv-duplicate-from-edit-anchor
status: distilled
problem: config.yaml carried a duplicate `child_env:` key that PyYAML `safe_load` silently parsed as a nested key (`{'child_env': None, ...}`), surfacing only as a pydantic ConfigError in `load_config()`, not a YAML syntax error.
decision: Surgically deleted the single duplicate header line (working tree only) and confirmed via a live `load_config()` call; diagnosed the cause as failure mode (a) — a prior Edit-tool replacement anchored below the `child_env:` header, appending a copy instead of replacing in place.
why: The old-comment-verbatim-immediately-followed-by-new-comment signature fits an Edit anchored at `HISTORIAN_SWEEP_ACTIVE:` rather than the header; (c) misread was ruled out mechanically by `git diff` plus the live YAML load, and (b) organic drift wouldn't reproduce the old text verbatim next to the new.
tags: [yaml, pyyaml, edit-tool, silent-failure, config]
until: fix lands in a commit

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: attributing the duplicate key to (b) organic drift or (c) a misread of the file, rather than (a) an Edit-tool replacement anchored below the header that appended instead of replacing in place.
Tradeoff: accepting (b) or (c) would require no further mechanical check, but (b) doesn't explain the old comment text surviving verbatim next to new text, and (c) was already ruled out by `git diff` plus a live YAML load; (a) required tracing the exact anchor/replacement signature but matched the evidence precisely.
Tipping factor: the old-comment-verbatim-immediately-followed-by-new-comment signature is the specific fingerprint of an Edit anchored below the header rather than at it — a pattern (b) and (c) don't produce.

### CASE: synthetic-zero-gate-hook-restores-adr22-control
status: distilled
problem: With the real historian hook apparently patched upstream, no working positive control existed to prove ADR-22's `--setting-sources ""` isolation does protective work — Step 3 was held on this.
decision: Chose Option (a): built and ran a project-owned synthetic zero-gate SessionEnd hook in project-scope `.claude/settings.json`, with Step B (go-red validation with pid/event/cwd disambiguation) gating Step C (real A-empty comparison), recorded with both-halves framing — control restored going forward, original root-cause claim permanently INFERRED.
why: The counter-case for Option (b) — a synthetic hook only proves `--setting-sources` behaves as documented, not that it would have stopped the specific original bug — was weighed and accepted as a known permanent limitation, not a reason to skip a forward-looking discriminating control re-armable on any future CLI re-pin.
tags: [positive-control, hooks, adr-22, synthetic-assay, vacuity]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: Option (b), retiring the control entirely and relying on structural/unit evidence instead of a live discriminating probe.
Tradeoff: retiring the control is simpler and avoids maintaining a synthetic hook, but gives up any forward-looking, re-armable way to prove the suppression mechanism still does real work on future CLI re-pins; building the synthetic hook costs upkeep but restores a genuine, if narrower, control.
Tipping factor: the counter-case for (b) — that a synthetic hook only proves `--setting-sources` behaves as documented, not that it would have stopped the specific original bug — was weighed and accepted as a known permanent limitation, not a reason to give up a forward-looking control re-armable on any future re-pin.

### CASE: raw-artifact-paste-over-self-report-for-step-b
status: cited
problem: The reviewer ruled Step B's "went red" claim INFERRED, not VERIFIED — the summary was executor narration describing a witnessed event, not the witnessed artifact itself, which is exactly the self-report class the evidence discipline forbids.
decision: Re-verified by pasting the raw projections: `cat` of the marker file (event/pid/ts/cwd literals), literal pid comparison (22956 ≠ 229655), the empty-probe `No such file or directory`, verbatim NEXT.md/doc-14 wording, and CLI version read from both transcripts' `claude_code_version` field (2.1.214).
why: A pass/fail claim must be a mechanical projection from witnessed output, and "the last inch is where vacuity hides" — a clean Step C is only meaningful against a Step B proven red from artifacts, not asserted red.
tags: [evidence-discipline, verification, review-gate, self-report]

#### JUDGMENT (distilled 2026-07-25)
Rejected alternative: accepting the executor's narrated claim that Step B "went red" as sufficient evidence, since it described a real, witnessed event.
Tradeoff: accepting the narration is faster and requires no further digging, but a description of a witnessed event carries the same trust problem as a description of any other result — it can't be checked against what actually happened; pasting the raw marker file, pid comparison, and version fields costs a re-verification pass but makes the claim mechanically checkable.
Tipping factor: the reviewer ruled that a self-report of a witnessed event is exactly the self-report class the evidence discipline forbids — only the raw projection (the marker file, the literal pid, the verbatim wording) counts as a pass/fail claim.

## Commands that worked
- `cat probe_cwd_trigger/.synth-marker/fired.txt` — read the synthetic hook's marker directly off disk to promote Step B to VERIFIED; output shown in transcript (event=SessionEnd, pid=229655, cwd match).
- Both background witness scripts (Item 0 passes 1–2; Steps B–C) completed with exit code 0 per task notifications, but their invocation lines are not in the transcript — UNVERIFIED as verbatim commands.

## Problems solved
- config.yaml failed to load through `load_config()` → duplicate `child_env:` key silently nested by PyYAML, tripping pydantic validation (`child_env.child_env — Input should be a valid string`) → removed the duplicate line; `load_config()` confirmed returning `{'HISTORIAN_SWEEP_ACTIVE': '1'}`.
- Positive control for ADR-22 non-discriminating (three independent non-reproductions) → likely cause is the INFERRED upstream historian-script patch removing the vulnerable path on this machine → replaced with a project-owned synthetic zero-gate hook that verifiably goes red (Step B) and is verifiably suppressed by the production argv (Step C).

## Mistakes
- Reported Step B/C as VERIFIED from the executor's own summary; the reviewer correctly demoted this to INFERRED. Corrected by pasting raw marker contents, literal pids, and file wording, after which all items went VERIFIED.
- The Step B/C witness script omitted a separate `claude --version` capture (unlike Item 0's script); recovered mechanically from the `claude_code_version` field inside both generated transcripts (2.1.214).

## Prompts worth keeping
- "Ruling. One item fails observability, and it changes the disposition of everything downstream." — the reviewer ruling distinguishing self-report-of-a-witnessed-event from the witnessed artifact; it caught the exact confound the project exists to eliminate and forced the raw-evidence paste.

## Questions I still have
- Whether the STANDING TICKLE's Probe 2/3 needs an explicit re-probe at CLI 2.1.214 before a live Step 3 run leans on it (currently only 2.1.212-witnessed); the user framed it as optional.
- Which prior session's edit produced the config.yaml duplication — diagnosis (a) is INFERRED from structural fingerprint only, with no tool-call log witnessed.
- (Recorded as permanently unresolvable, not open: whether A-empty stopped the original historian contamination — no pre-patch artifact survives.)

## TODOs for next session
- Commit the pending working-tree changes (config.yaml fix, NEXT.md, docs/14) when the user explicitly requests — standing no-commit rule.
- Re-run doc 14 §2.4's Probe 2/3 at CLI 2.1.214 before leaning on the STANDING TICKLE (named as next action in the handoff).
- Clear Step 3's five own preconditions, all user-owned and untouched this session: real validation command in config.yaml, Ollama with qwen2.5-coder pulled, Issues.md relocated/reformatted, baseline green, gitignore hygiene.
